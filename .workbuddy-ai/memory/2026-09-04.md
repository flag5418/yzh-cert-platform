# 2026-09-04

## 输出产物分析 → 编制《审核员端落地与前端治理-执行计划-V1》
- 用户要求：分析 `输出产物/` 目录（6 个文件：审核员端原型 V1/V1.1、vol.web 质量报告全量版/分层版 V1/V1.1）并给详细执行计划。
- 产出：`输出产物/审核员端落地与前端治理-执行计划-V1.md`（M0-M5 + Q 七组里程碑，32-42 人天）。

## 实测现状（2026-09-04 扫描，重要基线）
- 审核员端实际只有 `views/cert_admin/`：`Login.vue` 471 行 + `Workspace.vue` 431 行 = 902 行；`TaskDetail/`、`Enterprise/`、`Message/`、`Profile/`、`components/` **全是空目录**；`router/viewGird.auditor.js` 是 15 行空占位。
- 已对接接口仅 5 个：User/getVierificationCode、AuditorAuth/GetOrgList、user/login、AuditorAuth/GetCurrentUser、AuditorAuth/Register（后端 `Auditor/Partial/AuthController.cs` 236 行，就这 3 个接口）。
- 后端缺口：`AuditTaskController.cs` 21 行框架空壳（无 Partial），`AuditTaskService` 只有 `GetPageData`；导出 `/api/TaskExport/*` 完全缺失。已有：Enterprise(5)、EnterpriseFile(6)、Message(4)、Queue、WorkflowDefinition、ReportDefinition。
- DB 已有 audit_task / ent_enterprise / ent_enterprise_phase / audit_nonconformity / rpt_audit_report / cert_message 等；待建：ent_enterprise_contact、sys_notify_config、audit_task.is_focused。
- 目录重组已执行：`views/` 下出现 admin/(cert,sys)、cert_sys/、shared/；路由拆出 viewGird.admin.js(183)。工作树 27 项未提交（删 builder/formDraggable/signalR + 清理 MES 菜单脚本）。

## 识别出的 8 项 P0 冲突（C1-C8）
- C1-C5 来自评测报告：阶段模型 X/Y、enterprise_no 语义、微信绑定、资料清单导出、免费额度 10 次。
- **C6（新发现）**：文档要求 `views/auditor/`，代码实际是 `views/cert_admin/`（提交 5325681 把 auditor/ 改名 cert_admin/）→ 建议改回 auditor/，现在只有 2 个文件，成本最低。
- **C7（新发现）**：路由双前缀并存，已注册 `/auditor/*` 但守卫内部仍跳 `/cert_admin/workspace`。
- **C8（新发现）**：`src/auditor`（Naive UI 骨架 531 行）架构决策写"保留但冻结"，实测无业务价值 → 建议物理删除（历史留 git）。
- 另：两份质量报告 V1.1 的 §0.1 称"auditor 是独立前端 9991"已被 9/3 架构决策（方案 C 单前端多角色）推翻，且 console 149 处已于 8/25 清零 → 报告需升 V1.2。

## 关键文档时间线（避免再被过期文档误导）
- 9/1 质量报告 V1.1（基于已废弃的独立前端假设）
- 9/3 10:46 架构选型决策：方案 C，vol.web 唯一前端，抛弃 Naive UI，Vuex + Pinia 共存
- 9/3 10:52 审核员端详细设计（可开发级，含 Phase1-6）
- 9/3 12:26 目录结构与角色拆分设计（最新，登录入口 /admin-login、/auditor-login，目录 views/auditor/）

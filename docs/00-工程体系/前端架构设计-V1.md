# 前端架构设计（V1）— 原子化组件 + 基类继承

> **版本**：V1.0 | **日期**：2026-09-07 | **状态**：设计中
>
> 本文档定义：前端（admin + auditor）的组件原子化策略、页面基类设计、与后端约定。
> **本文件为前端架构的唯一权威表述**；后续前端编码必须对齐本文。

---

## 一、核心理念

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端架构核心理念                              │
├─────────────────────────────────────────────────────────────────────┤
│  1. 原子化组件    → 组件与接口无关，通过 props 接收数据、emit 事件    │
│  2. 配置驱动      → 表格列、表单字段由后端 GridConfig 统一提供        │
│  3. 基类继承      → 80% 逻辑由 CrudPageLogic 基类提供，子类可覆盖   │
│  4. ID 唯一性     → 动态生成组件唯一 ID，支持级联和多实例场景        │
│  5. Enable 控制   → 外部带入数据自动 disabled，前端零判断            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、分层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│  管理员端 / 审核员端                                                  │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │  pages/（页面层）                                                 │ │
│  │  ├── system/user/                                                 │ │
│  │  │   ├── index.vue      ← 纯 UI 模板                            │ │
│  │  │   └── index.ts       ← 可选：仅复杂页面需要                   │ │
│  │  └── ...                                                          │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                              ↓ 使用                                  │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │  share/logic/（逻辑层）                                           │ │
│  │  ├── CrudPageLogic.ts  ← 页面基类，封装 80% 通用逻辑            │ │
│  │  └── ...                ← 其他业务逻辑基类                       │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                              ↓ 使用                                  │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │  yzh.vue.core/（原子组件层）                                      │ │
│  │  ├── YzhSearchBar  搜索栏     YzhToolbar   工具栏                 │ │
│  │  ├── YzhTable      表格       YzhPagination 分页                  │ │
│  │  ├── YzhForm       表单       YzhDialog    弹窗                  │ │
│  │  └── YzhTree       树形       YzhStatusBadge 状态标签             │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                              ↓ 使用                                  │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │  yzh.vue.core/api/（接口层）                                      │ │
│  │  └── YzhApiClient  ← 自动注入 Token，归一化响应格式              │ │
│  └─────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、原子化组件设计

### 3.1 设计原则

| 原则 | 说明 |
|------|------|
| **接口无关** | 组件不直接调用 API，通过 props 接收数据 |
| **事件驱动** | 组件通过 emit 向父组件传递事件 |
| **独立可用** | 每个组件可独立使用，也可组合使用 |

### 3.2 组件清单

#### YzhSearchBar（搜索栏）

```typescript
// Props
interface Props {
  fields: SearchField[]      // 搜索字段配置
  defaultValue?: Record<string, any>  // 默认值
  maxFields?: number         // 默认显示字段数（默认 2）
  collapsed?: boolean        // 默认是否折叠
}

// Events
@search(params: Record<string, any>)    // 点击搜索
@reset()                                 // 点击重置
@expand()                                // 展开更多

// ID
:id="searchBarId"   // 由基类自动生成
```

#### YzhToolbar（工具栏）

```typescript
// Props
interface Props {
  buttons?: ToolbarButton[]  // 按钮配置（可从基类获取）
  disabled?: boolean         // 全部禁用
}

// Events
@click(buttonKey: string)    // 点击按钮

// Slots
#left                        // 左侧按钮区域
#right                       // 右侧按钮区域
```

#### YzhTable（表格）

```typescript
// Props
interface Props {
  columns: YzhTableColumn[]  // 列配置
  data: any[]                // 数据源
  loading?: boolean          // 加载状态
  selectable?: boolean       // 是否可多选
  showPagination?: boolean   // 显示分页（默认 true）
  defaultSort?: DefaultSort  // 默认排序
  rowKey?: string            // 行键字段（默认 'id'）
  height?: string | number   // 表格高度
}

// Events
@sort-change(prop: string, order: string)   // 排序变化
@selection-change(rows: any[])               // 选择变化
@row-click(row: any, index: number)         // 行点击

// Slots
#column-{prop}                               // 自定义列渲染
#toolbar-left                                // 工具栏左侧
#toolbar-right                               // 工具栏右侧
```

#### YzhForm（表单）

```typescript
// Props
interface Props {
  fields: FormField[]        // 字段配置
  modelValue: Record<string, any>  // 表单数据（v-model）
  labelWidth?: string        // 标签宽度（默认 100px）
  cols?: 1 | 2 | 3 | 4     // 列数（默认 2）
  loading?: boolean          // 提交中状态
}

// Events
@submit(formData: any)       // 提交
@reset()                     // 重置
@validate(valid: boolean)     // 校验结果

// Slots
#field-{prop}                // 自定义字段渲染
#actions                     // 自定义操作按钮
```

#### YzhPagination（分页）

```typescript
// Props
interface Props {
  page: number               // 当前页
  pageSize: number           // 每页条数
  total: number              // 总条数
  pageSizes?: number[]       // 可选每页条数（默认 [10, 20, 50, 100]）
}

// Events
@update:page(page: number)          // 页码变化
@update:page-size(size: number)     // 每页条数变化
```

#### YzhDialog（弹窗）

```typescript
// Props
interface Props {
  modelValue: boolean        // v-model 显示状态
  title?: string             // 标题
  width?: string | number    // 宽度（默认 600px）
  fullscreen?: boolean       // 全屏
  showFooter?: boolean       // 显示底部按钮（默认 true）
  confirmText?: string       // 确认按钮文字
  cancelText?: string        // 取消按钮文字
}

// Events
@confirm()                   // 确认
@cancel()                    // 取消

// Slots
#default                     // 内容区域
#footer                      // 自定义底部
```

#### YzhTree（树形）

```typescript
// Props
interface Props {
  data: TreeNode[]           // 树数据
  props?: TreeProps          // 字段映射
  nodeKey?: string           // 节点唯一键
  showCheckbox?: boolean     // 显示复选框
  defaultExpandAll?: boolean // 默认展开
  highlightCurrent?: boolean // 高亮当前
}

// Events
@node-click(node: TreeNode)              // 节点点击
@check-change(checkedNodes: TreeNode[])   // 选中变化
```

---

## 四、页面基类设计

### 4.1 后端接口约定

后端 `YzhControllerBase<T>` 提供：

| 接口 | 方法 | 请求体 | 响应 |
|------|------|--------|------|
| `/api/{Controller}/config` | GET | - | `GridConfig` |
| `/api/{Controller}/page` | POST | `PageRequest` | `PageResult<T>` |
| `/api/{Controller}/add` | POST | `T` | `T` |
| `/api/{Controller}/update` | POST | `T` | `T` |
| `/api/{Controller}/delete` | POST | `string[]` | `void` |
| `/api/{Controller}/action/{name}` | POST | `T` | `any` |

### 4.2 GridConfig 数据结构

```typescript
interface GridConfig {
  configName: string              // 配置名称
  tableName: string               // 表名
  primaryKey: string              // 主键（默认 Id）
  title: string                   // 页面标题
  layoutColumns: number           // 表单列数（默认 2）
  columns: DefineColumn[]         // 列定义
  toolbar: ToolbarConfig          // 工具栏配置
  rowButtons: RowButtonConfig     // 行按钮配置
}

interface DefineColumn {
  fieldName: string               // 字段名（PascalCase）
  desName: string                 // 显示标题
  width: number                   // 宽度
  type: ControlType               // 控件类型
  xsFlag: boolean                 // 表格显示标志
  bcFlag: boolean                 // 保存标志
  yxk: boolean                    // 允许空（true = 非必填）
  enable: boolean                 // 是否可编辑（false = disabled）
  sortable: boolean               // 可排序
  fixed: string                   // 固定列：left/right/null
  align: string                   // 对齐：left/center/right
  dictCode: string                // 字典编码
  options: SelectOption[]         // 下拉选项
  format: string                  // 格式化字符串
}
```

### 4.3 CrudPageLogic 基类

```typescript
// share/src/logic/CrudPageLogic.ts

export class CrudPageLogic<T extends Record<string, any>> {
  // ==================== 页面标识 ====================
  readonly controllerName: string
  readonly pageId: string
  
  // ==================== 后端配置 ====================
  config = ref<GridConfig | null>(null)
  
  // ==================== 表格状态 ====================
  rows = ref<T[]>([])
  loading = ref(false)
  selectedRows = ref<T[]>([])
  
  // ==================== 分页状态 ====================
  pagination = reactive({ page: 1, pageSize: 20, total: 0 })
  
  // ==================== 搜索状态 ====================
  searchParams = reactive<Record<string, any>>({})
  
  // ==================== 排序状态 ====================
  sortField = ref<string | undefined>()
  sortOrder = ref<'asc' | 'desc' | undefined>()
  
  // ==================== 弹窗状态 ====================
  dialogVisible = ref(false)
  dialogMode = ref<'add' | 'edit'>('add')
  submitting = ref(false)
  formData = reactive<Partial<T>>({})
  
  // ==================== 计算属性 ====================
  
  /** 表格列配置 */
  get columns(): YzhTableColumn<T>[] {
    return this.config.value?.columns
      ?.filter(c => c.xsFlag)
      ?.map(c => ({
        prop: this.toCamelCase(c.fieldName),
        label: c.desName,
        width: Number(c.width) || undefined,
        sortable: c.sortable || undefined,
        fixed: c.fixed as any,
        align: c.align as any,
        dictCode: c.dictCode,
        formatter: c.format ? (v: any) => this.formatValue(v, c.format) : undefined
      })) || []
  }
  
  /** 表单字段配置 */
  get formFields(): YzhFormField[] {
    return this.config.value?.columns
      ?.filter(c => c.bcFlag)
      ?.map(c => ({
        prop: this.toCamelCase(c.fieldName),
        label: c.desName,
        type: this.mapControlType(c.type),
        required: !c.yxk,
        disabled: !c.enable,           // ← enable 控制
        span: 24 / (this.config.value?.layoutColumns || 2),
        dictCode: c.dictCode,
        options: c.options,
        fieldProps: { id: this.fieldId(c.fieldName) }  // ← 动态 ID
      })) || []
  }
  
  /** 工具栏按钮 */
  get toolbarButtons(): ToolbarButton[] {
    const tb = this.config.value?.toolbar
    const btns: ToolbarButton[] = []
    if (tb?.add !== false) btns.push({ key: 'add', text: '新增', type: 'primary' })
    if (tb?.delete !== false) btns.push({ key: 'delete', text: '批量删除', type: 'danger' })
    if (tb?.export !== false) btns.push({ key: 'export', text: '导出', type: 'success' })
    return btns
  }
  
  // ==================== 构造函数 ====================
  
  constructor(controllerName: string) {
    this.controllerName = controllerName
    this.pageId = `${controllerName.replace(/Controller$/, '')}_${this.randomId()}`
  }
  
  // ==================== 初始化 ====================
  
  async init() {
    await this.loadConfig()
    await this.loadData()
  }
  
  protected async loadConfig() {
    this.config.value = await yzhApi.get<GridConfig>(
      `/api/${this.controllerName}/config`
    )
  }
  
  // ==================== 数据操作（可覆盖） ====================
  
  async loadData() {
    this.loading.value = true
    try {
      const res = await yzhApi.post<PageResult<T>>(`/api/${this.controllerName}/page`, {
        page: this.pagination.page,
        rows: this.pagination.pageSize,
        sort: this.sortField.value,
        order: this.sortOrder.value,
        conditions: this.buildConditions()
      })
      this.rows.value = res.rows
      this.pagination.total = res.total
      this.onDataLoaded(res.rows)
    } finally {
      this.loading.value = false
    }
  }
  
  protected async add(entity: Partial<T>): Promise<void> {
    await yzhApi.post(`/api/${this.controllerName}/add`, entity)
  }
  
  protected async update(entity: Partial<T>): Promise<void> {
    await yzhApi.post(`/api/${this.controllerName}/update`, entity)
  }
  
  protected async delete(codes: string[]): Promise<void> {
    await yzhApi.post(`/api/${this.controllerName}/delete`, codes)
  }
  
  async executeAction(actionName: string, row: T) {
    await yzhApi.post(`/api/${this.controllerName}/action/${actionName}`, row)
    await this.loadData()
  }
  
  // ==================== 钩子方法（子类可覆盖） ====================
  
  protected onDataLoaded(rows: T[]) {}
  protected onBeforeAdd(entity: Partial<T>) {}
  protected onAfterAdd(entity: Partial<T>) {}
  protected onBeforeUpdate(entity: Partial<T>) {}
  protected onDelete(codes: string[]): boolean | Promise<boolean> { return true }
  protected onPrepareAdd(entity: Partial<T>) {}
  
  protected buildConditions() {
    return Object.entries(this.searchParams)
      .filter(([, v]) => v != null && v !== '')
      .map(([field, value]) => ({ field, value, operator: 'eq' }))
  }
  
  // ==================== 事件处理 ====================
  
  onSearch(params: Record<string, any>) {
    Object.assign(this.searchParams, params)
    this.pagination.page = 1
    this.loadData()
  }
  
  onPageChange(page: number) {
    this.pagination.page = page
    this.loadData()
  }
  
  onSizeChange(size: number) {
    this.pagination.pageSize = size
    this.pagination.page = 1
    this.loadData()
  }
  
  onSortChange(prop: string, order: 'asc' | 'desc') {
    this.sortField.value = prop
    this.sortOrder.value = order
    this.loadData()
  }
  
  onSelectionChange(rows: T[]) {
    this.selectedRows.value = rows
  }
  
  // ==================== 弹窗操作 ====================
  
  openAddDialog() {
    this.dialogMode.value = 'add'
    Object.keys(this.formData).forEach(k => delete this.formData[k])
    this.onPrepareAdd(this.formData)
    this.dialogVisible.value = true
  }
  
  openEditDialog(row: T) {
    this.dialogMode.value = 'edit'
    Object.assign(this.formData, row)
    this.dialogVisible.value = true
  }
  
  async cancelDialog() {
    this.dialogVisible.value = false
  }
  
  async submitForm() {
    this.submitting.value = true
    try {
      if (this.dialogMode.value === 'add') {
        this.onBeforeAdd(this.formData)
        await this.add(this.formData)
        this.onAfterAdd(this.formData)
      } else {
        this.onBeforeUpdate(this.formData)
        await this.update(this.formData)
      }
      ElMessage.success('保存成功')
      this.dialogVisible.value = false
      await this.loadData()
    } finally {
      this.submitting.value = false
    }
  }
  
  async confirmDelete(rows?: T[]) {
    const targets = rows || this.selectedRows.value
    if (targets.length === 0) return
    
    const pk = this.config.value?.primaryKey?.toLowerCase() || 'code'
    const codes = targets.map(r => r[pk]).filter(Boolean)
    
    const canDelete = await this.onDelete(codes)
    if (!canDelete) return
    
    await ElMessageBox.confirm(`确定删除 ${codes.length} 条记录？`, '删除确认')
    await this.delete(codes)
    ElMessage.success('删除成功')
    await this.loadData()
  }
  
  // ==================== ID 生成 ====================
  
  /** 生成组件唯一 ID */
  id(type: string, suffix?: string): string {
    return suffix ? `${this.pageId}_${type}_${suffix}` : `${this.pageId}_${type}`
  }
  
  /** 生成字段 ID */
  fieldId(fieldName: string): string {
    return `${this.pageId}_field_${fieldName}`
  }
  
  get tableId(): string { return this.id('table') }
  get searchBarId(): string { return this.id('searchbar') }
  get formId(): string { return this.id('form') }
  get dialogId(): string { return this.id('dialog') }
  get paginationId(): string { return this.id('pagination') }
  get toolbarId(): string { return this.id('toolbar') }
  
  // ==================== 工具方法 ====================
  
  protected toCamelCase(str: string): string {
    return str.charAt(0).toLowerCase() + str.slice(1)
  }
  
  private mapControlType(type: ControlType): YzhFieldType {
    const map: Record<ControlType, YzhFieldType> = {
      TextBox: 'text',
      TextArea: 'textarea',
      NumberBox: 'number',
      DatePicker: 'date',
      DateTimePicker: 'datetime',
      ComboBox: 'select',
      DropDownList: 'select',
      RadioButtonList: 'radio',
      CheckBox: 'checkbox',
      Switch: 'switch',
      Upload: 'upload',
      TreeSelect: 'treeSelect',
      Cascader: 'cascader'
    }
    return map[type] || 'text'
  }
  
  private formatValue(value: any, format: string): string {
    // TODO: 实现日期/数字格式化
    return value
  }
  
  private randomId(len = 8): string {
    return Math.random().toString(36).slice(2, 2 + len)
  }
}
```

### 4.4 TypeScript Override 用法

```typescript
// pages/system/user/index.ts

import { CrudPageLogic } from '@share/logic/CrudPageLogic'
import { ElMessage } from 'element-plus'
import type { SysUser } from '@share/types/user'

export class UserPageLogic extends CrudPageLogic<SysUser> {
  
  // ===== 覆盖钩子 =====
  
  /** 新增默认值 */
  override onPrepareAdd(entity: Partial<SysUser>) {
    entity.enable = 1
  }
  
  /** 禁止删除管理员 */
  override onDelete(codes: string[]): boolean {
    if (codes.includes('admin')) {
      ElMessage.warning('不能删除系统管理员')
      return false
    }
    return true
  }
  
  /** 数据加载后脱敏处理 */
  override onDataLoaded(rows: SysUser[]) {
    rows.forEach(r => {
      if (r.phoneNo) {
        r.phoneNo = r.phoneNo.replace(/(\d{3})\d{4}(\d{4})/, '$1****$2')
      }
    })
  }
  
  // ===== 扩展方法 =====
  
  /** 重置密码 */
  async resetPassword(userId: number) {
    await ElMessageBox.confirm('确定重置该用户密码？')
    await this.executeAction('ResetPassword', { id: userId } as SysUser)
    ElMessage.success('密码已重置')
  }
}
```

---

## 五、页面结构规范

### 5.1 简单页面（纯列表展示）

```
pages/business/iso-standard/
└── index.vue    ← 唯一文件
```

```vue
<script setup lang="ts">
import { CrudPageLogic } from '@share/logic/CrudPageLogic'
import type { ISOStandard } from '@share/types/cert'

const page = new CrudPageLogic<ISOStandard>('ISOStandard')
await page.init()
</script>

<template>
  <div class="page-crud">
    <YzhSearchBar :id="page.searchBarId" @search="page.onSearch" @reset="page.loadData" />
    <YzhTable :id="page.tableId" :columns="page.columns" :data="page.rows" :loading="page.loading" />
    <YzhPagination :id="page.paginationId" :page="page.pagination.page" :page-size="page.pagination.pageSize" :total="page.pagination.total" @update:page="page.onPageChange" @update:page-size="page.onSizeChange" />
  </div>
</template>
```

### 5.2 标准 CRUD 页面

```
pages/system/user/
├── index.vue    ← 视图模板
└── index.ts     ← 继承基类，覆盖特殊逻辑
```

```vue
<!-- index.vue -->
<script setup lang="ts">
import { UserPageLogic } from './index'

const page = new UserPageLogic('Sys_User')
await page.init()
</script>

<template>
  <div class="page-crud">
    <YzhSearchBar :id="page.searchBarId" @search="page.onSearch" @reset="page.loadData" />
    
    <YzhToolbar :id="page.toolbarId">
      <template #left>
        <el-button type="primary" @click="page.openAddDialog">新增</el-button>
        <el-button type="danger" :disabled="!page.selectedRows.length" @click="page.confirmDelete()">批量删除</el-button>
      </template>
    </YzhToolbar>
    
    <YzhTable
      :id="page.tableId"
      :columns="page.columns"
      :data="page.rows"
      :loading="page.loading"
      selectable
      @selection-change="page.onSelectionChange"
      @sort-change="({ prop, order }) => page.onSortChange(prop, order === 'ascending' ? 'asc' : 'desc')"
    >
      <template #column-enable="{ row }">
        <el-tag :type="row.enable ? 'success' : 'info'">{{ row.enable ? '启用' : '禁用' }}</el-tag>
      </template>
      <template #column-actions="{ row }">
        <el-button text type="primary" @click="page.openEditDialog(row)">编辑</el-button>
        <el-button text type="warning" @click="page.resetPassword(row.id)">重置密码</el-button>
        <el-button text type="danger" @click="page.confirmDelete([row])">删除</el-button>
      </template>
    </YzhTable>
    
    <YzhPagination :id="page.paginationId" :page="page.pagination.page" :page-size="page.pagination.pageSize" :total="page.pagination.total" @update:page="page.onPageChange" @update:page-size="page.onSizeChange" />
    
    <YzhDialog :id="page.dialogId" v-model="page.dialogVisible" :title="page.dialogMode === 'add' ? '新增用户' : '编辑用户'" @confirm="page.submitForm" @cancel="page.dialogVisible = false">
      <YzhForm :id="page.formId" v-model="page.formData" :fields="page.formFields" :loading="page.submitting" />
    </YzhDialog>
  </div>
</template>
```

### 5.3 复杂定制页面

```
pages/business/workflow/
├── index.vue         ← 主视图
├── index.ts          ← 页面逻辑
├── NodePanel.vue     ← 自定义节点面板
└── hooks/
    └── useDragNode.ts  ← 拖拽逻辑
```

---

## 六、Enable 控制机制

### 6.1 后端配置

```json
// GET /api/Sys_User/config 响应示例
{
  "columns": [
    { "fieldName": "UserName", "desName": "用户名", "enable": true, "bcFlag": true },
    { "fieldName": "CreateDate", "desName": "创建时间", "enable": false, "bcFlag": false },
    { "fieldName": "OrgName", "desName": "所属机构", "enable": false, "bcFlag": false }
  ]
}
```

### 6.2 前端效果

| Enable | BCFlag | 表格显示 | 表单显示 | 表单状态 |
|--------|--------|---------|---------|---------|
| true | true | ✅ | ✅ | 可编辑 |
| true | false | ✅ | ❌ | - |
| false | true | ✅ | ✅ | disabled（灰显） |
| false | false | ❌ | ❌ | - |

---

## 七、动态 ID 应用场景

### 7.1 多实例隔离

```vue
<!-- 同一个页面打开多个实例 -->
<UserPage />
<!-- 生成 ID：Sys_User_8f3a2b1c_table_xxx -->

<UserPage />
<!-- 生成 ID：Sys_User_9e4c3d2e_table_xxx -->
```

### 7.2 级联操作

```vue
<template>
  <!-- 省市区三级级联 -->
  <YzhFormItem :id="page.fieldId('provinceId')">
    <el-select v-model="formData.provinceId" @change="onProvinceChange">
      <el-option v-for="p in provinces" :key="p.id" :label="p.name" :value="p.id" />
    </el-select>
  </YzhFormItem>
  
  <YzhFormItem :id="page.fieldId('cityId')">
    <el-select v-model="formData.cityId" @change="onCityChange" :disabled="!formData.provinceId">
      <el-option v-for="c in filteredCities" :key="c.id" :label="c.name" :value="c.id" />
    </el-select>
  </YzhFormItem>
  
  <YzhFormItem :id="page.fieldId('districtId')">
    <el-select v-model="formData.districtId" :disabled="!formData.cityId">
      <el-option v-for="d in filteredDistricts" :key="d.id" :label="d.name" :value="d.id" />
    </el-select>
  </YzhFormItem>
</template>

<script setup>
function onProvinceChange(provinceId) {
  formData.cityId = undefined
  formData.districtId = undefined
  filteredCities.value = await fetchCities(provinceId)
}

function onCityChange(cityId) {
  formData.districtId = undefined
  filteredDistricts.value = await fetchDistricts(cityId)
}
</script>
```

---

## 八、组件自由组合示例

### 8.1 左树右表布局

```vue
<template>
  <div class="tree-table-layout">
    <div class="tree-panel">
      <YzhTree :id="page.id('tree')" :data="treeData" @node-change="onNodeChange" />
    </div>
    <div class="table-panel">
      <YzhTable :id="page.id('table')" :columns="page.columns" :data="page.rows" />
      <YzhPagination :id="page.id('pagination')" :total="page.pagination.total" />
    </div>
  </div>
</template>
```

### 8.2 卡片列表

```vue
<template>
  <div class="card-list">
    <YzhSearchBar :id="page.id('search')" :fields="searchFields" @search="page.onSearch" />
    <div class="card-grid">
      <YzhCard v-for="item in page.rows" :key="item.id" @click="page.openEditDialog(item)">
        <template #title>{{ item.name }}</template>
        <template #body>{{ item.desc }}</template>
      </YzhCard>
    </div>
    <YzhPagination :id="page.id('pagination')" :total="page.pagination.total" @update:page="page.onPageChange" />
  </div>
</template>
```

---

## 九、项目结构总览

```
certplatform-web/
├── yzh.vue.core/                     # 核心原子组件（接口无关）
│   └── src/
│       ├── components/
│       │   ├── search-bar/
│       │   │   ├── YzhSearchBar.vue
│       │   │   └── index.ts
│       │   ├── toolbar/
│       │   │   ├── YzhToolbar.vue
│       │   │   └── index.ts
│       │   ├── table/
│       │   │   ├── YzhTable.vue
│       │   │   ├── types.ts
│       │   │   └── index.ts
│       │   ├── form/
│       │   │   ├── YzhForm.vue
│       │   │   ├── types.ts
│       │   │   └── index.ts
│       │   ├── pagination/
│       │   │   ├── YzhPagination.vue
│       │   │   └── index.ts
│       │   ├── dialog/
│       │   │   ├── YzhDialog.vue
│       │   │   └── index.ts
│       │   ├── tree/
│       │   │   ├── YzhTree.vue
│       │   │   └── index.ts
│       │   ├── ui/
│       │   │   ├── YzhCard.vue
│       │   │   ├── YzhEmptyState.vue
│       │   │   ├── YzhStatusBadge.vue
│       │   │   └── index.ts
│       │   └── layout/
│       │       ├── YzhPageLayout.vue
│       │       └── index.ts
│       ├── api/
│       │   ├── client.ts             # YzhApiClient
│       │   └── index.ts
│       ├── types/
│       │   ├── index.ts
│       │   └── grid.ts               # GridConfig 类型定义
│       ├── composables/
│       │   ├── useDict.ts            # 字典池
│       │   └── index.ts
│       └── index.ts
│
├── share/                            # 业务共享层
│   └── src/
│       ├── logic/
│       │   ├── CrudPageLogic.ts      # ★ 页面基类
│       │   ├── TreePageLogic.ts      # 树形页面基类
│       │   ├── ReportPageLogic.ts    # 报表页面基类
│       │   └── index.ts
│       ├── api/
│       │   ├── system-user.ts
│       │   ├── iso-standard.ts
│       │   └── ...
│       ├── types/
│       │   ├── cert.ts
│       │   ├── user.ts
│       │   ├── grid.ts
│       │   └── index.ts
│       ├── components/
│       │   ├── CertPageHeader.vue
│       │   ├── CertStatusBar.vue
│       │   └── ...
│       ├── utils/
│       │   ├── api.ts
│       │   └── ...
│       └── index.ts
│
├── admin/                            # 管理员端（Element Plus）
│   └── src/pages/
│       ├── system/
│       │   ├── user/
│       │   │   ├── index.vue         # 视图
│       │   │   └── index.ts          # 可选：定制逻辑
│       │   ├── role/
│       │   ├── dept/
│       │   ├── dict/
│       │   ├── menu/
│       │   └── log/
│       ├── cert/
│       │   ├── iso-standard/
│       │   ├── iso-clause/
│       │   ├── cert-stage/
│       │   ├── cert-body/
│       │   └── enterprise/
│       └── business/
│           ├── prompt-template/
│           ├── skill-manage/
│           ├── directory-manager/
│           └── ...
│
└── auditor/                          # 审核员端（Naive UI）
    └── src/pages/
        └── ...
```

---

## 十、实施路线图

| 阶段 | 内容 | 产出 |
|------|------|------|
| **Phase 1** | 抽取/完善原子组件（YzhDialog、YzhToolbar） | yzh.vue.core 组件库完善 |
| **Phase 2** | 实现 CrudPageLogic 基类 | share/logic/CrudPageLogic.ts |
| **Phase 3** | 补充后端 GetConfig 接口 | 后端 API 完善 |
| **Phase 4** | 重构用户管理页面验证 | 第一个新架构页面 |
| **Phase 5** | 批量迁移简单列表页面 | 覆盖 80% 页面 |
| **Phase 6** | 实现 dictPool 字典池 | 自动字典翻译 |

---

## 十一、与后端约定清单

| 约定项 | 说明 | 状态 |
|--------|------|------|
| 统一响应格式 | `{ success, data, message, code }` | 已有 |
| GridConfig 接口 | `GET /api/{controller}/config` | 需确认/完善 |
| 分页接口 | `POST /api/{controller}/page` | 已有 |
| CRUD 接口 | add / update / delete | 已有 |
| 行操作接口 | `POST /api/{controller}/action/{name}` | 已有 |
| Enable 字段 | DefineColumn 中定义 | 需确认 |

---

*（内容由AI生成，仅供参考）*

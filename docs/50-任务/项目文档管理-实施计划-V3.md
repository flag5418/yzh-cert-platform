# 项目文档管理 — 完整实施计划 V3

> 创建时间：2026-09-14
> 状态：P0+P1 完成，P2-P4 待实施
> 基于历史项目完整分析 + 新架构现状盘点 + YZH 架构分层设计

---

## 一、现状盘点

### 已完成（数据层 + 基础设施 + 应用层 + 控制器层）

| 层级 | 状态 | 文件 |
|------|------|------|
| Entity 定义 | ✅ | `CertPlatform.Shared/Entities/Dir/` 7 个文件（含 SqlSugar 映射） |
| DTO 定义 | ✅ | `UploadManifestDto.cs`, `UploadFileDto.cs` |
| 数据库表 | ✅ | 6 张表全部存在（config/folder/file/template/upload_task/queue*） |
| Queue 引擎 | ✅ | `YZH.Core.DataBase/Services/QueueManager.cs` (711 行) |
| 对象存储接口 | ✅ | `IObjectStorage` + `MinioObjectStorage` + `AliyunOssStorage` |
| CodeGeneratorService | ✅ | `CertPlatform.Admin/Services/StandardDirectory/CodeGeneratorService.cs` |
| StandardDirectoryService | ✅ | `CertPlatform.Admin/Services/StandardDirectory/StandardDirectoryService.cs` (1036 行) |
| DirectoryTemplateService | ✅ | `CertPlatform.Admin/Services/StandardDirectory/DirectoryTemplateService.cs` |
| OfficeConvertService | ✅ | `CertPlatform.Admin/Services/StandardDirectory/OfficeConvertService.cs` |
| OfficeConvertTaskExecutor | ✅ | `CertPlatform.Admin/Services/StandardDirectory/OfficeConvertTaskExecutor.cs` |
| CertQueueNotifier | ✅ | `CertPlatform.Admin/Services/StandardDirectory/CertQueueNotifier.cs` |
| UploadQueueCancelHandler | ✅ | `CertPlatform.Admin/Services/StandardDirectory/UploadQueueCancelHandler.cs` |
| StandardDirectoryController | ✅ | `CertPlatform.Admin/Controllers/Workflow/StandardDirectoryController.cs` (22 APIs) |
| DirectoryTemplateController | ✅ | `CertPlatform.Admin/Controllers/Foundation/DirectoryTemplateController.cs` (8 APIs) |
| QueueMonitorController | ✅ | `CertPlatform.Admin/Controllers/System/QueueMonitorController.cs` (7 APIs) |
| DI 注册 | ✅ | `Program.cs` 中注册所有服务 + ApplicationPart |

### 待实施（前端层）

| 模块 | 状态 |
|------|------|
| TS 接口定义 | ❌ 未实施 |
| API 客户端 | ❌ 未实施 |
| 组合式函数 | ❌ 未实施 |
| 前端组件 | ❌ 未实施 |
| 前端页面 | ❌ 未实施 |

---

## 二、架构分层设计

### 2.1 分层原则

```
YZH.Core（架构层）        → 跨项目可复用的抽象 + 引擎
CertPlatform.Shared（共享层）→ 多角色共用的基础设施实现
CertPlatform.Admin/Auditor/Enterprise（业务层）→ 认证平台特定业务逻辑
```

### 2.2 YZH.Core 架构层（已就绪，无需改动）

| 文件 | 职责 | 状态 |
|------|------|------|
| `IObjectStorage.cs` | 对象存储抽象（7 个方法） | ✅ 已定义 |
| `IYzhTaskExecutor.cs` | 任务执行器抽象 | ✅ 已定义 |
| `IYzhQueueNotifier.cs` | 队列终态通知抽象 | ✅ 已定义 |
| `IYzhQueueCancelHandler.cs` | 队列取消清理钩子抽象 | ✅ 已定义 |
| `QueueManager.cs` | 通用队列引擎（资源锁+并发+重试+通知） | ✅ 已实现 |
| `QueueHostedService.cs` | 队列后台 Worker | ✅ 已实现 |
| `IExcelService.cs` | Excel 导入导出抽象 | ✅ 已定义 |
| `IUserContext.cs` | 用户上下文抽象 | ✅ 已定义 |

### 2.3 CertPlatform.Shared 共享层（本次需补充）

| 文件 | 职责 | 说明 |
|------|------|------|
| `MinioObjectStorage.cs` | IObjectStorage 的 MinIO 实现 | ✅ 已存在 |
| `AliyunOssStorage.cs` | IObjectStorage 的阿里云实现 | ✅ 已存在（骨架） |
| `FileConvertService.cs` | **新建** 通用文件转换编排（下载→转换→上传） | 抽象为 `IFileConvertService`，放入共享层 |

### 2.4 CertPlatform.Admin 业务层（本次需新建）

```
CertPlatform.Admin/
├── Controllers/
│   ├── Workflow/
│   │   └── StandardDirectoryController.cs    ← 目录管理 22 个 API
│   ├── Foundation/
│   │   └── DirectoryTemplateController.cs    ← 模板管理 11 个 API
│   └── System/
│       └── QueueMonitorController.cs         ← 队列监控 7 个 API
├── Services/
│   └── StandardDirectory/
│       ├── StandardDirectoryService.cs       ← 核心业务（树/文件夹/文件 CRUD + 上传 4 步）
│       ├── CodeGeneratorService.cs           ← 编码/路径生成（SDC-/FD-/FL- + V3 路径）
│       ├── DirectoryTemplateService.cs       ← 模板管理
│       ├── OfficeConvertService.cs           ← 文件转换编排（调用 IFileConvertService）
│       ├── OfficeConvertTaskExecutor.cs      ← IYzhTaskExecutor 实现
│       ├── CertQueueNotifier.cs              ← IYzhQueueNotifier 实现
│       └── UploadQueueCancelHandler.cs       ← IYzhQueueCancelHandler 实现
└── JsonConfig/
    ├── StandardDirectory.json                ← EntityConfig
    └── DirectoryTemplate.json                ← EntityConfig
```

### 2.5 依赖关系

```
CertPlatform.Admin (业务层)
  ├── 引用 YZH.Core (接口 + 引擎)
  ├── 引用 CertPlatform.Shared (实体 + 共享服务)
  └── 注册 IYzhTaskExecutor / IYzhQueueNotifier / IYzhQueueCancelHandler

CertPlatform.Shared (共享层)
  └── 引用 YZH.Core (接口)

YZH.Core (架构层)
  └── 无外部依赖（纯抽象 + 引擎）
```

---

## 三、MinIO 存储结构分析

### 3.1 Bucket 与路径规则

```
Bucket: cert-platform（appsettings.json → MinIO:BucketName）

V3 路径格式（当前生产）：
/standard-directory/{OrgCode}/{StandardCode}/{PhaseCode}/{FolderPath}/{FileName}

示例：
/standard-directory/CB001/ISO134852016/STAGE01/质量手册/程序文件.docx
/standard-directory/CB001/ISO134852016/STAGE01/质量手册/.converted/程序文件.docx
```

### 3.2 路径生成规则

| 组件 | 清洗规则 | 示例 |
|------|---------|------|
| OrgCode | 去掉 `:-空格/\` | `CB-001` → `CB001` |
| StandardCode | 去掉 `:-空格/\` | `ISO13485:2016` → `ISO134852016` |
| PhaseCode | 去掉 `:-空格/\` | `STAGE-01` → `STAGE01` |
| FolderPath | 仅替换 `\|` → `-`，去掉首尾 `/` | `质量手册\|子目录` → `质量手册-子目录` |
| FileName | 去掉路径分隔符 `/\\` | `my/file.pdf` → `myfile.pdf` |

### 3.3 转换文件路径

```
源文件: /standard-directory/CB001/ISO134852016/STAGE01/质量手册/程序文件.doc
转换后: /standard-directory/CB001/ISO134852016/STAGE01/质量手册/.converted/程序文件.docx
                                                          ^^^^^^^^^
                                                          隐藏子目录
```

### 3.4 文件夹在 MinIO 中的表现

MinIO 中**文件夹是虚拟的**——不存在显式的文件夹对象。文件夹由对象键中的 `/` 分隔符隐式创建。

例如上传 `standard-directory/CB001/ISO134852016/STAGE01/doc.pdf` 时，MinIO 自动创建 `CB001/`、`ISO134852016/`、`STAGE01/` 等"目录"。

**文件夹元数据存储在数据库** `cert_standard_directory_folder` 表中：
- `ParentCode`: 父文件夹编码
- `Depth`: 层级深度
- `FullPath`: 人类可读路径（如 `质量手册/程序文件`）

### 3.5 上传流程（4 步）

```
┌─────────────────────────────────────────────────────────────┐
│ 1. UploadInit（预初始化）                                    │
│    POST /api/standard-directory/upload-init                  │
│    输入: directoryCode, fileList[{folderName,fileName,...}]  │
│    处理: 生成 StoragePath, 创建 DB 记录(IsValid=false)       │
│    输出: taskId, fileList[+fileCode, +storagePath]          │
├─────────────────────────────────────────────────────────────┤
│ 2. UploadFileV2（逐文件上传）                                │
│    POST /api/standard-directory/upload-file-v2               │
│    输入: file(multipart), fileCode, taskId                  │
│    处理: 从 DB 读 StoragePath → 上传 MinIO                   │
│    输出: {ok}                                               │
├─────────────────────────────────────────────────────────────┤
│ 3. UploadConfirm（确认激活）                                  │
│    POST /api/standard-directory/upload-confirm?taskId=xxx   │
│    处理: IsValid=true, 扫描 .doc/.xls → 创建转换队列         │
│    输出: {ok, convertTaskId?}                               │
├─────────────────────────────────────────────────────────────┤
│ 4. UploadCancel（回滚）                                      │
│    POST /api/standard-directory/upload-cancel?taskId=xxx    │
│    处理: 删除 DB 记录 + MinIO 对象, 恢复被替换文件            │
│    输出: {ok}                                               │
└─────────────────────────────────────────────────────────────┘
```

### 3.6 Content-Type 映射

| 文件类型 | Content-Type |
|---------|-------------|
| 默认 | `application/octet-stream` |
| .docx | `application/vnd.openxmlformats-officedocument.wordprocessingml.document` |
| .xlsx | `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` |
| .pdf | `application/pdf` |
| .jpg/.jpeg | `image/jpeg` |
| .png | `image/png` |

---

## 四、完整实施计划

### P0：后端 Service 层（7 个文件）

| # | 文件 | 方法数 | 层级 | 说明 |
|:-:|------|:------:|:----:|------|
| 1 | `Services/StandardDirectory/StandardDirectoryService.cs` | 27 | Admin | 核心：树/文件夹/文件 CRUD + 上传 4 步 |
| 2 | `Services/StandardDirectory/DirectoryTemplateService.cs` | 11 | Admin | 目录模板管理 |
| 3 | `Services/StandardDirectory/OfficeConvertService.cs` | 8 | Admin | 文件转换编排（doc→docx, xls→xlsx） |
| 4 | `Services/StandardDirectory/OfficeConvertTaskExecutor.cs` | 3 | Admin | IYzhTaskExecutor 实现 |
| 5 | `Services/StandardDirectory/CertQueueNotifier.cs` | 1 | Admin | IYzhQueueNotifier 实现 |
| 6 | `Services/StandardDirectory/UploadQueueCancelHandler.cs` | 1 | Admin | IYzhQueueCancelHandler 实现 |
| 7 | `Services/StandardDirectory/CodeGeneratorService.cs` | 6 | Admin | 编码/路径生成 |

### P1：后端 Controller 层（3 个文件）

| # | 文件 | API 数 | 说明 |
|:-:|------|:------:|------|
| 1 | `Controllers/Workflow/StandardDirectoryController.cs` | 22 | 目录管理全部 API |
| 2 | `Controllers/Foundation/DirectoryTemplateController.cs` | 11 | 模板管理 API |
| 3 | `Controllers/System/QueueMonitorController.cs` | 7 | 队列监控 API |

### P2：前端 Composable + API（4 个文件）

| # | 文件 | 说明 |
|:-:|------|------|
| 1 | `cert-share/src/composables/useDirectoryApi.ts` | 重写：对接 22 个后端 API |
| 2 | `cert-share/src/composables/useFileTree.ts` | 重写：树形数据加载 + 节点操作 |
| 3 | `cert-share/src/api/standard-directory.ts` | 新建：API 定义 |
| 4 | `cert-share/src/types/standard-directory.ts` | 新建：TS 接口 |

### P3：前端组件（2 个文件）

| # | 文件 | 说明 |
|:-:|------|------|
| 1 | `cert-share/src/components/CertDirectoryTree.vue` | 重写：3 级树（机构→标准→阶段） |
| 2 | `cert-share/src/components/ConvertProgressPanel.vue` | 新建：转换进度面板 |

### P4：前端页面（3 个文件）

| # | 文件 | 说明 |
|:-:|------|------|
| 1 | `cert-admin/src/pages/workflow/directory/index.vue` | 重写：主页面（左树+右内容） |
| 2 | `cert-admin/src/pages/system/queue-monitor/index.vue` | 新建：队列监控页 |
| 3 | `cert-admin/src/router/index.ts` | 更新：添加路由 |

---

## 五、后端 API 清单（33 个接口）

### StandardDirectoryController（22 个）

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `organization-tree` | 组织树（org→std→phase） |
| GET | `configs` | 配置列表 |
| GET | `configs/{directoryCode}` | 单个配置 |
| POST | `configs/create` | 创建配置 |
| POST | `configs/{directoryCode}` | 更新配置 |
| POST | `configs/{directoryCode}/delete` | 删除配置 |
| GET | `configs/{directoryCode}/folders` | 文件夹树 |
| POST | `configs/{directoryCode}/folders/create` | 创建文件夹 |
| POST | `folders/{folderCode}` | 更新文件夹 |
| POST | `folders/{folderCode}/delete` | 删除文件夹 |
| GET | `folders/{folderCode}/files` | 文件列表 |
| POST | `files/{fileCode}` | 更新文件 |
| POST | `files/{fileCode}/delete` | 删除文件 |
| POST | `upload-init` | 上传预初始化 |
| POST | `upload-file-v2` | 上传文件 |
| POST | `upload-confirm` | 确认上传 |
| POST | `upload-cancel` | 回滚上传 |
| GET | `upload-status` | 上传状态 |
| GET | `active-queue` | 活跃队列 |
| POST | `convert/progress` | 转换进度 |
| POST | `convert/cancel` | 取消转换 |
| GET | `download` | 下载文件 |

### DirectoryTemplateController（11 个）

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `tree` | 目录树 |
| POST | `addFolder` | 新增文件夹 |
| POST | `updateFolder` | 修改文件夹 |
| POST | `deleteFolder` | 删除文件夹 |
| GET | `fileRequirements` | 文件要求列表 |
| POST | `saveFileRequirement` | 保存文件要求 |
| POST | `deleteFileRequirement` | 删除文件要求 |
| POST | `uploadTemplateFile` | 上传模板文件 |
| GET | `downloadTemplateFile` | 下载模板文件 |
| POST | `deleteTemplateFile` | 删除模板文件 |
| POST | `renameTemplateFile` | 重命名模板文件 |

### QueueMonitorController（7 个）

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | `list` | 队列列表 |
| POST | `detail` | 队列详情 |
| POST | `cancel` | 取消队列 |
| POST | `retry` | 重跑队列 |
| POST | `task/retry` | 单任务重试 |
| POST | `status` | 队列统计 |
| POST | `resource/locked` | 资源锁查询 |

---

## 六、前端页面结构

### 目录管理主页面（左树 + 右内容）

```
┌──────────────────────────────────────────────────────────────────┐
│ 上传队列横幅（UploadQueueBanner）                                  │
├──────────────┬───────────────────────────────────────────────────┤
│ 左侧树面板    │ 右侧内容区                                        │
│              │                                                   │
│ 🔍 搜索      │ 面包屑: SDC-ISO134852016|STAGE01 > 质量手册       │
│              │                                                   │
│ 🏢 认证机构A  │ ┌─────────────────────────────────────────────┐  │
│  📋 ISO13485 │ │ [上传队列] [新建文件夹] [上传] | [刷新] [删除] │  │
│   📌 初审 ◀──│ ├─────────────────────────────────────────────┤  │
│   📌 复审    │ │ ☐ 📁 质量手册     --    —    2024-01-01  ...│  │
│   📌 监督    │ │ ☐ 📁 程序文件     --    —    2024-01-01  ...│  │
│  📋 ISO14001 │ │ ☐ 📄 营业执照.pdf 1.2MB ✓   2024-01-01  ...│  │
│   📌 初审    │ │ ☐ 📄 质量手册.docx 2.5MB ✓   2024-01-01  ...│  │
│              │ └─────────────────────────────────────────────┘  │
│ 🏢 认证机构B  │ 共 4 项 | 文件夹 2 个，文件 2 个    总大小 3.7MB  │
│  ...         │                                                   │
└──────────────┴───────────────────────────────────────────────────┘
```

### 左侧树节点点击行为

| 节点类型 | 点击行为 |
|---------|---------|
| 机构 (Level 0) | 展开/折叠子节点 |
| 标准 (Level 1) | 展开/折叠子节点 |
| 阶段 (Level 2) | **选中** → 右侧加载该阶段的文件夹/文件 |

### 右侧文件列表操作

| 操作 | 文件夹 | 文件 | 说明 |
|------|:------:|:----:|------|
| 新建文件夹 | ✅ | — | 在当前目录下创建 |
| 上传 | — | ✅ | 上传到当前目录 |
| 重命名 | ✅ | ✅ | 弹窗输入新名称 |
| 替换 | — | ✅ | 重新上传同名文件 |
| 下载 | — | ✅ | 流式下载 |
| 删除 | ✅ | ✅ | 确认后删除 |

---

## 七、实施顺序建议

```
阶段 1: 后端基础（P0）
├── CodeGeneratorService（编码/路径生成）
├── StandardDirectoryService（核心 CRUD + 上传 4 步）
├── DirectoryTemplateService（模板管理）
├── OfficeConvertService + TaskExecutor + CancelHandler + Notifier
└── 编译验证

阶段 2: 后端 API（P1）
├── StandardDirectoryController（22 个 API）
├── DirectoryTemplateController（11 个 API）
├── QueueMonitorController（7 个 API）
└── 编译验证 + 后端重启

阶段 3: 前端基础（P2）
├── TS 接口定义
├── API 客户端
├── useDirectoryApi composable
└── useFileTree composable

阶段 4: 前端页面（P3 + P4）
├── CertDirectoryTree 组件
├── ConvertProgressPanel 组件
├── 目录管理主页面
├── 队列监控页面
└── 路由配置

阶段 5: 集成验证
├── 后端 33 个 API 全部可用
├── 前端页面功能完整
├── 上传 → MinIO 存储验证
├── 下载 → MinIO 流式返回验证
└── 文件转换 → 队列执行验证
```

---

## 八、风险点

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| MinIO 服务未启动 | 上传/下载不可用 | 确认 Docker MinIO 容器运行 |
| LibreOffice 未安装 | doc→docx 转换失败 | OfficeConvertService 检查可用性后降级 |
| 文件较大时内存溢出 | 上传/下载 OOM | 使用 Stream 流式处理 |
| SignalR Hub 未实现 | 上传进度无实时推送 | 先用轮询，SignalR 后续补充 |

---

## 九、文件清单汇总

| # | 文件路径 | 操作 | 阶段 | 层级 |
|:-:|----------|------|:----:|:----:|
| 1 | `CertPlatform.Admin/Services/StandardDirectory/CodeGeneratorService.cs` | 新建 | P0 | 业务 |
| 2 | `CertPlatform.Admin/Services/StandardDirectory/StandardDirectoryService.cs` | 新建 | P0 | 业务 |
| 3 | `CertPlatform.Admin/Services/StandardDirectory/DirectoryTemplateService.cs` | 新建 | P0 | 业务 |
| 4 | `CertPlatform.Admin/Services/StandardDirectory/OfficeConvertService.cs` | 新建 | P0 | 业务 |
| 5 | `CertPlatform.Admin/Services/StandardDirectory/OfficeConvertTaskExecutor.cs` | 新建 | P0 | 业务 |
| 6 | `CertPlatform.Admin/Services/StandardDirectory/CertQueueNotifier.cs` | 新建 | P0 | 业务 |
| 7 | `CertPlatform.Admin/Services/StandardDirectory/UploadQueueCancelHandler.cs` | 新建 | P0 | 业务 |
| 8 | `CertPlatform.Admin/Controllers/Workflow/StandardDirectoryController.cs` | 新建 | P1 | 业务 |
| 9 | `CertPlatform.Admin/Controllers/Foundation/DirectoryTemplateController.cs` | 新建 | P1 | 业务 |
| 10 | `CertPlatform.Admin/Controllers/System/QueueMonitorController.cs` | 新建 | P1 | 业务 |
| 11 | `cert-share/src/types/standard-directory.ts` | 新建 | P2 | 前端 |
| 12 | `cert-share/src/api/standard-directory.ts` | 新建 | P2 | 前端 |
| 13 | `cert-share/src/composables/useDirectoryApi.ts` | 重写 | P2 | 前端 |
| 14 | `cert-share/src/composables/useFileTree.ts` | 重写 | P2 | 前端 |
| 15 | `cert-share/src/components/CertDirectoryTree.vue` | 重写 | P3 | 前端 |
| 16 | `cert-share/src/components/ConvertProgressPanel.vue` | 新建 | P3 | 前端 |
| 17 | `cert-admin/src/pages/workflow/directory/index.vue` | 重写 | P4 | 前端 |
| 18 | `cert-admin/src/pages/system/queue-monitor/index.vue` | 新建 | P4 | 前端 |
| 19 | `cert-admin/src/router/index.ts` | 更新 | P4 | 前端 |

---

## 十、预估工时

| 阶段 | 文件数 | 预估 |
|------|:------:|------|
| P0 后端 Service | 7 | 大（核心逻辑最复杂） |
| P1 后端 Controller | 3 | 中（API 薄层） |
| P2 前端基础 | 4 | 中 |
| P3 前端组件 | 2 | 中 |
| P4 前端页面 | 3 | 大（主页面逻辑复杂） |
| **总计** | **19** | — |

---

## 附录：AI 功能说明

AI 文档分析功能（`POST /api/DocExtractionRule/analyze`）属于**标准文档内容提取规则**页面，不属于本次目录管理迁移范围。该功能需要：

- `IFileExtractor`（文档内容提取）
- `WorkflowEngine`（LLM 调用）
- `cert_sys_config.ai_model_name`（模型配置）
- `wf_prompt_template`（提示词模板表）

将在「标准文档内容提取规则」模块中单独实施。

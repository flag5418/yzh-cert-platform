# 专家端菜单与系统一览（驾驶舱）设计 V1

> 状态：**菜单部分已实施并端到端验收**（2026-09-23）；驾驶舱部分待实施 | 编制日期：2026-09-23
> 目标代码：`src/certplatform-web/cert/cert-auditor/`（前端）+ `src/certplatform-api/CertPlatform.Auditor/`（后端）
> 前置文档：《体系认证专家系统建设计划-V4》（产品定义、分层纪律、阶段划分）
> 本文范围：**只做菜单信息架构 + 驾驶舱（系统一览）设计**；不含各功能页面的实现细节

---

## 第零篇 结论摘要

| 项 | 结论 |
|---|---|
| 一级菜单 | **1 个分组**：「专家系统」（实施时收敛，见 §0.2） |
| 二级菜单 | **6 个**：系统一览 / 企业管理 / 任务中心 / 资料库 / 组织与成员 / 系统设置（菜单记录合计 **7 条**） |
| 落地页 | `/overview`「系统一览」，登录后默认跳转 |
| 驾驶舱形态 | 三段式滚动 · **五区**：身份条 → 待办 + 能力就绪度 → 企业进度矩阵 → 最近任务 + 快捷入口 |
| 驾驶舱设计要点 | **能力优先**：MVP 阶段只有"能力就绪度"能出真数据，其余区走**降级空态**，不报错 |
| 菜单隔离机制 | 后端**只认 `Sys_RoleMenu`**；`Tag` 后端不过滤，**但前端按 `Tag` 分流**（实施时新增，见 §0.2） |
| 分期策略 | 菜单一次性全量种下，用 `Sys_Menu.IsValid` 控制开放；未开放的不出现在侧边栏 |
| ⛔ 前置阻塞 | **机构关联键不一致**（注册写 `CbCode`，能力表存 `Code`）→ 驾驶舱能力查询会**查空且不报错**（见 §5.1）｜**截至 2026-09-23 仍未修复** |

### 0.1 实施状态（2026-09-23 端到端验收通过）

| 项 | 状态 | 证据 |
|---|---|---|
| 菜单入库 | ✅ | `Sys_Menu` 33 条（`admin` 26 + `auditor` 7） |
| 角色授权 | ✅ | `Sys_RoleMenu` 给 `ROLE_AUDIT_CLIENT_ADMIN` 授权 7 条 |
| 专家端动态菜单 | ✅ | `wzm`（`ROLE_AUDIT_CLIENT_ADMIN`）登录 → `GET /api/System/MenuManagement/tree` 返回 7 条 → 侧边栏渲染 1 分组 + 6 子项，图标全出 |
| 管理端隔离 | ✅ | 超管登录管理端：后端返回 33 条，侧边栏只渲染 26 条，**专家菜单零泄漏** |
| 路由可达 | ✅ | 6 个子菜单逐个点击，路由正确、无 404 |
| 占位页兜底 | ✅ | `cert-share/src/components/CertPagePlaceholder.vue`（共享层） |

### 0.2 实施时对设计的偏离（★ 必读，防止按本文旧版返工）

| # | 本文原设计 | 实际实施 | 原因 |
|---|---|---|---|
| 1 | **6 个一级菜单**（每个一级即落地页，无分组） | **1 个分组「专家系统」+ 6 个子菜单** | 侧边栏渲染层**只支持 2 层**（`el-sub-menu` + `el-menu-item`）。6 个平级一级菜单会与管理端 2 个分组（系统管理/业务管理）视觉不一致；且用户要求「**按一个独立的专家系统大的分类菜单**」建立子菜单，便于**角色-菜单分配**时整组勾选 |
| 2 | `Tag` 只作分类标签，**不构成隔离** | `Tag` **成为前端分流依据**：`filterMenuTreeByTag(menus, 'admin'\|'auditor')` | 后端 `MenuPermissionService` 仍只认 `Sys_RoleMenu`，**超管会拿到全量 33 条**。若不按 `Tag` 分流，超管登录专家端会看到 26 条管理菜单、登录管理端会看到专家菜单（点击即 404）。**后端不变，前端补一层** |
| 3 | 二级菜单 12–13 个（含 `新建企业`/`NC 检查`/`报告生成`/`我的关注`/`资料检索`/`目录模板`/`成员管理`/`角色分组`/`个人信息`/`账户与用量`/`消息设置`/`操作日志`） | **只种 6 个二级**（每个一级菜单本身即页面） | 更深层级改由**页面内 Tab / 子路由**承载。首版二级菜单若全是占位页，侧边栏会显得空洞且点击率低 |
| 4 | 菜单 `Code` 从 `MENU_AUD_01` 起 | 根为 `MENU_AUD_00`（专家系统），子为 `MENU_AUD_01`~`06` | 与新增的顶层分组对应 |
| 5 | 占位页放 `cert-auditor/pages/_placeholder/index.vue`（专家端专属） | 放 `cert-share/src/components/CertPagePlaceholder.vue`（**共享层**） | 管理端同样需要占位页；且符合 V4「基础展示组件进 `cert-share`」的分层纪律 |
| 6 | 菜单图标由设计指定（`Odometer`/`OfficeBuilding`/…） | 沿用设计指定的 Element Plus 图标名，**并经共享层 `formatMenuIcon()` 归一化** | 历史菜单可能存 Element UI / iView 图标名（如 `el-icon-xxx`），需映射为 Element Plus 组件名，否则图标静默不渲染 |

> **实现位置（三层同构）**：
> - 菜单类型 + 归一化 + 建树：`cert-share/src/api/system/menu.ts`
> - 图标映射 + Tag 分流：`cert-share/src/utils/menu.ts`（`formatMenuIcon` / `filterMenuTreeByTag`）
> - 菜单单例 store：`cert-share/src/composables/useMenuTree.ts`
> - 管理端：`cert-admin/src/store/menu.ts`（薄适配）+ `AdminLayout.vue`
> - 专家端：`cert-auditor/src/layouts/AuditorLayout.vue` + `router/index.ts`

---

## 第一篇 输入盘点

### 1.1 文档位置纠正

需求提到"阅读 `docs/30-项目规则` 关于专家系统相关的文档"。**实测该目录下没有任何"专家"相关文档**（全目录 grep `专家` 命中 0 个文件）。

`docs/30-项目规则` 实际内容为：`README.md`、`Skill清单-V1.md`、`前后端代码结构统一规则-V1.md`、`知识库/`（边界约束、踩坑记录、速查手册、术语表）。

**专家系统的文档实际分布在两处**：

| 位置 | 内容 |
|---|---|
| `输出产物/` | 《体系认证专家系统建设计划》**V1–V4**（V4 为定稿待审批版，产品定义权威源） |
| `docs/20-体系认证/03-详细设计/02-审核员端/` | 功能设计、界面设计、多标准结合审核（专家端前身） |

→ **建议**：把 V4 计划从 `输出产物/` 移入 `docs/`（`输出产物/` 是过程产物目录，而 V4 已是权威设计），并在 `docs/20-体系认证/README.md` 登记。

### 1.2 本轮实际精读的文档

| # | 文档 | 用途 |
|---|---|---|
| 1 | `输出产物/体系认证专家系统建设计划-V4.md`（617 行） | 产品定义、`cert-share` 分层纪律、阶段划分、D1–D9 决策 |
| 2 | `docs/20-体系认证/03-详细设计/02-审核员端/审核员端功能设计-V1.md`（555 行） | 8 功能模块、业务规则、任务状态机、接口草案 |
| 3 | `docs/20-体系认证/03-详细设计/02-审核员端/审核员端界面设计-页面结构-V1.md`（355 行） | 页面清单、交互骨架、预留入口清单 |
| 4 | `docs/10-YZH架构/14-框架核心功能设计-V1.md` §4–§5 | 菜单/权限机制权威描述 |
| 5 | `docs/10-YZH架构/13-框架数据库设计-V1.md` §6.1 | `Sys_Menu` 表设计 |
| 6 | 源码：`MenuPermissionService.cs` / `MenuController.cs` / `MenuManagementController.cs` / `cert-admin` 菜单链 | 机制事实核对 |

### 1.3 三处口径冲突（必须先收敛）

同一套导航，三份文档给了三种结构：

| 来源 | 一级导航 | 问题 |
|---|---|---|
| 功能设计 V1 §1.2 | **8 模块**：当前任务 / 企业管理 / 工作平台 / 协同系统 / 企业资料 / 费用管理 / 系统日志 / 系统管理 | 任务导向，与该文档 §4.2 自己确立的"**关注核心是企业，不是任务**"直接矛盾 |
| 界面设计 V1 §2 | **5 项**：工作台 / 企业管理 / 资源管理 / 消息中心 / 个人中心 | 企业导向，但**丢了"任务"与"日志"**；文档自称"导航重组、功能不削减"，实际削减了 |
| V4 计划 §4.8 | **技术目录**：workspace / enterprises / tasks / organization / system-profile | 是代码目录树，不是菜单；且只有 5 项，与上两者都对不上 |

**收敛原则（本文采用）**：

1. **以产品逻辑定菜单，不以功能模块定菜单** —— 模块是"系统有什么"，菜单是"专家要干什么"
2. **一级菜单不超过 6 个** —— 个人 SaaS 工具，超过 6 个就开始找不到东西
3. **一级菜单即落地页**，二级菜单才是可独立寻址的功能页（避免"点了一级还要再点一次"）
4. **任务不是一级实体** —— 任务是企业 × 标准 × 阶段的派生物，收进"任务中心"作为**视图**，不与"企业管理"平级争注意力

---

## 第二篇 菜单机制事实（源码级核实，2026-09-23）

> 这一篇是后面所有设计的依据。**其中"`Tag` 不参与过滤"是本轮最重要的发现** —— 它推翻了"给菜单设 `Tag='auditor'` 就能隔离"的常见误判。

### 2.1 过滤链路：只认 `Sys_RoleMenu`

`YZH.Core.Api/Services/MenuPermissionService.cs` 的 `GetVisibleMenusAsync()` 实际逻辑：

```
① 取全部菜单 → 排除 Enable == 0
② 若当前用户是 ROLE_SUPER_ADMIN → 直接返回全部（不看授权）
③ 否则取用户全部 RoleCode（来自 JWT claims）
④ 查 Sys_RoleMenu WHERE RoleCode IN (...) → 得到已授权 MenuCode 集合
⑤ 若授权集合为空 → 返回空列表（一个菜单都不给）
⑥ 祖先补全（避免父菜单未授权导致子菜单断链）
⑦ 按 MenuCode 过滤 + 按 OrderNo 排序
```

**关键结论**：

| 事实 | 影响 |
|---|---|
| **`Tag` 字段在「后端」完全不参与过滤** | 设 `Tag='auditor'` **不会**让 admin 菜单对专家不可见，也**不会**让 auditor 菜单对管理员不可见 |
| `Sys_Menu.IsValid = 0` → 不出现 | 这是**唯一**的后端"开关"手段（不需要删数据；原 `Enable` 列已 DROP） |
| 超管绕过一切授权 | 超管从后端拿到**全量 33 条**（`admin` 26 + `auditor` 7）—— 测试隔离必须用真实注册账号，别用超管 |
| `Sys_RoleMenu` 为 0 → 菜单全空 | ~~当前 `ROLE_AUDIT_CLIENT_ADMIN` 正是 **0 条**~~ → **已修复**：现授权 **7 条**，注册用户登录后侧边栏正常 |

> `Tag` 的用途：① 后台菜单管理页的分类标签；② Vol 兼容端点 `getTreeMenu` 把它当 `tableName` 返回；
> ③ ★ **前端分流依据**（2026-09-23 新增，见 §0.2 偏离 #2）—— `filterMenuTreeByTag()` 按当前端（`admin`/`auditor`）过滤。
>
> ⚠️ **后端不隔离 + 前端补一层 = 必须成对理解**：`Tag` 在后端**仍不是**隔离机制，隔离靠 `Sys_RoleMenu`；
> 但**前端**必须按 `Tag` 分流，否则超管（绕过授权的唯一角色）登录任一端口都会看到另一端的菜单，点击即 404。

### 2.2 端点与字段

| 端点 | 认证 | 返回 | 用途 |
|---|---|---|---|
| `GET /api/System/MenuManagement/tree` | `[Authorize]` | `ApiResponse<List<Sys_Menu>>` —— **扁平**列表，PascalCase | ✅ **运行时菜单树，前端用这个** |
| `GET /api/System/Menu/tree` | `[AllowAnonymous]` | `{ menu: [...] }`（`MenuNode` DTO，字段名是 `Name` 不是 `MenuName`） | ⛔ Vol 兼容，**不要用** |
| `GET /api/System/MenuManagement/tree/all` | `[Authorize]` | 全量，不过滤 | 菜单管理页维护用 |

字段语义（`docs/10-YZH架构/14-框架核心功能设计-V1.md` §5.1）：

| 字段 | 用途 |
|---|---|
| `Code` / `ParentCode` | 树结构（`ParentCode = '0'` 为根） |
| `Url` | **前端路由 path**（`/overview`） |
| `Icon` | Element Plus 图标名（字符串） |
| `OrderNo` | 排序（现有约定 100 / 200 / 300…） |
| `Enable` | 1=显示，0=隐藏 |
| `Tag` | 分组标记（**不参与过滤**） |
| `Auth` | **按钮级权限标识** —— 当前全库未使用，属预留 |

前端消费链（`cert-admin` 已跑通的模式，专家端照搬）：
`store/menu.ts` → `loadMenus()` → `api/system/menu.ts#getMenuTree()` → `MenuManagement/tree` → 前端 `buildTree()` → `AdminLayout.vue` 用 `el-sub-menu` / `el-menu-item` 渲染。

### 2.3 现状数据（2026-09-23 复测）

| 表 / 项 | 实测值 | 说明 |
|---|---|---|
| `Sys_Menu` | **33 条** | `Tag=admin` **26** + `Tag=auditor` **7**；`Tag IS NULL` **0 条** |
| `Sys_Menu.Tag` | **已全部填充** | `admin\|auditor` 分流机制**已启用**（原为全 NULL） |
| `Sys_RoleMenu` | **30 条 / 7 个角色** | 含新增的 `ROLE_AUDIT_CLIENT_ADMIN`(7) |
| `ROLE_AUDIT_CLIENT_ADMIN` 的菜单 | **7 条** | ✅ 已修复（原为 0 条）→ 注册用户登录后侧边栏正常 |
| `sys_api` | **399 条** | 含 `api/AuditorAuth/GetOrgList`(Id 440) / `Register`(Id 441) |
| `sys_role_api` | **90 条**（其中该角色 87 条，全 `Foundation/*` 只读） | |
| `Sys_RoleAuth` | **330 条** | 旧接口权限载体，**仍在使用**，不可删 |
| 专家端接口是否已授权 | **否**（复测确认） | `AuditorAuth/*` 两个端点**仍未关联**到 `ROLE_AUDIT_CLIENT_ADMIN`（当前靠 `[AllowAnonymous]` 放行）；`sys_role_api` 中 `Path LIKE '%AuditorAuth%'` → **0 行** |

> ⚠️ **`sys_api` 无 `ControllerName`/`ActionName` 列**：只有 `Code`(ApiCode) / `Method` / `Path` / `GroupPath` / `Name`。
> 按控制器名检索要查 `Path LIKE '%<控制器名>%'`，查 `ControllerName` 会报 `ERROR 1054`。

> ⚠️ **文档已过时**：`docs/10-YZH架构/14-框架核心功能设计-V1.md` §4.5 写"`sys_api` / `sys_role_api` **均未创建**" —— 实际两表已建且已有 399 / 90 条数据。该节需修订，否则会误导下一个开发者去"新建"已存在的表。

### 2.4 由事实导出的五条硬约束

1. **专家端菜单必须写 `Sys_RoleMenu`**（`RoleCode='ROLE_AUDIT_CLIENT_ADMIN'` × 18 条 MenuCode），只写 `Sys_Menu` 不生效
2. **绝不给该角色授权任何管理端菜单** —— 现有 26 条全是后台配置页，专家是**只读消费方**，不参与配置
3. **`Enable` 是唯一开关**，用于分期开放（§3.5），不要用删数据实现"暂不开放"
4. **`Tag` 照设但不指望它**：设成 `auditor` 便于后台辨识，同时在文档/注释里写明"不构成隔离"
5. **新增受保护接口后必须跑 ApiSync 并关联 `sys_role_api`**，否则非超管调用 **403**（`ApiCode = SHA256("{METHOD}|{控制器名}|{动作名小写}")`）

---

## 第三篇 菜单信息架构设计

### 3.1 设计原则

| # | 原则 | 说明 |
|---|---|---|
| 1 | **按专家的动作路径组织，不按系统模块组织** | 专家的路径是「看全局 → 管企业 → 跑任务 → 查资料 → 管人 → 管自己」 |
| 2 | **一级菜单 = 6 个封顶** | 超过 6 个在侧边栏就开始需要"找"，个人 SaaS 工具的容忍度更低 |
| 3 | **一级即落地页** | 点"企业管理"直接进企业列表，不再展开一层"企业列表"子菜单 |
| 4 | **任务降级为视图** | 任务是企业×标准×阶段的派生物。保留"任务中心"承载**跨企业的任务视角**（NC 任务 / 报告任务 / 我的关注），但排在"企业管理"之后 |
| 5 | **能力边界显式化** | 专家最容易困惑的是"我能做什么"。菜单里不体现，改由驾驶舱的"能力就绪度"承担（§4.2 ②） |
| 6 | **P2/P3 功能只占位不实现** | 账户与用量、消息设置、目录模板等先种菜单 + 占位页，避免首版侧边栏空洞 |

### 3.2 推荐方案

> ⚠️ **本节为原设计稿（6 个一级平级菜单）。实际实施收敛为「1 个分组 + 6 个子菜单」，见下方 §3.2.1。**
> 保留原稿是为了记录取舍理由（§3.3 的 A/B/C/D 对比仍然有效，只是层级形态变了）。

**原设计（未采用）：6 个一级平级菜单**

```
映智汇认证专家平台
├─ ① 系统一览        /overview                     ← 驾驶舱，登录后落地页
├─ ② 企业管理        /enterprises
│    ├─ 企业档案        /enterprises               （列表 + 筛选 + 归档 Tab）
│    └─ 新建企业        /enterprises/new           （向导：基本信息 → 选标准 → 开阶段）
├─ ③ 任务中心        /tasks
│    ├─ NC 检查         /tasks/nc                  （按企业/标准/阶段筛选的任务列表）
│    ├─ 报告生成        /tasks/report
│    └─ 我的关注        /tasks/focused             （is_focused=1）
├─ ④ 资料库          /resources
│    ├─ 资料检索        /resources                 （跨企业全局检索）
│    └─ 目录模板        /resources/templates       （本机构可用目录模板，只读）
├─ ⑤ 组织与成员      /organization
│    ├─ 成员管理        /organization/members      （管理员/审核员/审核组长/企业用户）
│    └─ 角色分组        /organization/roles        （4 个默认分组的只读展示）
└─ ⑥ 系统设置        /settings
     ├─ 个人信息        /settings/profile
     ├─ 账户与用量      /settings/account          （P2 计费，先占位）
     ├─ 消息设置        /settings/notify           （P3，先占位）
     └─ 操作日志        /settings/logs             （操作留痕 + 队列执行日志）
```

#### 3.2.1 ★ 实际实施结构（2026-09-23 端到端验收通过）

```
映智汇认证专家平台
└─ 专家系统                          （MENU_AUD_00，顶层分组，Icon=Avatar，OrderNo=300）
     ├─ 系统一览     /overview        MENU_AUD_01  Odometer        100   ← 登录后落地页（驾驶舱）
     ├─ 企业管理     /enterprises     MENU_AUD_02  OfficeBuilding  200
     ├─ 任务中心     /tasks           MENU_AUD_03  List            300
     ├─ 资料库       /resources       MENU_AUD_04  FolderOpened    400
     ├─ 组织与成员   /organization    MENU_AUD_05  UserFilled      500
     └─ 系统设置     /settings        MENU_AUD_06  Setting         600
```

**为什么改成 1 分组 + 6 子**：

| 理由 | 说明 |
|---|---|
| **用户明确要求** | 「按一个**独立的专家系统大的分类菜单**，建立对应的专家系统应该拥有的子菜单（这样方便我进行**角色-菜单分配**的时候好进行分配）」 |
| **渲染层限制** | 侧边栏只支持 2 层（`el-sub-menu` + `el-menu-item`）。若 6 个一级平级，则每个一级的"子功能"必须靠页面内 Tab 承载，与管理端（2 分组）视觉结构不一致 |
| **角色-菜单分配友好** | 整组勾选「专家系统」= 一次授权全部 6 项，不必逐条勾选 |
| **与原设计不冲突** | 原设计的 6 项全部保留、`Url` 全部保留，只是多了一层分组壳 |

> **更深层级（企业详情 / 阶段详情 / 任务详情）不进菜单**，由页面内 Tab 或子路由承载 —— 与 §3.2 原设计的"隐藏路由"一致，见下。

**隐藏路由（不进菜单，但可独立寻址）**：

| 路由 | 说明 |
|---|---|
| `/enterprises/:code` | 企业详情（内含次级导航：基本信息 / 阶段 / 资料 / 联系人 / 归档记录） |
| `/enterprises/:code/phases/:phaseCode` | 阶段详情（四 Tab：资料库 / NC 检查 / 报告 / 标准目录） |
| `/tasks/:taskCode` | 任务详情（六 Tab：企业信息 / 资料管理 / 执行情况 / 检查结论 / 报告 / 标准条款） |
| `/login`、`/register` | 匿名页（`meta.anonymous`） |

> 任务详情用**同一路由**承载 NC 任务与报告任务（功能设计 V1 §6.2 的设计），靠 Tab 可用性区分，不拆两个页面。

### 3.3 方案对比

| 方案 | 一级菜单 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **A（本文推荐）** | 6 项，见 §3.2 | 覆盖完整形态；任务与企业各归其位；预留位清晰 | 首版有 3 个占位菜单 | ✅ **采用** |
| **B（MVP 极简）** | 4 项：系统一览 / 企业管理 / 任务中心 / 系统设置 | 无占位，每个菜单都能用 | 资料库、成员管理塞进二级，后续要"搬家"，用户会看到菜单变动 | 备选 |
| **C（照抄 8 模块）** | 8 项 | 与功能设计 V1 一致 | 任务导向、与"关注核心是企业"矛盾；协同系统/费用管理首版空置 | ⛔ 否 |
| **D（照抄 5 项）** | 5 项：工作台/企业管理/资源管理/消息中心/个人中心 | 与界面设计 V1 一致 | 丢了"任务"这一核心操作入口；"工作台"与"系统一览"职责重叠 | ⛔ 否 |

**为什么推荐 A 而不是 B**：菜单一旦上线，用户就会形成肌肉记忆。B 方案省下的两个菜单，代价是后续"资料库从二级提升为一级"这类变更 —— 这类变更对老用户是负体验。**菜单一次性种全，用 `Enable` 控制开放**（§3.5），是代价最小的做法。

**A 与 D 的关键差异**：D 的"工作台"被我拆成了两个东西 ——「系统一览」（驾驶舱，看全局状态）与「任务中心」（干活的入口）。理由：**看板与工作台是两种不同的心智**，混在一起会导致页面既想当仪表盘又想当待办列表，最后两者都做不好。

### 3.4 菜单种子表

> ⚠️ **原设计稿为 19 条**（6 父 + 13 子）。**实际实施为 7 条**（1 父 + 6 子），见 §3.4.1。
> 原稿的多层级方案已收敛，理由是侧边栏只渲染 2 层、且首版二级菜单会全是占位页（见 §0.2 偏离 #3）。

#### 3.4.1 ★ 实际入库的 7 条（`scripts/db/` 对应脚本已执行）

`OrderNo` 约定：根 300（排在管理端 `MENU_000xx` 之后），子 100/200/…。`Code` 用 `MENU_AUD_` 前缀与 `MENU_000xx` 区隔。

| # | Code | ParentCode | MenuName | Icon | OrderNo | Url | Tag | Enable |
|---|---|---|---|---|---|---|---|---|
| 1 | `MENU_AUD_00` | `0` | **专家系统** | `Avatar` | 300 | `/` | `auditor` | 1 |
| 2 | `MENU_AUD_01` | `MENU_AUD_00` | 系统一览 | `Odometer` | 100 | `/overview` | `auditor` | 1 |
| 3 | `MENU_AUD_02` | `MENU_AUD_00` | 企业管理 | `OfficeBuilding` | 200 | `/enterprises` | `auditor` | 1 |
| 4 | `MENU_AUD_03` | `MENU_AUD_00` | 任务中心 | `List` | 300 | `/tasks` | `auditor` | 1 |
| 5 | `MENU_AUD_04` | `MENU_AUD_00` | 资料库 | `FolderOpened` | 400 | `/resources` | `auditor` | 1 |
| 6 | `MENU_AUD_05` | `MENU_AUD_00` | 组织与成员 | `UserFilled` | 500 | `/organization` | `auditor` | 1 |
| 7 | `MENU_AUD_06` | `MENU_AUD_00` | 系统设置 | `Setting` | 600 | `/settings` | `auditor` | 1 |

**两步 SQL（缺一不可）**：

```sql
-- ① 种菜单（Tag=auditor 既是分类标签，也是前端分流依据）
INSERT INTO `Sys_Menu`
  (`Code`,`ParentCode`,`MenuName`,`Icon`,`OrderNo`,`Url`,`Tag`,`Enable`,`IsValid`,`IsDeleted`,`CreateTime`,`CreateBy`)
VALUES
  ('MENU_AUD_00','0','专家系统','Avatar',300,'/','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_01','MENU_AUD_00','系统一览','Odometer',100,'/overview','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_02','MENU_AUD_00','企业管理','OfficeBuilding',200,'/enterprises','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_03','MENU_AUD_00','任务中心','List',300,'/tasks','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_04','MENU_AUD_00','资料库','FolderOpened',400,'/resources','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_05','MENU_AUD_00','组织与成员','UserFilled',500,'/organization','auditor',1,1,0,NOW(),'seed_auditor'),
  ('MENU_AUD_06','MENU_AUD_00','系统设置','Setting',600,'/settings','auditor',1,1,0,NOW(),'seed_auditor');

-- ② 授权给专家角色（★ 这一步不做，侧边栏就是空的）
INSERT INTO `Sys_RoleMenu` (`Id`,`RoleCode`,`MenuCode`,`OrderNo`,`CreateTime`,`CreateBy`)
SELECT REPLACE(UUID(),'-',''), 'ROLE_AUDIT_CLIENT_ADMIN', `Code`, `OrderNo`, NOW(), 'seed_auditor'
FROM `Sys_Menu` WHERE `Code` LIKE 'MENU\_AUD\_%' AND `IsDeleted` = 0;

-- ③ 给管理端菜单打 admin 标签（纯分类 + 前端分流依据）
UPDATE `Sys_Menu` SET `Tag` = 'admin' WHERE `Tag` IS NULL AND `Code` LIKE 'MENU\_000%';
```

> - `Sys_RoleMenu.Id` 是 `varchar(64)` 且为 PK（无自增），**必须显式赋值**（`REPLACE(UUID(),'-','')`）。
> - ⚠️ **`LIKE 'MENU_AUD%'` 会把 `_` 当单字符通配符**，虽不致命但语义不精确；用 `LIKE 'MENU\_AUD\_%'` 转义。
> - ⚠️ **本库排序规则已全库统一 `utf8mb4_general_ci`**（`项目全局规则.md` §16.10 铁律八）。
>   若某列仍是 `utf8mb4_0900_ai_ci`，第 ② 步的 `SELECT` 与 `Sys_RoleMenu` 比对时会报
>   **`ERROR 1267 Illegal mix of collations`**。执行前先跑 §16.10 的体检 SQL。

### 3.5 分期开放策略

用 `Sys_Menu.IsValid` 做开关，**不改代码、不删数据**：

| 期 | 开放菜单 | 对应 V4 阶段 |
|---|---|---|
| **首版** | 系统一览、企业档案、新建企业、任务中心（3 项）、资料检索、成员管理、角色分组、个人信息、操作日志 | 阶段 0–3 |
| **核心卖点上线** | （全部已开放） | 阶段 4 |
| **计费上线** | 账户与用量（`Enable=1`） | 阶段 5 |
| **延展** | 目录模板、消息设置 | 阶段 6 / P3 |

**未实现路由的兜底**：菜单已开放但页面未实现时，路由统一指向**共享层**占位组件
`cert-share/src/components/CertPagePlaceholder.vue`（props：`title` / `desc?` / `features?[]`，用 `el-card` + `el-empty` 展示规划说明），
**不要留白屏或 404**。管理端与专家端共用同一实现（实施时由专家端专属的 `pages/_placeholder/` 上移到共享层，见 §0.2 偏离 #5）。

### 3.6 与功能设计 V1 的 8 模块映射

确保"功能不削减"：

| 8 模块（功能设计 V1） | 本方案落点 |
|---|---|
| ① 当前任务 | 任务中心 → 我的关注 |
| ② 企业管理 | 企业管理（企业档案 + 新建企业） |
| ③ 工作平台 | 任务中心 → NC 检查 / 报告生成 |
| ④ 协同系统（P2） | ⏸ 暂不占位（依赖企业用户账号体系，属阶段 6） |
| ⑤ 企业资料 | 资料库 → 资料检索 |
| ⑥ 费用管理（P2） | 系统设置 → 账户与用量（`Enable=0` 占位） |
| ⑦ 系统日志 | 系统设置 → 操作日志 |
| ⑧ 系统管理 | 系统设置 → 个人信息 / 消息设置 |

**未落点的一项**：④ 协同系统。它与"企业用户"角色强耦合（需要 `ent_enterprise_user` 表 + 企业侧登录），在阶段 6 之前连占位都无意义（点进去什么都没有）。**建议首版不种这条菜单**，待阶段 6 再补 —— 这是唯一一处与"菜单一次种全"原则的例外，理由是该功能的用户对象都还不存在。

---

## 第四篇 系统一览（驾驶舱）设计

### 4.1 设计目标与心智模型

**要回答的唯一问题**：「我现在该干什么？」

**两个心智维度**（这是整个页面的骨架）：

| 维度 | 问题 | 数据特征 |
|---|---|---|
| **能力** | 我选的机构**给了我什么**？ | 来自后台配置，**真实有数据**（3 标准 / 23 阶段 / 4 NC 规则 / 2 报告模板） |
| **业务** | 我用这些能力**做了什么**？ | 来自我自己的工作区，**当前全 0 行** |

> ★ **关键设计洞察**：新注册的专家工作区是空的，但"能力"不是空的。所以驾驶舱在 MVP 阶段**不是靠业务统计撑起来的，而是靠"能力就绪度"** —— 它把 V4 §1.2 的产品逻辑（选机构 = 选能力）变成用户看得见的东西。业务指标区**允许为空**，用引导态替代。

**明确不做的事**：不做花哨的图表（0 数据时图表是噪音）；不做实时刷新（首版按需刷新 + 手动刷新按钮）；不做可拖拽自定义布局（个人 SaaS 不需要）。

### 4.2 布局

**三段式滚动，五区**：

```
┌────────────────────────────────────────────────────────────────────────────┐
│  系统一览                                              [刷新]  2026-09-23 12:00│
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │ ⓪ 身份条                                                              │  │
│  │ 工作区：尚龙认证 · 张三 ｜ 挂靠机构：河北雄安尚龙认证有限公司 ｜        │  │
│  │ 角色：体系认证客户端管理员 ｜ 剩余次数：—（未开通计费）                 │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────────────────────┤
│  第一屏：待办 + 能力（左 7 : 右 5）                                          │
│  ┌───────────────────────────────────┐ ┌──────────────────────────────────┐ │
│  │ ① 待办聚合                         │ │ ② 能力就绪度 ★                   │ │
│  │ ┌────┬────┬────┬────┬────┐        │ │ 来自「河北雄安尚龙认证有限公司」  │ │
│  │ │待复核│执行中│失败│资料待确认│NC逾期│  │ │ ┌──────┬──────┬──────┬──────┐   │ │
│  │ │  0 │  0 │  0 │   0    │  0  │        │ │ │可用标准│认证阶段│NC规则│报告模板│  │ │
│  │ └────┴────┴────┴────┴────┘        │ │ │   3  │  23  │  4  │  2  │   │ │
│  │ 全 0 → 替换为引导条：              │ │ └──────┴──────┴──────┴──────┘   │ │
│  │ 「暂无待办 · 去创建第一家企业 →」   │ │ 标准：ISO 9001:2015 ｜ ISO 13485 │ │
│  └───────────────────────────────────┘ │      ｜ TEST-TT-001              │ │
│                                        │ 说明：这是你当前能使用的能力边界  │ │
│                                        └──────────────────────────────────┘ │
├────────────────────────────────────────────────────────────────────────────┤
│  第二屏：③ 企业进度矩阵（全局视觉核心）                                       │
│  ┌──────────┬───────────┬────────┬──────┬──────┬──────────────────┐        │
│  │ 企业      │ 标准集     │ 资料    │ NC   │ 报告 │ 完成度            │        │
│  ├──────────┼───────────┼────────┼──────┼──────┼──────────────────┤        │
│  │ 企业A     │ 9001+13485│ 12/15  │ 2/3  │ 0/1  │ ███████░░░  68%  │        │
│  │ 企业B     │ 9001      │ 8/8    │ 3/3  │ 1/1  │ ██████████ 100%  │        │
│  └──────────┴───────────┴────────┴──────┴──────┴──────────────────┘        │
│  空态：还没有企业 · [+ 新建企业]                                             │
├────────────────────────────────────────────────────────────────────────────┤
│  第三屏：最近任务 + 快捷入口（左 8 : 右 4）                                   │
│  ┌───────────────────────────────────────┐ ┌──────────────────────────────┐ │
│  │ ④ 最近任务（最多 8 条）                │ │ ⑤ 快捷入口                    │ │
│  │ 任务号 │企业│标准│类型│状态│耗时│时间   │ │ [+ 新建企业]                 │ │
│  │ （空态：引导先建企业）                  │ │ [一键 NC]  [一键报告]         │ │
│  └───────────────────────────────────────┘ │ [资料检索] [导出清单]         │ │
│                                            │ 未就绪 → 置灰 + tooltip       │ │
│                                            └──────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

**③ 企业进度矩阵 —— 为什么它是"全局视觉"的核心**

其他区都是"单点数字"，只有矩阵是**二维视图**：行 = 企业，列 = 核心链路的四个环节（资料 → NC → 报告 → 完成度）。专家扫一眼就知道"哪家企业卡在哪一步"。

- 每个计数格都是**可点击的**：点 `2/3` 直接跳到该企业该环节的任务列表
- 完成度 = 四环节加权（资料 30% + NC 40% + 报告 30%），权重后续可由后台配置
- 排序：完成度升序（**最需要关注的排最上面**），而非按时间

> 这一区在业务数据为 0 时显示引导空态 —— 但它的**结构**必须先定下来，因为它是整个页面的视觉锚点。

### 4.3 各区数据源与降级矩阵 ★

> 这张表是本文最实用的部分：它明确了**每一格数字从哪来、现在有没有、没有时显示什么**。

| 区 | 指标 | 数据源 | 现状 | 降级表现 |
|---|---|---|---|---|
| ⓪ | 工作区名 | `Sys_Organization.OrgName`（当前用户 `OrgPath` 第 2 段） | ✅ 有 | — |
| ⓪ | 挂靠机构名 | 工作区节点 `Sys_Organization.OrgCode` → `cert_certification_body` | ⚠️ **键不一致，查空**（§5.1） | 显示"—" |
| ⓪ | 角色名 | JWT claims `role` → `Sys_Role.RoleName` | ✅ 有 | — |
| ⓪ | 剩余次数 | `billing_account`（阶段 5） | ❌ 无表 | "—（未开通计费）" |
| ① | 待复核 | `audit_task` 状态派生 | ❌ 0 行 | 计入"暂无待办" |
| ① | 执行中 | `wf_execution_task` / `audit_task` | ❌ 0 行 | 同上 |
| ① | 执行失败 | 同上 | ❌ 0 行 | 同上 |
| ① | 资料待确认 | `ent_enterprise_file` | ❌ 0 行 | 同上 |
| ① | NC 逾期 | `audit_nonconformity` | ❌ 0 行 | 同上 |
| ② | 可用标准数 | `cert_org_standard WHERE OrgCode = 机构Code` | ✅ **3** | — |
| ② | 认证阶段数 | `cert_org_stage WHERE OrgCode = 机构Code` | ✅ **23** | — |
| ② | NC 规则数 | `cert_validation_rule`（按机构/标准过滤） | ✅ **4** | — |
| ② | 报告模板数 | `cert_report_template`（按机构/标准过滤） | ✅ **2** | — |
| ② | 标准清单 | `cert_iso_standard`（经 `cert_org_standard` 关联） | ✅ 有 | — |
| ③ | 企业 + 四环节计数 | `ent_enterprise` + `ent_enterprise_phase` + `ent_enterprise_file` + `audit_task` + `rpt_report_task` | ❌ 全 0 行 | 空态引导 |
| ④ | 最近任务 | `audit_task` ORDER BY `CreateTime` DESC LIMIT 8 | ❌ 0 行 | 空态引导 |
| ⑤ | 快捷入口 | 依赖前置数据是否存在 | ❌ | 全部置灰 + tooltip |

**结论**：MVP 阶段**只有 ② 能力就绪度能出真数据**，其余四区走降级。这不是缺陷 —— 它恰好把"注册 → 立即可用"的产品承诺具象化了：**你一注册就能看到这个机构给你配好的能力**。

### 4.4 空态与降级策略

| 场景 | 表现 |
|---|---|
| 全部业务数据为 0（新注册） | ① 显示引导条「暂无待办 · 去创建第一家企业 →」；③ 显示引导卡；④ 显示引导卡；⑤ 快捷入口仅「新建企业」可用 |
| 能力数据为 0（机构未配标准） | ② 显示警告态「该机构尚未配置可用标准，请联系平台运营」—— **这是异常，必须显式提示**，不能静默显示 0 |
| 某个聚合查询失败 | 该区独立降级为「数据加载失败 · 点击重试」，**其余区正常渲染**；响应带 `Partial=true` + `Missing[]`（沿用 `AuditTask/detail` 的降级契约） |
| 挂靠机构查不到 | ⓪ 机构名显示「—」，② 显示「能力信息不可用」，页面其余部分正常 |

**原则**：**任一区失败都不允许整页白屏**。驾驶舱是登录后第一屏，白屏等于系统不可用。

### 4.5 接口契约

**单接口聚合**（首屏只发 1 个请求，符合"驾驶舱要快"）：

```
GET /api/Overview/summary
认证：强认证（[YZHAuthorize]）
```

```jsonc
// ApiResponse<OverviewSummaryDto>（E1 信封 camelCase；data 内层为 C# DTO，PascalCase）
{
  "success": true, "code": 200, "message": "获取成功", "timestamp": 1758600000000,
  "data": {
    "Workspace": {                      // ⓪ 身份条
      "OrgCode": "…", "OrgName": "尚龙认证 · 张三",
      "CbCode": "CB001", "CbName": "河北雄安尚龙认证有限公司",
      "RoleCode": "ROLE_AUDIT_CLIENT_ADMIN", "RoleName": "体系认证客户端管理员",
      "QuotaLeft": null                 // null = 未开通计费
    },
    "Capability": {                     // ② 能力就绪度
      "StandardCount": 3, "StageCount": 23,
      "NcRuleCount": 4, "ReportTemplateCount": 2,
      "Standards": [
        { "Code": "77deabb4…", "StandardCode": "ISO 9001:2015", "StandardName": "质量管理体系", "VersionYear": 2015 }
      ]
    },
    "Todo": {                           // ① 待办聚合
      "PendingReview": 0, "Running": 0, "Failed": 0, "FilePending": 0, "NcOverdue": 0
    },
    "EnterpriseProgress": [             // ③ 企业进度矩阵
      {
        "EnterpriseCode": "…", "EnterpriseName": "企业A",
        "Standards": "ISO 9001:2015 + ISO 13485:2016",
        "FileDone": 12, "FileTotal": 15,
        "NcDone": 2, "NcTotal": 3,
        "ReportDone": 0, "ReportTotal": 1,
        "Percent": 68
      }
    ],
    "RecentTasks": [                    // ④ 最近任务
      {
        "TaskCode": "…", "TaskNumber": "NC-2026-0001",
        "EnterpriseName": "企业A", "StandardName": "ISO 9001:2015",
        "TaskType": "nc_check", "Status": "completed",
        "DurationMs": 12400, "CreateTime": "2026-09-23 11:20:00"
      }
    ],
    "Partial": false,                   // true = 有区块降级
    "Missing": []                       // 降级区块名，如 ["RecentTasks"]
  }
}
```

**三层同构落位**（`docs/30-项目规则/前后端代码结构统一规则-V1.md` 强制）：

| 层 | 文件 |
|---|---|
| 后端 Controller | `src/certplatform-api/CertPlatform.Auditor/Controllers/OverviewController.cs` |
| 前端 API | `src/certplatform-web/cert/cert-auditor/src/api/overview.ts` |
| 前端 Pages | `src/certplatform-web/cert/cert-auditor/src/pages/overview/index.vue` |

> 若后续拆成多接口（`/capability`、`/todo`、`/enterpriseProgress`），路径仍在 `Overview` 控制器下，**控制器名与动作名一旦确定不要改**（改了就断 `sys_role_api` 关联，见 §2.4 约束 5）。

**⚠️ 新增接口后必做**：跑 ApiSync → `sys_api` 出现 `api/Overview/summary` → 在 `sys_role_api` 里关联到 `ROLE_AUDIT_CLIENT_ADMIN`，否则专家调用会 **403**。

### 4.6 前端落位

```
cert-auditor/src/
├─ api/overview.ts                 ← 新（三层同构）
├─ store/menu.ts                   ← 新（照搬 cert-admin 模式，改用 @share 或本地实现）
├─ layouts/AuditorLayout.vue       ← 改：静态单菜单 → 动态菜单树
├─ router/index.ts                 ← 改：加 /overview 等路由 + redirect '/' → '/overview'
├─ pages/
│   ├─ overview/index.vue          ← 新（驾驶舱，五区）
│   ├─ enterprises/…               ← 新
│   ├─ tasks/…                     ← 新
│   ├─ resources/…                 ← 新
│   ├─ organization/…              ← 新
│   ├─ settings/…                  ← 新
│   └─ _placeholder/index.vue      ← 新（未实现路由的兜底页）
└─ components/overview/
    ├─ CapabilityPanel.vue         ← 能力就绪度卡
    ├─ TodoBar.vue                 ← 待办聚合条
    ├─ EnterpriseProgressTable.vue ← 企业进度矩阵
    └─ RecentTaskList.vue          ← 最近任务
```

**分层纪律**（V4 §2.3）：`OverviewController` 与驾驶舱四张卡片是**专家版专属**（个人空间视角），**放 `cert-auditor`，不进 `cert-share`**。但矩阵里用到的**状态徽标 / 进度条 / 空态引导**等基础展示组件，未来机构版也需要 → 应进 `cert-share`。

---

## 第五篇 建设意见

### 5.1 ★ 关键路径上的三个阻塞

> **状态（2026-09-23）**：阻塞 2 ✅ 已解｜阻塞 1 ⛔ 未解（**阻塞驾驶舱第 5 步**）｜阻塞 3 ⛔ 未解
> 三条都在关键路径上：不修，驾驶舱做出来就是"全是 0 和 —"，会让人误以为驾驶舱本身没做完。

#### 阻塞 1：机构关联键不一致 —— 驾驶舱能力查询会**查空且不报错**（最严重）

**实测证据**：

| 数据源 | 关联键取值 | 实例 |
|---|---|---|
| `cert_certification_body.Code` | GUID | `5833c904-a802-11f1-9d10-96af2c2a4a1a` |
| `cert_certification_body.CbCode` | 业务码 | `CB001` |
| `cert_org_standard.OrgCode` | **= 机构 `Code`（GUID）** | `5833c904-…` |
| `cert_org_stage.OrgCode` | **= 机构 `Code`（GUID）** | `5833c904-…` |
| 注册时写入的 `Sys_Organization.OrgCode` | **= 机构 `CbCode`** | `CB001` ⛔ |

→ 若驾驶舱按"工作区 `OrgCode` = 能力表 `OrgCode`"查询，**匹配 0 行**，页面显示"可用标准 0 个"，且**不会有任何报错**。

**这正是列名铁律要防的那类问题**：字段名一样，语义不一样，静默失配。

**修复方案（三选一，建议 A）**：

| 方案 | 做法 | 评价 |
|---|---|---|
| **A（推荐）** | 注册时改为写 `Sys_Organization.OrgCode = certBody.Code`（GUID），与能力表对齐 | 改动最小（1 行代码 + 1 条数据修正脚本），语义仍成立（`OrgCode` 是"机构编码"）；`CbCode` 另找位置存 |
| B | 能力表 `OrgCode` 全部改存 `CbCode` | ⛔ 破坏存量数据与后台配置页，不可接受 |
| C | 驾驶舱查询时做 `CbCode → Code` 转换 | 只是把问题藏在查询层，下一个调用方还会踩 |

**附加要求**：`cert_auditor_org.CbCode`（V4 §4.6 设计的工作区 Attach 表）应尽快建表 —— 它是存放 `CbCode` / 配额 / 到期的正位，能把"机构归属"从 `Sys_Organization` 的语义借用中解放出来。

#### 阻塞 2：注册用户登录后侧边栏为空 —— ✅ 已解决（2026-09-23）

~~`ROLE_AUDIT_CLIENT_ADMIN` 的 `Sys_RoleMenu` = **0 条** → 按 `MenuPermissionService` 逻辑，非超管用户拿到的菜单列表是**空数组**。~~

**已修复**：执行 §3.4.1 的种子 SQL 第 ② 步 → 现授权 **7 条**。
**端到端验收证据**：专家账号 `wzm`（`RoleCode=ROLE_AUDIT_CLIENT_ADMIN`）登录 → `GET /api/System/MenuManagement/tree` 返回 7 条 → 侧边栏渲染 1 分组 + 6 子项，6 条路由逐个点击均可达、无 404。

#### 阻塞 3：机构下拉混入无 `CbCode` 的脏数据 —— ⛔ 仍未修复（2026-09-23 复测）

`cert_certification_body` 中 `IsValid=1 AND Status='active'` 的有 **7 条**，其中 **3 条 `CbCode` 为 NULL**：

| Name | CbCode | 评价 |
|---|---|---|
| 河北雄安尚龙认证有限公司 | `CB001` | ✅ 真实可用 |
| 测试认证机构A-改名 | `CBTEST001X` | ✅ 有码 |
| 测试机构_694ed346… | `TEST_694ed346…` | ✅ 有码 |
| 事务测试_82826dac… | `TX_82826dac…` | ✅ 有码 |
| 测试认证机构 | **NULL** | ⛔ 脏数据 |
| 接口驱动测试 | **NULL** | ⛔ 脏数据 |
| 6 | **NULL** | ⛔ 脏数据 |

→ 专家注册下拉里会看到「测试认证机构」「接口驱动测试」「6」，选中后 `OrgCode` 写入 NULL、能力边界为空。

**修复（未做）**：
1. `AuditorRegisterService.GetCertBodyOptionsAsync()`（约 110–125 行）的查询条件加 `&& !string.IsNullOrEmpty(x.CbCode)`；
2. 把 3 条 `CbCode IS NULL` 的机构置为 `Status='inactive'` 或 `IsValid=0`。

> ⚠️ **`CbCode` 是注册下拉的过滤键，但 `OrgCode` 的语义键是 `Code`（GUID）** —— 见阻塞 1，两件事不要混。

> 这三条都不大，但**都在关键路径上**：不修，驾驶舱做出来就是"全是 0 和 —"，会让人误以为驾驶舱本身没做完。

### 5.2 实施顺序建议

> **进度更新（2026-09-23）**：第 2/3/4 步 ✅ 已完成并端到端验收；第 1 步 **部分完成**（阻塞 2 已解，阻塞 1/3 未解）；第 5–8 步待做。

| 序 | 事项 | 依赖 | 交付 | 状态 |
|---|---|---|---|---|
| 1 | 修 §5.1 三个阻塞 | — | 数据正确 + 菜单可见 | ⏳ **部分**：阻塞 2 ✅；阻塞 1（机构关联键）⛔；阻塞 3（下拉脏数据）⛔ |
| 2 | 种菜单 + 授权（§3.4.1 SQL） | 1 | 侧边栏出现 1 分组 + 6 子菜单 | ✅ 完成 |
| 3 | 菜单 store + `AuditorLayout.vue` 动态菜单 | 2 | 菜单由后台驱动，不再是写死的 1 条 | ✅ 完成（store 上移 `cert-share/composables/useMenuTree.ts`） |
| 4 | 路由骨架 + 占位页兜底 | 3 | 6 条菜单全部可点，不白屏 | ✅ 完成（占位页 `CertPagePlaceholder.vue`） |
| 5 | `GET /api/Overview/summary` 后端（**先只实现 Capability，其余返回空结构**） | 1 | 驾驶舱能出真数据 | ⛔ 待做 |
| 6 | `pages/overview/index.vue` 驾驶舱五区 | 5 | 首屏可用 | ⛔ 待做 |
| 7 | ApiSync + `sys_role_api` 关联 `Overview/*` | 5 | 专家调用不 403 | ⛔ 待做 |
| 8 | 逐区把 ①③④ 从"降级空态"升级为真实数据 | 阶段 3/4 业务接口 | 驾驶舱完整 | ⛔ 待做 |

**关键点**：第 5 步**刻意只实现 Capability 一个区**。理由 —— 接口契约先定死（`OverviewSummaryDto` 全字段返回，其余给空结构），前端一次成型，后续只是后端填数，**前端不用改**。这样能把"驾驶舱"和"业务接口"解耦，不被阶段 3/4 阻塞。

> ⚠️ **第 5 步之前必须先解阻塞 1**：否则 Capability 区按"工作区 `OrgCode` = 能力表 `OrgCode`"查询会**匹配 0 行且不报错**，驾驶舱会显示"可用标准 0 个"——看起来像"驾驶舱没做完"，实际是数据关联键不一致。

### 5.3 风险

| 风险 | 等级 | 缓解 |
|---|---|---|
| **机构关联键不一致导致能力查询静默查空** | **高** | §5.1 阻塞 1，第一件事 |
| 误以为设 `Tag` 就能隔离菜单 → 管理端菜单暴露给专家 | **高** | 本文 §2.1 已明确（**后端不隔离，前端按 `Tag` 分流**）；代码注释同步标注；**绝不授权 admin 菜单给专家角色** |
| 用超管账号测专家端 → 看到全量菜单，误判"隔离失效" | 中 → ✅ **已缓解** | 后端给超管返回**全量 33 条**（实测），但**前端 `filterMenuTreeByTag` 已拦截** → 管理端实测只渲染 26 条、专家菜单零泄漏。**测试仍应用真实注册账号**（超管路径只验证了前端分流这一层） |
| 前端分流被绕过（如新增端口未接入 `filterMenuTreeByTag`） | 中 | 新增前端包/端口时，**必须**在 Layout 里调 `filterMenuTreeByTag(menus, '<tag>')`；否则超管会看到另一端菜单，点击 404 |
| 改控制器名/动作名 → `sys_role_api` 关联静默断裂 → 403 | 中 | 遵守 §2.4 约束 5；改名后必跑 ApiSync + 重关联 |
| 菜单一次性种全，但页面长期不实现 → 用户点进去全是"建设中" | 中 | 用 `Enable=0` 关闭未实现项（§3.5），不要靠占位页撑场面 |
| 驾驶舱聚合查询慢（跨 6 张表） | 低 | 首版按需查询 + 手动刷新；后续加缓存；**不要**做实时轮询 |
| 18 条菜单 + 未来机构版菜单混在一张 `Sys_Menu` 表 | 低 | 用 `Code` 前缀（`MENU_AUD_`）+ `Tag` 双维度区分 |

### 5.4 顺手要修的文档问题

| # | 问题 | 处置 |
|---|---|---|
| 1 | `docs/10-YZH架构/14-框架核心功能设计-V1.md` §4.5 称 `sys_api`/`sys_role_api` "均未创建" | 实际已建（399 / 90 条）→ **需修订**，否则误导 |
| 2 | 《审核员端功能设计-V1》§2.2 决策 D1 称"`credit_code`/`enterprise_no` 全局唯一约束维持不变" | 与"同一企业换审核员 = 新建独立档案"自相矛盾，V4 §4.2 已推翻 → **需回写修订** |
| 3 | 三份文档的导航口径不一致（8 / 5 / 技术目录） | 本文 §3.2 已收敛为 6 项 → 三份文档的导航章节**需同步指向本文** |
| 4 | V4 计划放在 `输出产物/` | 建议移入 `docs/20-体系认证/` 并登记到 README |
| 5 | `docs/20-体系认证/03-详细设计/02-审核员端/` 目录名与产品定位（专家端）已不符 | 建议改为 `02-专家端/` 或与本文 `04-专家端/` 合并 |

---

## 第六篇 待你决策

| # | 决策点 | 选项 | 我的建议 | 状态 |
|---|---|---|---|---|
| **M1** | 一级菜单数量 | A 6 项平级（本文 §3.2）<br>B 4 项 MVP 极简<br>**C 1 个分组 + 6 子菜单** | C —— 用户已拍板「独立专家系统大分类 + 子菜单，便于角色-菜单分配」 | ✅ **已定并实施**（§3.2.1） |
| **M2** | 驾驶舱与"任务中心"的关系 | A 拆开（驾驶舱看全局 / 任务中心干活）<br>B 合并为一个"工作台" | **A** —— 看板与工作台是两种心智 | ⏳ 未决（做驾驶舱时确认） |
| **M3** | 机构关联键修复方案 | A 注册改写机构 `Code`（GUID）<br>B 能力表改存 `CbCode`<br>C 查询层转换 | **A**（§5.1 阻塞 1） | ⛔ **未决 —— 阻塞第 5 步，建议尽快拍板** |
| **M4** | 驾驶舱首版范围 | A 只实现"能力就绪度"，其余降级空态<br>B 等业务接口齐了再上 | **A** —— 接口契约先定死，前端一次成型 | ⏳ 未决 |
| **M5** | 驾驶舱是否要图表 | A 不要（0 数据时是噪音）<br>B 要（折线/环形） | **A** —— 首版用进度条 + 数字卡，数据起来后再评估 | ⏳ 未决 |
| **M6** | "协同系统"是否占位 | A 不种菜单（等阶段 6）<br>B 种菜单 + 置灰 | **A** —— 该功能的用户对象（企业用户）还不存在 | ✅ **已随 M1 落实**（未种该菜单） |
| **M7** | 菜单 `Code` 前缀 | A `MENU_AUD_xx`<br>B 沿用 `MENU_000xx` 顺延 | **A** —— 与现有 26 条一眼可分 | ✅ **已定并实施**（`MENU_AUD_00`~`06`） |
| **M8** | 是否同步修订 §5.4 的五处文档问题 | A 本轮一并修订<br>B 另开一轮 | **A**（其中 #1 #2 属事实性错误，建议尽快） | ⏳ 部分（本文自身已修订） |
| **M9** | 专家端接口授权（`AuditorAuth/*` 是否关联 `sys_role_api`） | A 关联（去掉对 `[AllowAnonymous]` 的依赖）<br>B 维持现状（注册/下拉本就该匿名） | **B** —— 注册与机构下拉**必须匿名可访问**，关联反而不对 | ✅ **已定：维持 `[AllowAnonymous]`**（但**新增**的受保护接口必须关联，见 §2.4 约束 5） |

---

## 附录 A：核实的关键证据

### A.1 设计评审期（2026-09-23 上午）

| 结论 | 证据 |
|---|---|
| `docs/30-项目规则` 无专家系统文档 | 全目录 grep `专家` → 0 命中 |
| 菜单过滤只认 `Sys_RoleMenu` | `MenuPermissionService.GetVisibleMenusAsync()` 源码；`Tag` 未出现在过滤逻辑中 |
| `Tag` 仅在 Vol 兼容端点当 `tableName` | `MenuController.GetTreeMenu()` 第 79 行 `tableName = x.Tag` |
| 前端用的端点是 `MenuManagement/tree` | `cert-admin/src/api/system/menu.ts` → `yzhApi.get('/api/System/MenuManagement/tree')` |
| `Sys_Menu` 26 条、`Tag` 全 NULL | `SELECT Id,Code,ParentCode,MenuName,Icon,OrderNo,Url,Tag FROM Sys_Menu WHERE IsDeleted=0` |
| 该角色菜单授权 0 条 | `SELECT RoleCode, COUNT(*) FROM Sys_RoleMenu GROUP BY RoleCode`（无 `ROLE_AUDIT_CLIENT_ADMIN` 行） |
| `sys_api` 399 / `sys_role_api` 90 / `Sys_RoleAuth` 330 | 三表 COUNT |
| 专家端两接口已在 `sys_api` 但未授权 | `sys_api` Id 440/441（`api/AuditorAuth/*`）；`sys_role_api` 中 `Path LIKE '%AuditorAuth%'` → 0 行 |
| 能力数据：标准 3 / 阶段 23 / NC 规则 4 / 报告模板 2 | 四表 COUNT |
| **能力表关联键 = 机构 `Code`（GUID），注册写的是 `CbCode`** | `cert_org_standard.OrgCode` / `cert_org_stage.OrgCode` 实测值 `5833c904-…` vs `cert_certification_body.CbCode = 'CB001'` |
| 业务数据全 0 | `ent_enterprise` 0 / `audit_task` 0 |
| 注册写 `OrgCode = CbCode` | `AuditorRegisterService.cs:187` `OrgCode = certBody.CbCode` |
| 工作区节点是 L2、用户挂 L3 分组 | `AuditorRegisterService.cs`：`WorkspaceLevel=2`、`GroupLevel=3`、`Sys_User.OrgCode = ownerGroupCode` |

### A.2 实施验收期（2026-09-23 下午，端到端）

| 结论 | 证据 |
|---|---|
| 菜单已入库 7 条（1 父 + 6 子） | `SELECT Code,MenuName,Icon,Tag,ParentCode,OrderNo,Url FROM Sys_Menu WHERE Tag='auditor'` → 7 行 |
| `Tag` 已全部填充 | `Sys_Menu` 33 条：`Tag=admin` 26 / `Tag=auditor` 7 / `Tag IS NULL` 0 |
| 专家角色已授权 7 条菜单 | `SELECT COUNT(*) FROM Sys_RoleMenu WHERE RoleCode='ROLE_AUDIT_CLIENT_ADMIN'` → 7 |
| 专家账号登录后拿到 7 条 | `wzm` 登录（`RoleCode=ROLE_AUDIT_CLIENT_ADMIN`）→ `GET /api/System/MenuManagement/tree` → `data.length = 7` |
| 侧边栏渲染 1 分组 + 6 子项，图标全出 | 浏览器 DOM：`.el-sub-menu` = 1（标题「专家系统」），其下 `.el-menu-item` = 6；7 个节点均 `querySelector('svg') !== null` |
| 6 条路由全部可达、无 404 | 逐个点击：`/overview` `/enterprises` `/tasks` `/resources` `/organization` `/settings` 全部命中，页头标题同步 |
| **管理端零泄漏（前端分流生效）** | 超管 `cb001_admin` 从后端拿到**全量 33 条**，但管理端侧边栏只渲染 **26 条**（系统管理 9 + 业务管理 15 + 2 分组节点）；`aside.innerText.includes('专家系统') === false` |
| 前端契约映射正确 | `cert-share/src/api/system/menu.ts` 的 `normalizeMenu()` 把后端 PascalCase（`MenuName`/`ParentCode`/…）转 camelCase；`buildTree()` 建树 —— **字段名不是 `Name` 而是 `MenuName`，直接读 `name` 会静默空白** |
| `sys_api` 无 `ControllerName` 列 | `DESC sys_api` → `Id/Code/Method/Path/GroupPath/Name/Author/Enable/…`；按控制器名检索必须用 `Path LIKE '%…%'` |

## 附录 B：关联文档

| 文档 | 路径 |
|---|---|
| 产品定义（权威源） | `输出产物/体系认证专家系统建设计划-V4.md` |
| 专家端功能设计（前身） | `docs/20-体系认证/03-详细设计/02-审核员端/审核员端功能设计-V1.md` |
| 专家端界面设计（前身） | `docs/20-体系认证/03-详细设计/02-审核员端/审核员端界面设计-页面结构-V1.md` |
| 框架菜单/权限机制 | `docs/10-YZH架构/14-框架核心功能设计-V1.md` §4–§5 |
| 框架数据库设计 | `docs/10-YZH架构/13-框架数据库设计-V1.md` §6.1 |
| 接口权限自动同步 | `docs/10-YZH架构/11-接口权限自动同步设计-V1.md` |
| 三层同构规则 | `docs/30-项目规则/前后端代码结构统一规则-V1.md` |
| 项目宪法 | `项目全局规则.md` |
| 架构唯一入口 | `docs/10-YZH架构/README.md` |

---

*本文为菜单与驾驶舱设计评审稿，评审通过后进入页面实现。*



# 02-数据库设计 — 业务表设计

> **状态**：生效 | **更新**：2026-09-12

## 内容

| 文档 | 说明 |
|------|------|
| `数据库表设计-V2.md` | 业务表设计（六大数据域 + 附录） |

## 范围

本层只覆盖**业务表**：`cert_` / `ent_` / `wf_` / `dir_` / `rpt_` 前缀。

**框架表**（`Sys_*` / `sys_*` / `yzh_*`，共 28 张）见
[`10-YZH架构/13-框架数据库设计-V1.md`](../../10-YZH架构/13-框架数据库设计-V1.md)。

## 已知待修正

`数据库表设计-V2.md` 的**域 E「系统基础」**存在两处表名错误，且只覆盖 5/28 张框架表：

| 文档中写的 | 数据库中实际的 |
|------------|----------------|
| `Sys_Log` | `sys_log` |
| `SystemConfig` | `sys_config` |

**处置建议**：域 E 移除，改为一行指针指向 `10-YZH架构/13-框架数据库设计-V1.md`。

## 数据库连接

- MySQL 8.0 @ `yzh-mysql` 容器，端口 **3307**
- 库名：`yzh_cert_platform`
- 脚本统一放 `scripts/db/`

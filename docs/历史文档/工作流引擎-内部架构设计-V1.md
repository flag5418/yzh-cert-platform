# 工作流引擎 - 内部架构设计 V1

> **版本**：V1.0 | **日期**：2026-08-21 | **状态**：设计定稿
>
> **定位**：引擎内部模块划分、执行协议、数据结构设计的权威文档。**引擎如何设计、能力如何划分、模块间如何协作的工程师级说明书**。
>
> **与现有文档的边界**：
> - `审核规则库与工作流设计器-功能设计-V4.md`：功能设计（**做什么**，11 章模板）
> - `自定义工作流引擎-功能设计-V1.md`：引擎对外契约与用户交互（**怎么呈现**）
> - **本文件**：引擎内部模块设计与数据流（**怎么拆、怎么实现**）
>
> **承接来源**：docs/历史文档/归档-2026-08-xx-工作流架构讨论/（讨论稿已归档，核心设计吸收至本文档）
>
> **前置阅读**：
> - `项目全局规则.md`（宪法）、`技能清单-V1.md`（Skill 契约）、`vol-skill.md`（Vol 钩子）

---

## 一、引擎总体结构

### 1.1 为什么需要这份文档

| 现有文档覆盖 | 现有文档缺失 |
|---|---|
| 引擎能做什么（功能设计） | 引擎内部如何拆分 |
| 用户交互怎么设计（画布/面板） | 执行时数据怎么流转 |
| Skill 如何注册（元数据契约） | 队列如何驱动执行 |
| 配置 JSON 怎么校验 | 日志、存储、测试如何统一 |
| 节点如何分类 | 模块间接口边界在哪 |

**本文档解决**：引擎内部的**模块划分、接口定义、数据协议、执行生命周期**。

### 1.2 引擎六大核心模块

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         工作流引擎（WorkflowEngine）                      │
│                                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  任务管理器  │  │  DAG 解释器  │  │ 节点执行器   │  │ 上下文管理器 │    │
│  │  Execution  │→│  Workflow   │→│  Node       │→│  Workflow   │    │
│  │  Task Mgr   │  │  Interpreter│  │  Executor   │  │  Context Mgr│    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
│        ↑                ↑                ↑                ↑            │
│        │                │                │                │            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────────┐  │
│  │ 队列集成器   │  │ 路径分析器   │  │         存储层                   │  │
│  │  Queue      │  │  Topology   │  │ wf_execution_task               │  │
│  │  Integrator │  │  Analyzer   │  │ wf_node_output                  │  │
│  └─────────────┘  └─────────────┘  │ wf_execution_log                │  │
│                                     └─────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.3 模块职责与接口定义

```csharp
namespace YZH.Core.Workflow
{
    /// <summary>
    /// 入口：创建执行任务（TEST / NC_CHECK / REPORT_GENERATE）
    /// 对外：唯一创建执行上下文的入口
    /// </summary>
    public interface IExecutionTaskManager
    {
        /// <summary>创建执行任务</summary>
        Task<ExecutionTask> CreateAsync(CreateTaskRequest req);
        /// <summary>查询任务状态</summary>
        Task<ExecutionTask> GetByCodeAsync(string taskCode);
        /// <summary>取消任务</summary>
        Task<bool> CancelAsync(string taskCode, string reason);
    }

    /// <summary>
    /// 入口：拓扑排序，输出确定性的节点执行顺序
    /// 输入：workflow_config JSON  输出：nodeId[] 执行序列
    /// </summary>
    public interface ITopologyAnalyzer
    {
        IReadOnlyList<string> TopoSort(WorkflowConfig config);
        bool HasCycles(WorkflowConfig config, out List<string> cycle);
        IReadOnlyList<PathSegment> GetAllPaths(WorkflowConfig config);
    }

    /// <summary>
    /// 入口：按拓扑序执行节点，串联 Skill 调用与输出传递
    /// 输入：workflow_config + 执行上下文   输出：最终结果 + 执行日志
    /// </summary>
    public interface IWorkflowInterpreter
    {
        Task<WorkflowRunResult> RunAsync(string config, ExecutionTask task, CancellationToken ct);
    }

    /// <summary>
    /// 入口：队列驱动（节点级串行），解耦"执行卡"与"结果获取"
    /// 对接：YzhQueueManager（已有）/ IYzhTaskExecutor（已有）
    /// </summary>
    public interface IQueueDrivenExecutor
    {
        /// <summary>将工作流任务入队（yzh_queue）</summary>
        Task<string> EnqueueAsync(ExecutionTask task);
        /// <summary>队列消费回调（HostedService 触发）</summary>
        Task<TaskExecuteResult> ExecuteTaskAsync(YzhQueueTask queueTask);
    }

    /// <summary>
    /// 入口：AI 节点专用执行器（封装 LLM 调用、提示词组装）
    /// </summary>
    public interface IAiNodeExecutor
    {
        Task<AiNodeResult> ExecuteAsync(AiNodeContext ctx);
    }

    /// <summary>
    /// 入口：节点输出持久化（供下游/AI 消费）
    /// </summary>
    public interface INodeOutputStore
    {
        Task SaveOutputAsync(string taskCode, string nodeId, string portName, object value);
        Task<T> GetOutputAsync<T>(string taskCode, string nodeId, string portName);
        Task<IDictionary<string, object>> GetAllOutputsAsync(string taskCode, string nodeId);
    }

    /// <summary>
    /// 入口：执行日志查询（按任务聚合）
    /// </summary>
    public interface IExecutionLogQuery
    {
        Task<IReadOnlyList<ExecutionLogEntry>> GetByTaskAsync(string taskCode);
        Task<IReadOnlyList<ExecutionLogEntry>> GetByNodeAsync(string taskCode, string nodeId);
        Task<ExecutionTrace> GetTraceAsync(string taskCode);
    }

    /// <summary>
    /// 入口：Hub 推送实时执行事件
    /// </summary>
    public interface IWorkflowNotifier
    {
        Task NotifyNodeStarted(string taskCode, string nodeId);
        Task NotifyNodeCompleted(string taskCode, string nodeId, bool success, object output);
        Task NotifyTaskCompleted(string taskCode, bool success, object result);
    }
}
```

---

## 二、核心数据结构

### 2.1 执行任务表 wf_execution_task

> **定位**：所有执行（测试/NC检查/报告生成）的上下文隔离单元。

```sql
CREATE TABLE wf_execution_task (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    code            VARCHAR(36) NOT NULL UNIQUE COMMENT '任务编码(GUID)',
    task_type       VARCHAR(20) NOT NULL COMMENT 'TEST | NC_CHECK | REPORT_GENERATE',
    task_status     VARCHAR(20) NOT NULL DEFAULT 'running' 
                    COMMENT 'running | completed | failed | cancelled',
    
    -- 执行上下文（JSON 快照，确保执行期间配置不变）
    config_snapshot JSON NOT NULL COMMENT '执行时的工作流配置快照',
    
    -- 关联业务（NC 规则 / 章节 / 企业）
    rule_code       VARCHAR(64) COMMENT 'cert_validation_rule.rule_code',
    section_code    VARCHAR(64) COMMENT 'rpt_report_section.section_code',
    enterprise_code VARCHAR(50) COMMENT 'cert_application.enterprise_code',
    phase_code      VARCHAR(30) COMMENT '审核阶段',
    
    -- 关联队列（驱动执行）
    queue_code      VARCHAR(64) COMMENT 'yzh_queue.queue_code',
    
    -- 结果摘要
    result_summary  JSON COMMENT '执行结果摘要(end 输出)',
    error_message   VARCHAR(2000) COMMENT '失败原因',
    
    -- 执行时间
    started_at      DATETIME NOT NULL,
    completed_at    DATETIME,
    duration_ms     INT COMMENT '执行耗时(ms)',
    token_usage     INT COMMENT 'AI token 消耗（如有）',
    
    -- 审计
    org_code        VARCHAR(50) COMMENT '机构编码',
    user_id         INT COMMENT '发起用户ID',
    user_name       VARCHAR(100) COMMENT '发起用户名',
    create_date     DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_type_status (task_type, task_status),
    INDEX idx_rule (rule_code),
    INDEX idx_enterprise (enterprise_code),
    INDEX idx_queue (queue_code),
    INDEX idx_org (org_code)
) ENGINE=InnoDB COMMENT='工作流执行任务（TEST/NC_CHECK/REPORT_GENERATE）';
```

### 2.2 节点输出表 wf_node_output

> **定位**：每个节点的输出落库，供下游节点 / AI 节点调试 / UI 回查消费。

```sql
CREATE TABLE wf_node_output (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    task_code       VARCHAR(36) NOT NULL COMMENT 'wf_execution_task.code',
    node_id         VARCHAR(64) NOT NULL COMMENT '节点ID（如 n1/n2/get_field_n1）',
    node_type       VARCHAR(30) NOT NULL COMMENT 'ai_node/skill/compare/…',
    skill_code      VARCHAR(64) COMMENT 'wf_skill.skill_code（功能节点）',
    
    port_name       VARCHAR(64) NOT NULL COMMENT '输出端口名',
    data_type       VARCHAR(20) NOT NULL COMMENT 'string/number/boolean/json/table',
    text_value      TEXT COMMENT '字符串值（小尺寸直接存）',
    json_value      JSON COMMENT '结构化值（大尺寸/表格式）',
    
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE uk_task_node_port (task_code, node_id, port_name),
    INDEX idx_task_node (task_code, node_id),
    INDEX idx_task (task_code)
) ENGINE=InnoDB COMMENT='节点输出（按执行任务隔离，支持下游引用）';
```

### 2.3 执行日志表 wf_execution_log（扩展现有）

> **现有**：字段已有 workflow_instance_id / biz_type / biz_code / status 等。
> **补充**：增加 task_code 关联 + 扩展字段。

```sql
ALTER TABLE wf_workflow_execution_log ADD COLUMN task_code VARCHAR(36) NULL 
    COMMENT 'wf_execution_task.code（新版关联键）';
ALTER TABLE wf_workflow_execution_log ADD COLUMN node_type VARCHAR(30) NULL 
    COMMENT '节点类型（ai/skill/logic/start/end）';
ALTER TABLE wf_workflow_execution_log ADD COLUMN tokens_used INT NULL 
    COMMENT 'AI token 消耗（AI 节点）';
CREATE INDEX idx_task_code ON wf_workflow_execution_log(task_code);
```

---

## 三、模块间数据流协议

### 3.1 一次完整执行的生命周期

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           REQ: POST /api/workflow/run                          │
│                      {config, taskType, context, triggerMode}                    │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                   [1] ExecutionTaskManager.CreateAsync()                         │
│                   · 创建 wf_execution_task 记录（status=running）                 │
│                   · 生成 taskCode / queueCode                                    │
│                   · 锁定 config_snapshot（防止配置变更）                          │
│                   · 返回 taskCode 给调用方                                       │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                    ┌───────────────────┴───────────────────┐
                    │                                       │
               同步触发                                   队列触发
                    │                                       │
                    ▼                                       ▼
┌─────────────────────────────────┐     ┌─────────────────────────────────────────┐
│ [2a] 同步：IWorkflowInterpreter │     │ [2b] 异步：IQueueDrivenExecutor          │
│        .RunAsync(config, task)  │     │         .EnqueueAsync(task)             │
│ · 同步执行（小图/测试场景）     │     │ · 生成队列（wf_queue + wf_queue_task）  │
│ · 直接返回结果                  │     │ · 返回 taskCode（前端轮询或 SignalR）   │
└─────────────────────────────────┘     └─────────────────────────────────────────┘
                    │                                       │
                    │                                       ▼
                    │                   ┌─────────────────────────────────────────┐
                    │                   │ [3b] YzhQueueHostedService               │
                    │                   │      触发 ExecuteTaskCallback           │
                    │                   │ · 获取队列中 WF_NODE_EXEC 任务           │
                    │                   │ · 调用 WFNodeExecutor.ExecuteAsync()   │
                    │                   │ · 逐节点串行回调 WorkflowInterpreter     │
                    │                   └─────────────────────────────────────────┘
                    │                                       │
                    └───────────────────┬───────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    [4] IWorkflowInterpreter.RunAsync()                          │
│                                                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │ 4.1 TopologyAnalyzer.TopoSort(config)                                    │   │
│  │     → 输出：nodeId[] 执行顺序                                             │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
│                                       │                                         │
│                                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │ 4.2 foreach(nodeId in order)                                            │   │
│  │     ├── NotifyNodeStarted(taskCode, nodeId)      // SignalR 推送         │   │
│  │     ├── NodeExecutor.ExecuteAsync(node, ctx)    // 实际执行节点          │   │
│  │     ├── SaveOutput(taskCode, nodeId, outputs)    // 落 wf_node_output    │   │
│  │     ├── WriteLog(taskCode, nodeId, result)       // 写 wf_execution_log  │   │
│  │     └── NotifyNodeCompleted(taskCode, nodeId)    // SignalR 推送         │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
│                                       │                                         │
│                                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────────────────────┐   │
│  │ 4.3 EndNode 汇聚输出                                                    │   │
│  │     → 更新 wf_execution_task.result_summary                              │   │
│  │     → 更新 wf_execution_task.task_status = completed/failed              │   │
│  │     └── NotifyTaskCompleted(taskCode, success)                          │   │
│  └──────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          RES: {taskCode, status, result}                        │
│                          + SignalR 实时事件（节点级推送）                        │
│                          + wf_node_output（供查询/调试/下游消费）                │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 节点类型与执行器映射

| nodeType | 执行器 | 输出端口 | 执行后副作用 |
|---|---|---|---|
| `start` | 引擎内置 | 声明的工作流输入 | 无（注入上下文） |
| `skill` | `SkillExecutor.ExecuteAsync()`（反射）| 按 wf_skill_output 定义 | 写 wf_node_output |
| `ai_node` | `AiNodeExecutor.ExecuteAsync()` | content / json / confidence | 写 wf_node_output + token 统计 |
| `logic` | 引擎内置（条件求值）| result(boolean) | 仅决定路由 |
| `loop` | v1: AI 提示词整包执行 | results(json) | 写 wf_node_output |
| `docField` / `docTable` | 引擎内置（查 B-08/B-09）| 字段值/表格数据 | 读数据后写 wf_node_output |
| `end` | 汇聚输出 | 无（终态写 result_summary）| 更新任务状态 |

---

## 四、队列驱动执行引擎（核心）

### 4.1 设计动机

| 问题 | 同步执行 | 队列驱动 |
|---|---|---|
| 超时控制 | HTTP 超时（60s），长工作流易卡死 | 队列任务租约，自然超时回收 |
| 失败重试 | 手动/全量 | 单节点级重试（Retryable 分类） |
| 并发控制 | 无 | 信号量（SemaphoreSlim）限制背压 |
| 中断恢复 | 不支持 | 已执行节点输出落库，后续续跑 |
| 进度感知 | 无 | SignalR 实时推送节点状态 |
| 日志聚合 | workflow_instance_id 虚关联 | taskCode 强关联 |

### 4.2 队列与引擎的协作模型

```
                   ┌──────────────────────────────────┐
                   │         yz h_queue 表            │
                   │  queue_code | queue_name | status │
                   │  WF-{taskCode} | NC检查 | running │
                   └────────────┬─────────────────────┘
                                │
          ┌─────────────────────┼─────────────────────┐
          │  task_type=WF_CREATE │  task_type=WF_NODE_EXEC  │  task_type=WF_END  │
          │  payload = null      │  payload = {nodeId,...}   │  payload = null      │
          └─────────────────────┼─────────────────────┘
                                │
          YzhQueueHostedService ──IYzhTaskExecutor.ExecuteTaskAsync()───┐
                                │                                       │
                    ┌───────────┴───────────────┐                      │
                    │  WFNodeExecutor           │                      │
                    │  (WorkflowNodeExecutor)   │                      │
                    │  · 解析 task_type         │                      │
                    │  · 调用 WorkflowEngine    │                      │
                    │  · 处理节点级回调          │                      │
                    └───────────────────────────┘                      │
                                                                       │
          ┌────────────────────────────────────────────────────────────┘
          │   WorkflowEngine.RunAsync(config, task, ct)
          │   → 返回 NodeExecutionResult（部分执行 / 需要继续）
          │   → 如有后续节点 → 重新入队 WF_NODE_EXEC
          ▼
```

### 4.3 节点级入队策略

```csharp
// IYzhTaskExecutor 实现
public class WorkflowNodeExecutor : IYzhTaskExecutor
{
    public string TaskType => "WF_NODE_EXEC";

    public async Task<TaskExecuteResult> ExecuteTaskAsync(YzhQueueTask queueTask)
    {
        var payload = JsonSerializer.Deserialize<NodeExecPayload>(queueTask.Payload);
        var task = await taskMgr.GetByCodeAsync(payload.TaskCode);
        
        // 查找待执行节点（queueTask.QueueCode 携带了 nodeId 上下文）
        var targetNode = task.Config.FindNode(payload.NodeId);
        
        // 获取该节点输入（从 wf_node_output 读）
        var inputs = await outputStore.ResolveInputsAsync(
            task.Code, 
            targetNode.Inputs, 
            task.WorkflowInputs);
        
        // 执行单节点
        var result = await skillExecutor.ExecuteSkill(
            targetNode.SkillCode,
            new SkillContext(inputs, task.Code, payload.NodeId));
        
        // 落输出 + 写日志
        await outputStore.SaveOutputsAsync(task.Code, payload.NodeId, result.Outputs);
        await logWriter.WriteAsync(task.Code, payload.NodeId, result);
        
        // 推送事件
        await notifier.NotifyNodeCompleted(task.Code, payload.NodeId, result.Success, result.Outputs);
        
        // 判断下一个节点（按拓扑 + 已执行节点）
        var nextNode = topology.GetNextExecutable(task.Config, payload.NodeId);
        
        if (nextNode == null)
        {
            // 已是最后一个节点 → 触发汇聚
            return new TaskExecuteResult { IsCompleted = true, NextTaskType = "WF_END" };
        }
        
        // 返回下一个节点任务入队信息
        return new TaskExecuteResult 
        { 
            IsCompleted = false,
            NextTaskType = "WF_NODE_EXEC",
            NextPayload = JsonSerializer.Serialize(new NodeExecPayload 
            { 
                TaskCode = task.Code, 
                NodeId = nextNode.NodeId 
            })
        };
    }
}
```

### 4.4 队列与现有 YzhQueueManager 对接

**现有能力复用**（`YzhQueueManager` / `YzhQueueTask`）：

| 现有机制 | 引擎使用方式 |
|---|---|
| 资源锁（resource_lock） | wf_execution_task 行级锁 + 并发队列隔离 |
| Semaphore 并发控制 | 限制同时执行的 NC 检查/报告数 |
| 租约 + 超时回收 | 长时间未完成的节点任务自动释放 |
| 指数退避重试 | AI 节点调用失败重试（3 次） |
| 取消 / 整队重跑 | 用户取消工作流执行 |

**引擎新增任务类型**：

```csharp
// 在 YzhQueueOptions 中注册
public static class WorkflowTaskTypes
{
    public const string WF_CREATE     = "wf_create";     // 工作流执行（入队触发）
    public const string WF_NODE_EXEC  = "wf_node_exec";  // 单节点执行
    public const string WF_END        = "wf_end";        // 执行完成（汇聚）
    public const string WF_NODE_TEST  = "wf_node_test";  // 单节点测试（独立上下文）
}
```

---

## 五、测试与正式执行的协议差异

### 5.1 差异对照表

| 维度 | TEST | NC_CHECK | REPORT_GENERATE |
|---|---|---|---|
| **触发场景** | 配置期画布「试运行」 | 审核员触发 NC 检查 | 审核员触发报告生成 |
| **task_type** | TEST | NC_CHECK | REPORT_GENERATE |
| **config 来源** | 画布当前编辑态 JSON | wf_check_rule.rule_config 落库快照 | rpt_report_section.workflow_config 落库快照 |
| **执行上下文** | enterprise_code = "YZH-STD-ENT"（验证企业）| 真实 enterprise_code | 真实 enterprise_code |
| **数据读写** | 只读（查询 B-08/B-09）| 只读 + 写 NC 结果 | 只读 + 写 section.content |
| **输出用途** | 前端展示 + 不持久化 | 写 audit_finding / 更新 NC 状态 | 更新 section.content |

### 5.2 测试期前端行为

```javascript
// 前端 POST /api/workflow/run（测试场景）
const req = {
  taskType: "TEST",
  triggerMode: "sync",         // 测试用小图同步执行（大图走 queue）
  context: {
    enterpriseCode: "YZH-STD-ENT",  // 验证数据
    phaseCode: "stage_1",
  },
  config: canvas.exportConfig()   // 当前画布编辑态
};

// 响应：直接返回结果 + 执行日志
const res = await http.post("/api/workflow/run", req);
res.data.nodeOutputs;    // 每节点真实输入/输出
res.data.logs;           // 节点级日志
res.data.trace;          // TRACE 序列（供调试面板展示）
```

### 5.3 执行上下文 EnvironmentContext

```csharp
/// <summary>
/// 执行上下文的"环境"部分——区分 TEST / NC_CHECK / REPORT_GENERATE 的数据边界
/// </summary>
public class EnvironmentContext
{
    /// <summary>TEST 模式：固定 YZH-STD-ENT</summary>
    public bool IsTestMode { get; set; }
    
    /// <summary>真实企业编码（NC_CHECK / REPORT_GENERATE）</summary>
    public string EnterpriseCode { get; set; }
    
    /// <summary>验证数据注入（TEST+NC_CHECK：从 B-08/B-09 查）</summary>
    public string PhaseCode { get; set; }
    
    /// <summary>产出物写入目标（NC_CHECK：NC 状态更新 / REPORT：section.content）</summary>
    public OutputTarget OutputTarget { get; set; }
}

public enum OutputTarget
{
    None,           // TEST：不写终态
    AuditFinding,   // NC_CHECK：写不符合项
    SectionContent  // REPORT_GENERATE：写章节内容
}
```

---

## 六、前端执行面板协议

### 6.1 SignalR Hub 事件列表

```csharp
// WorkflowHub.cs
public class WorkflowHub : Hub
{
    // 服务端推送（前端订阅）
    // 1. 任务创建
    public async Task TaskStarted(string taskCode, string taskType) { }
    
    // 2. 节点开始执行
    public async Task NodeStarted(string taskCode, string nodeId, string nodeTitle) { }
    
    // 3. 节点执行完成
    public async Task NodeCompleted(string taskCode, string nodeId, bool success, object output) { }
    
    // 4. 节点执行失败（可重试）
    public async Task NodeFailed(string taskCode, string nodeId, string error, bool retryable) { }
    
    // 5. 任务完成（全部成功）
    public async Task TaskCompleted(string taskCode, object result) { }
    
    // 6. 任务取消
    public async Task TaskCancelled(string taskCode, string reason) { }
    
    // 前端请求（服务端响应）
    // 1. 连接时注册监听
    public async Task JoinTask(string taskCode) { }
    
    // 2. 退出监听
    public async Task LeaveTask(string taskCode) { }
    
    // 3. 取消任务
    public async Task CancelTask(string taskCode) { }
    
    // 4. 请求 TRACE（调试面板）
    public async Task<ExecutionTrace> GetTrace(string taskCode) { }
}
```

### 6.2 执行面板 UI 结构（参考扣子）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  底部执行面板（WorkflowExecutionPanel）                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌ 任务信息区 ──────────────────────────────────────────────────────────┐  │
│  │  任务类型：NC CHECK    状态：执行中    耗时：12.3s    节点：3/8      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│  ┌ 节点执行时间线（Mermaid）────────────────────────────────────────────┐  │
│  │  graph TD                                                            │  │
│  │    start[开始] --> n1[获取营业执照]                                  │  │
│  │    n1 --> n2[AI 判断合规]                                            │  │
│  │    n2 -->|满足| n3[组装结论]                                         │  │
│  │    n2 -->|不满足| end1[终止：不合规]                                 │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│  ┌ 节点详情列表 ────────────────────────────────────────────────────────┐  │
│  │  ✅ n1 获取营业执照    耗时：0.3s    输出：{...}    [查看]           │  │
│  │  ✅ n2 AI 判断合规     耗时：4.2s    Token: 892     [查看]           │  │
│  │  🔄 n3 组装结论        执行中...                                     │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│  ┌ 操作区 ──────────────────────────────────────────────────────────────┐  │
│  │  [取消执行] [重试失败节点] [导出TRACE]                               │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 七、模块完成度对照表

> **定位**：代码现状 vs 设计目标，明确后续开发优先级。

| # | 模块 | 现有代码 | 缺口 | 优先级 |
|---|---|---|---|---|
| 1 | **任务管理器** | 无 | ExecutionTaskManager 接口 + wf_execution_task 表 + CreateAsync/CancelAsync | P0 |
| 2 | **DAG 解释器** | WorkflowEngine.RunAsync（有拓扑排序、节点执行、分支匹配）| wf_node_output 落库、wf_execution_task 关联 | P0 |
| 3 | **节点执行器** | SkillExecutor（反射）+ CompareSkill 等 | 标准输出端口校验、AI 节点独立执行器 | P0 |
| 4 | **上下文管理器** | WorkflowContext 基础字段 | EnvironmentContext（TEST/正式区分） | P0 |
| 5 | **队列集成器** | **YzhQueueManager 完整**（资源锁/并发/超时/重试）| WorkflowNodeExecutor（IYzhTaskExecutor）+ 节点级回调 | P0 |
| 6 | **节点输出存储** | 无 | wf_node_output 表 + INodeOutputStore 接口 + 下游解析逻辑 | P0 |
| 7 | **日志写入器** | ExecutionLogEntry 基础写入 | taskCode 关联 + token 统计 | P1 |
| 8 | **日志查询器** | 无 | IExecutionLogQuery 接口 + 按任务聚合 + TRACE 序列生成 | P1 |
| 9 | **AI 执行器** | 无 | PromptAssembler + AiNodeExecutor（LLM 调用封装） | P1 |
| 10 | **SignalR Hub** | 无 | WorkflowHub + 节点状态推送 | P1 |
| 11 | **前端执行面板** | 无 | useWorkflowExecution composable + ExecutionPanel.vue | P1 |
| 12 | **测试控制器** | 无 | /api/workflow/run（同步）+ /api/workflow/enqueue（异步）| P0 |

---

## 八、引擎内部 API 接口清单

### 后端 API（Controller 层）

| 接口 | 职责 | 对应模块 |
|---|---|---|
| `POST /api/workflow/run` | 同步执行（测试用小图）| WorkflowController |
| `POST /api/workflow/enqueue` | 异步入队（大图/NC/报）| WorkflowController |
| `GET /api/workflow/task/{code}` | 查询任务状态 | WorkflowTaskController |
| `POST /api/workflow/task/{code}/cancel` | 取消任务 | WorkflowTaskController |
| `GET /api/workflow/task/{code}/trace` | 获取 TRACE（调试用）| WorkflowTaskController |
| `GET /api/workflow/node/{code}/{nodeId}/output` | 查询节点输出 | WorkflowNodeController |
| `POST /api/workflow/test/node` | 单节点独立测试 | WorkflowTestController |
| `POST /api/workflow/test/node/retry` | 单节点重试 | WorkflowTestController |

### 前端 Composable / 组件

| 模块 | 文件 | 职责 |
|---|---|---|
| useWorkflowExecutor | composables/useWorkflowExecutor.js | 任务创建/取消/轮询/SignalR 订阅 |
| useWorkflowExecution | composables/useWorkflowExecution.js | 单任务执行状态管理 |
| ExecutionPanel.vue | components/workflow/ExecutionPanel.vue | 底部执行面板容器 |
| TimelineMermaid.vue | components/workflow/TimelineMermaid.vue | Mermaid 时间线 |
| NodeOutputViewer.vue | components/workflow/NodeOutputViewer.vue | 节点输入/输出查看器 |

---

## 九、分阶段开发路线图（引擎内部）

### Phase 0：底座（P0，阻塞后续）

| # | 任务 | 依赖 | 验收标准 |
|---|---|---|---|
| 0-1 | 创建 wf_execution_task 表 + wf_node_output 表 | 无 | SQL 幂等脚本，Index 正确 |
| 0-2 | ExecutionTaskManager.CreateAsync | 0-1 | 单元测试通过 |
| 0-3 | 现有 WorkflowEngine 关联 taskCode（写入日志 + 落输出）| 0-1, 0-2 | 日志与 wf_node_output 可查询 |
| 0-4 | WorkflowNodeExecutor（IYzhTaskExecutor 实现）| 0-3 | 队列任务可驱动节点串行 |
| 0-5 | POST /api/workflow/run（同步入口）| 0-2, 0-3 | Swagger 能跑通 |
| 0-6 | POST /api/workflow/enqueue（异步入口）| 0-2, 0-4 | 返回 taskCode，队列可消费 |

### Phase 1：可观测可调试（P1）

| # | 任务 | 依赖 | 验收标准 |
|---|---|---|---|
| 1-1 | ExecutionLogQuery（按 taskCode 聚合）| Phase 0 | /api/workflow/task/{code}/trace 返回 |
| 1-2 | TRACE 序列生成 | 1-1 | 前端可展示节点输入/输出/token |
| 1-3 | WorkflowHub（SignalR）| Phase 0 | 节点状态推送可订阅 |
| 1-4 | useWorkflowExecutor composable | Phase 0, 1-3 | 前端可创建/监听任务 |
| 1-5 | ExecutionPanel.vue 基础版 | 1-3, 1-4 | 底部面板可展示执行进度 |

### Phase 2：AI 节点 + 单步调试（P1-P2）

| # | 任务 | 依赖 | 验收标准 |
|---|---|---|---|
| 2-1 | AiNodeEngine（PromptAssembler + LLM 调用）| Phase 1 | ai_node 可执行 |
| 2-2 | token 统计写 wf_node_output | 2-1 | TRACE 含 token 信息 |
| 2-3 | 断点执行（stopAt nodeId）| Phase 0 | 可暂停在指定节点 |
| 2-4 | 参数调整重跑 | 2-3 | 暂停后改参数可续跑 |

### Phase 3：测试独立环境（P2）

| # | 任务 | 依赖 | 验收标准 |
|---|---|---|---|
| 3-1 | EnvironmentContext（TEST 标识注入）| Phase 1 | TEST 模式自动用 YZH-STD-ENT |
| 3-2 | 单节点独立测试接口 | 3-1 | 不连线节点可单独跑 |
| 3-3 | TEST 结果不写终态 | 3-1 | TEST 不修改任何业务状态 |

---

## 十、待决策事项

| # | 决策 | 候选 | 建议 | 备注 |
|---|---|---|---|---|
| 1 | 超大队列工作流执行策略 | A. 节点级重入队 B. 长任务单队列 | A | 回收租约保护 |
| 2 | AI 节点重试策略 | A. 队列级重试 B. Skill 内部重试 | A | 走队列 Retryable |
| 3 | wf_node_output 存储格式 | A. 列存（每端口一行）B. JSON 整体 | A | 查询灵活（当前设计）|
| 4 | 前端面板技术选型 | A. Mermaid 静态渲染 B. 自研 Timeline | A | 静态图易实现 |
| 5 | 测试模式 token 消耗 | A. YZH 账号 B. 独立预算 | B | 防止测试刷爆配额 |

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|---|---|---|
| V1.0 | 2026-08-21 | 初始版本，承接讨论稿核心设计，建立引擎内部架构设计规范 |

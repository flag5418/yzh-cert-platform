# 开发流程

> **版本**：V1.0 | **日期**：2026-09-10 | **状态**：编写中
>
> 本文档提供新页面开发的完整步骤和常见场景解决方案。
> 编码前必读，确保不遗漏任何步骤。

---

## 一、新增单表页面（3步）

### Step 1：创建 EntityConfig JSON

```
src/yzh-core/YZH.Core.Web/Assets/EntityConfigs/{Domain}/{Type}.json
```

**示例：`Foundation/ISOClause.json`**

```json
{
  "Title": "ISO条款管理",
  "FillMode": "AutoFix",
  "Columns": [
    { "FieldName": "ClauseCode", "DesName": "条款编码", "Type": "TextBox", "Sxh": 1, "XsFlag": true, "BcFlag": true, "Yxk": false, "Width": 150, "GroupIndex": "0" },
    { "FieldName": "ClauseName", "DesName": "条款名称", "Type": "TextBox", "Sxh": 2, "XsFlag": true, "BcFlag": true, "Yxk": false, "Width": 200, "GroupIndex": "0" },
    { "FieldName": "StandardCode", "DesName": "所属标准", "Type": "TextBox", "Sxh": 3, "XsFlag": true, "BcFlag": true, "Yxk": false, "Width": 150, "GroupIndex": "0" },
    { "FieldName": "Enable", "DesName": "状态", "Type": "Switch", "Sxh": 4, "XsFlag": true, "BcFlag": true, "Yxk": false, "Mrz": 1, "Width": 80, "GroupIndex": "0" },
    { "FieldName": "CreateTime", "DesName": "创建时间", "Type": "Other", "Sxh": 5, "XsFlag": true, "BcFlag": false, "Yxk": true, "Width": 180, "GroupIndex": "99" }
  ]
}
```

**GroupIndex 使用示例（用户管理）**：

```json
{
  "Title": "用户管理",
  "Columns": [
    { "FieldName": "UserName", "DesName": "用户名", "Type": "TextBox", "GroupIndex": "0", "BcFlag": true, "Yxk": true },
    { "FieldName": "UserPwd", "DesName": "密码", "Type": "PasswordBox", "GroupIndex": "0", "BcFlag": true, "Yxk": true },
    { "FieldName": "RoleId", "DesName": "角色", "Type": "ComboBox", "GroupIndex": "0", "BcFlag": true, "Yxk": true },
    { "FieldName": "RoleName", "DesName": "角色名称", "Type": "Other", "GroupIndex": "1", "BcFlag": true, "Yxk": false },
    { "FieldName": "OrgCode", "DesName": "机构", "Type": "TreeSelect", "GroupIndex": "0", "BcFlag": true, "Yxk": true },
    { "FieldName": "OrgName", "DesName": "机构名称", "Type": "Other", "GroupIndex": "1", "BcFlag": true, "Yxk": false },
    { "FieldName": "CreateTime", "DesName": "创建时间", "Type": "Other", "GroupIndex": "99", "BcFlag": false, "Yxk": false }
  ]
}
```

> **说明**：
> - `GroupIndex=0`：新增和编辑时都可编辑
> - `GroupIndex=1`：新增时可编辑，编辑时只读（如角色名称、机构名称等视图字段）
> - `GroupIndex=99`：始终只读（如创建时间、更新时间等审计字段）

### Step 2：创建后端 Controller

```
src/certplatform-api/CertPlatform.Admin/Controllers/{Domain}/{Type}Controller.cs
```

```csharp
[ApiController]
[Route("api/{Domain}/[controller]")]
public class ISOClauseController : YzhControllerBase<ISOClause>
{
    public ISOClauseController(EntityService<ISOClause> entityService, IUserContext userContext)
        : base(entityService, userContext) { }

    // 如需自定义逻辑，覆盖钩子：
    // protected override async Task<(bool ok, string? msg)> OnBeforeAdd(ISOClause entity) { ... }
    // protected override void OnQueried(PagedResult<ISOClause> result) { ... }
}
```

### Step 3：创建前端页面

```
src/certplatform-web/admin/src/pages/{domain}/{type}/index.vue
src/certplatform-web/admin/src/pages/{domain}/{type}/logic.ts  （可选）
src/certplatform-web/share/src/api/{api-domain}/{type}.ts
```

**logic.ts（可选，简单页面可直接写在 .vue 中）**：

```typescript
import { CrudPageLogic } from '@share/logic'
import type { ISOClause } from '@share/api/cert/iso-clause'

export class ISOClauseLogic extends CrudPageLogic<ISOClause> {
    controllerName = 'ISOClause'
}
```

**index.vue**：

```vue
<script setup lang="ts">
import { YzhTable } from '@yzh-core/components/table'
import { YzhForm } from '@yzh-core/components/form'
import { onMounted, ref } from 'vue'
import { ISOClauseLogic } from './logic'

const logic = new ISOClauseLogic()
const tableRef = ref()

onMounted(async () => { await logic.init() })
</script>

<template>
  <YzhTable
    :columns="logic.columns"
    :data-loader="logic.loadPage.bind(logic)"
    :search-fields="logic.searchFields"
    :selectable="true"
    @selection-change="logic.selectedRows = $event"
  >
    <template #toolbar-left>
      <el-button type="primary" @click="logic.openAddDialog()">新增</el-button>
    </template>
    <template #column-actions="{ row }">
      <el-button text type="primary" @click="logic.openEditDialog(row)">编辑</el-button>
      <el-button text type="danger" @click="logic.confirmDelete([row])">删除</el-button>
    </template>
  </YzhTable>

  <el-dialog v-model="logic.dialogVisible" :title="logic.dialogMode === 'add' ? '新增' : '编辑'">
    <YzhForm
      v-model="logic.formData"
      :fields="logic.formFields"
      :loading="logic.submitting"
      @submit="logic.submitForm"
      @reset="logic.dialogVisible = false"
    />
  </el-dialog>
</template>
```

**API 文件**：

```typescript
// share/src/api/cert/iso-clause.ts
import { yzhApi } from '@yzh-core/api/client'
import type { ApiResponse, PagedData } from '@share/types/contracts'

export interface ISOClause {
    Code: string
    ClauseCode: string
    ClauseName: string
    StandardCode: string
    Enable: number
    CreateTime: string
}

export function getISOClausePage(params: any) {
    return yzhApi.post<ApiResponse<PagedData<ISOClause>>>('/api/Foundation/ISOClause/filter', params)
}
```

**注册路由**：

```typescript
// admin/src/router/index.ts
{ path: 'foundation/iso-clause', component: () => import('@/pages/foundation/iso-clause/index.vue'), name: 'FoundationISOClause' }
```

---

## 二、新增左树右表页面（4步）

### Step 1：创建 EntityConfig JSON（表格 + 树节点表单）

```
src/yzh-core/YZH.Core.Web/Assets/EntityConfigs/{Domain}/{TableType}.json
src/yzh-core/YZH.Core.Web/Assets/EntityConfigs/{Domain}/{TreeType}Form.json
```

### Step 2：创建后端 Controller

```
src/certplatform-api/CertPlatform.Admin/Controllers/{Domain}/{Type}Controller.cs
```

```csharp
[ApiController]
[Route("api/{Domain}/[controller]")]
public class StandardDirectoryController : TreeTableControllerBase<Std_Directory, Std_Directory>
{
    public StandardDirectoryController(
        EntityService<Std_Directory> treeEntityService,
        EntityService<Std_Directory> tableEntityService,
        IUserContext userContext)
        : base(treeEntityService, tableEntityService, userContext)
    {
        // ★ 必须配置
        TreeConfig.NameField = "DirectoryName";
        TreeConfig.CodeField = "Code";
        TreeConfig.ParentCodeField = "ParentCode";
        TreeConfig.RelateField = "ParentCode";
        TreeConfig.MaxLevel = 10;
        TreeFormConfigName = "Foundation/StandardDirectoryForm";

        // 注册自定义操作
        RegisterTreeAction("disable", DisableNodeAsync);
    }
}
```

### Step 3：创建前端 Logic

```
admin/src/pages/{domain}/{type}/logic.ts
```

```typescript
import { TreeTableLogic } from '@share/logic'
import type { StdDirectory } from '@share/api/cert/directory'

export class StandardDirectoryLogic extends TreeTableLogic<StdDirectory> {
    controllerName = 'StandardDirectory'
}
```

### Step 4：创建前端页面

```
admin/src/pages/{domain}/{type}/index.vue
```

```vue
<script setup lang="ts">
import { YzhTreeTable } from '@yzh-core/components/layout'
import { YzhTable } from '@yzh-core/components/table'
import { onMounted } from 'vue'
import { StandardDirectoryLogic } from './logic'

const logic = new StandardDirectoryLogic()

onMounted(async () => { await logic.init() })
</script>

<template>
  <YzhTreeTable
    :tree-data="logic.treeData"
    :tree-lazy="true"
    :tree-load-data="logic.loadChildren.bind(logic)"
    @tree-node-click="logic.onNodeClick"
  >
    <template #default>
      <YzhTable
        :columns="logic.columns"
        :data-loader="logic.loadPage.bind(logic)"
        :search-fields="logic.searchFields"
      />
    </template>
  </YzhTreeTable>
</template>
```

---

### Step 1.5：创建实体类（如需视图结构）

```csharp
// src/certplatform-api/CertPlatform.Shared/Models/Foundation/ISOClause.cs
using YZH.Core.Stand.Annotations;
using YZH.Core.Stand.Models;

[ViewName("v_iso_clause")]  // 可选：指定查询视图
public class ISOClause : BaseEntity
{
    // 物理表字段
    public string ClauseCode { get; set; } = "";
    public string ClauseName { get; set; } = "";
    public string StandardCode { get; set; } = "";
    public int Enable { get; set; } = 1;
    
    // 视图字段（不持久化）
    [NotMapped]
    public string? StandardName { get; set; }
    [NotMapped]
    public string? EnableDesc { get; set; }
}
```

> **视图字段规则**：
> - `[NotMapped]` 标记的字段不写入数据库
> - EntityConfig 中配置 `Type=Other` 的字段应对应 `[NotMapped]` 属性
> - 视图字段值在 `OnQueried` 钩子中填充

---

## 三、生命周期钩子速查表

| 场景 | 覆盖的钩子 | 典型代码 |
|------|-----------|---------|
| 新增前校验唯一性 | `OnBeforeAdd` | `if (await Entity.ExistsByCodeAsync(entity.Code)) return (false, "已存在")` |
| 新增前填充默认值 | `OnBeforeAdd` | `entity.Enable = 1; entity.CreateBy = UserContext.UserCode` |
| 修改前校验状态 | `OnBeforeUpdate` | `if (entity.Status != "Draft") return (false, "只能修改草稿")` |
| 删除前检查关联 | `OnBeforeDelete` | `var count = await Entity.CountAsync(x => x.ParentCode == code)` |
| 查询后翻译字典 | `OnQueried` | `item.RoleName = item.RoleId == 1 ? "超级管理员" : "未知"` |
| 查询后脱敏 | `OnQueried` | `item.PhoneNo = item.PhoneNo[..3] + "****" + item.PhoneNo[^4..]` |
| 注入全局过滤 | `OnBuildingFilter` | `filters.Add(new FilterItem { Field = "Enable", Operator = "eq", Value = "1" })` |
| 新增后写日志 | `OnAfterAdd` | `_auditLogger.Info("新增用户", ...)` |
| 新增后发送通知 | `OnAfterAdd` | `_notificationService.SendAsync(entity)` |
| 写入后清理缓存 | `OnAfterCommitted` | `_cache.Remove($"user_list")` |

---

## 四、常见开发场景

### 4.1 添加启用/禁用开关（IsValid + toggle-valid）

**方式一：使用内置 toggle-valid 端点（推荐）**

基类已提供 `/toggle-valid` 和 `/tree/toggle-valid` 端点，无需额外后端代码：

```vue
<template #column-actions="{ row }">
  <el-button v-if="row.IsValid === 1" text type="warning"
    @click="logic.toggleIsValid(row.Code)">禁用</el-button>
  <el-button v-else text type="success"
    @click="logic.toggleIsValid(row.Code)">启用</el-button>
</template>
```

**方式二：自定义行操作（需要额外逻辑时）**

```csharp
// 注册行操作
RegisterRowAction("disable", DisableAsync);
RegisterRowAction("enable", EnableAsync);

// 实现
private async Task<Result<ApiResponse<object?>>> DisableAsync(V entity)
{
    var result = await Entity.GetByCodeAny(entity.Code);
    if (!result.Success || result.Data == null)
        return Result<ApiResponse<object?>>.Fail("记录不存在");
    result.Data.IsValid = 0;
    await Entity.Update(result.Data, UserContext.ClientIp);
    return Result<ApiResponse<object?>>.Ok(ApiResponse<object?>.Ok("已禁用"));
}
```

### 4.2 ShowDisabled 开关（显示已禁用记录）

**后端**（覆盖 `OnBuildingFilter`）：

```csharp
protected override List<FilterItem> OnBuildingFilter(List<FilterItem> filters)
{
    var showDisabled = false;
    var sdFilter = filters.FirstOrDefault(f => f.Field == "ShowDisabled");
    if (sdFilter != null && bool.TryParse(sdFilter.Value?.ToString(), out var sd))
    {
        showDisabled = sd;
        filters.Remove(sdFilter);
    }
    if (!showDisabled)
    {
        // IsValid 过滤已在 SqlSugarDbOrm 层自动处理
        // 此处仅处理 ShowDisabled 开关
    }
    return filters;
}
```

**前端**（在搜索栏添加 Switch）：

```typescript
// 在 searchFields 或 toolbar-right 中添加 Switch
// 切换时：
this.searchParams.ShowDisabled = !this.searchParams.ShowDisabled
this.pagination.page = 1
this.loadPage()
```

### 4.3 树节点递归禁用

```csharp
// OrganizationController 中的典型实现
private async Task<object?> DisableOrgRecursiveAsync(Sys_Organization entity)
{
    var result = await TreeEntity.GetByCodeAny(entity.Code);
    if (!result.Success || result.Data == null) return "机构不存在";

    var org = result.Data;
    org.IsValid = 0;
    await TreeEntity.Update(org, UserContext.ClientIp);

    // 递归禁用子机构
    var childCount = await DisableChildrenRecursive(org.Code);
    return $"已禁用机构及其 {childCount} 个子机构/用户";
}

private async Task<int> DisableChildrenRecursive(string parentCode)
{
    int count = 0;
    var children = await TreeEntity.GetChildren(parentCode);
    foreach (var child in children)
    {
        child.IsValid = 0;
        await TreeEntity.Update(child, UserContext.ClientIp);
        count++;
        count += await DisableChildrenRecursive(child.Code);

        // 禁用该子机构下的所有用户
        var users = await Entity.GetListAsync(u => u.OrgCode == child.Code);
        if (users.Success && users.Data != null)
        {
            foreach (var user in users.Data)
            {
                if (user.IsValid == 1)
                {
                    user.IsValid = 0;
                    await Entity.Update(user, UserContext.ClientIp);
                    count++;
                }
            }
        }
    }
    return count;
}
```

### 4.4 字典翻译（OnQueried）

```csharp
protected override void OnQueried(PagedResult<Sys_User> result)
{
    foreach (var item in result.Items)
    {
        // 角色名称
        item.RoleName = item.RoleId switch
        {
            1 => "超级管理员",
            10 => "总管理员",
            20 => "审核管理员",
            21 => "审核组长",
            22 => "普通审核员",
            30 => "企业账号",
            _ => "未知"
        };

        // 性别
        item.GenderDesc = item.Gender switch { 1 => "男", 2 => "女", _ => "未知" };

        // 手机号脱敏
        if (!string.IsNullOrEmpty(item.PhoneNo) && item.PhoneNo.Length >= 7)
            item.PhoneNo = $"{item.PhoneNo[..3]}****{item.PhoneNo[^4..]}";
    }
}
```

---

## 五、开发效率对比

| 场景 | 改造前（Vol 框架） | 改造后（YZH 架构） |
|------|------------------|------------------|
| 新增单表页面 | ~300 行代码（5 文件） | **~30 行**（实体 + JSON + 5 行 Controller + 10 行 Vue） |
| 新增左树右表页面 | ~500 行代码（8 文件） | **~50 行**（实体 + JSON + TreeConfig + 10 行 Controller） |
| UI 变更 | 改 Vue 模板 | **改 JSON 配置**，无需改代码 |
| Bug 修复 | 逐个页面修 | **修基类，所有页面生效** |
| API 规范 | 新旧混用 | **统一 11 个接口** |

---

## 六、常见错误速查

| 错误现象 | 根因 | 修复 |
|---------|------|------|
| 表格列为空 | 未调 `logic.init()` 或 JSON 中 `XsFlag=false` | 检查 init() 调用、检查 JSON 配置 |
| 表单字段不显示 | JSON 中 `BcFlag=false` | 改为 `BcFlag=true` |
| `/filter` 报 400 | 实体无 `Code` 属性 | 确保实体继承 `BaseEntity` |
| 树节点点击后表格不刷新 | 未调用 `logic.onNodeClick(node)` | 在 `@tree-node-click` 中调用 |
| 删除后表格数据重复 | 未使用 Split 方法 | 改用 `removeRowByCode(code)` |
| 新增后表格无新行 | 未使用 Split 方法 | 改用 `insertRow(row)` |
| 前端 TypeScript 报类型错误 | 使用了 camelCase 访问 PascalCase 字段 | 统一使用 PascalCase，不做转换 |
| 树节点表单不显示 | 未设置 `TreeFormConfigName` | 在构造函数中设置 `TreeFormConfigName = "Domain/Name"` |

---

## 七、完整检查清单

新增页面时逐项检查：

- [ ] 后端实体继承 `BaseEntity`
- [ ] 后端 Controller 继承 `YzhControllerBase<V>` 或 `TreeTableControllerBase<T,V>`
- [ ] EntityConfig JSON 已创建，`XsFlag`/`BcFlag` 配置正确
- [ ] **视图字段**：`[NotMapped]` 属性与 `Type=Other` 字段对应
- [ ] **Other 字段保存控制**：`Type=Other` 字段不应有 `BCFlag=true`（除非是只读展示）
- [ ] 后端路由 `[Route("api/{Domain}/[controller]")]` 正确
- [ ] 前端 API 文件路径与后端路由一致
- [ ] 前端 Pages 文件夹路径与后端 Controller 域一致
- [ ] 路由已注册到 `router/index.ts`
- [ ] 前端 Logic 继承 `CrudPageLogic` 或 `TreeTableLogic`
- [ ] `controllerName` 已设置
- [ ] `logic.init()` 已在 `onMounted` 中调用
- [ ] 树形页面已设置 `TreeConfig` 所有必填字段
- [ ] 自定义操作已注册（`RegisterRowAction` / `RegisterTreeAction`）

---
status: plan
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9a16bac6e27d25132787d930f50d9879_dc9ece8c992711f1a98a525400f8a581
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9a16bac6e27d25132787d930f50d9879_dc9ece8c992711f1a98a525400f8a581
    ReservedCode2: 6cXy5QC9kBwQ1Qa+9ECvLPfdiIX8UwkfKyqJRKCnru6xKPdmfw8PffDC9CnpxAXPSbNo0KQCFs7RTgrkbRIKJiksv3B+djqisOeezj5I3KRPULmc+fW21aIOKU1cobejxVclFy75NribYDx7nqbMwBK2K1DsTDCGAwuzyZi/Omy9daiSjCnkeJ7O6U8=
---

# 前后端信封统一改造计划 V1

> **语义权威是 `22-接口返回规范-V1.md`，本文只管「怎么改、改到哪、怎么验收」。**
> 语义与本文冲突 → 先改本文；本文与 22 冲突 → 先改 22。
>
> **状态**：`plan` → 实施完成后改 `done` 并填「实际执行记录」章节。
> **批准**：2026-09-25 用户批准 6 项决策（见 §二）。
> **铁律**：**后端 P0 不可先于前端 F0 上线**（见 §一 结论②）。

---

## 一、三个决定一切的结论

### 1.1 结论①：现状是「静默失败」，P0 单独上线会退化成「假成功」

| 阶段 | 业务失败时（以 `submitForm` 为例） |
|---|---|
| **现状**（后端 HTTP 400） | `client.ts` throw → `SingleTableCore:736` **无 catch** → 弹窗不关、**零提示**、`submitting` 复位 = **静默失败** |
| **只做后端 P0**（200 + `success:false`） | 不 throw → `add()` 返回 `undefined` → `:743 onAfterAdd` 执行 → `insertRow(undefined)` → **`:757 ElMessage.success('保存成功')` + 关弹窗 = 假成功** |
| **F0 先行**（后端未改） | throw 进 `submitForm` 新增的 catch → **真提示 + 弹窗保持打开** → 立即改善 |
| **F0 + P0 同批** | `expectOk` 拦下 → 同上（真提示、不关窗） |

### 1.2 结论②：上线顺序约束（不可违背）

```
F0 ──可独立先上线──▶ 立即收益：静默失败 → 真实报错
P0 ──必须在 F0 之后或同批──▶ 否则：静默失败 → 假成功（更糟）
```

**本次执行顺序**：`文档 → F0 → P0 → P1 → P2 → F1 → F2 → P3/F3 → 全面验证`。
F0 与 P0 **分两个逻辑批次**，但**在同一次验收内完成**（避免中间态合入主干）。

### 1.3 结论③：最严重的 3 处「无条件成功」

| # | 位置 | 后果 |
|---|---|---|
| 1 | `SingleTableCore.ts:796` | `delete()` 结果被丢弃 → `ElMessage.success('删除成功')` + `removeRowByCode` = **假删除**（行消失、服务端还在） |
| 2 | `TreeTableCore.ts:662` | `deleteTreeNodeWithConfirm` 同上 = **假删除树节点** |
| 3 | `TreeTableCore.ts:685-690` | `res.data` 为空时用请求体兜底构造节点 → **追加幽灵节点** |

**页面自定义 handler 是"对的但会失效"**：`iso-standard:161,172`、`report-rule:222`、`skill-manage:135,146`、`ConfigTab:76` 共 **6 处**已是 `try { await…; success } catch { error }` 正确形态，**但 catch 只靠 HTTP 400 触发**。P0 后 catch 永不进入 → 照样弹"保存成功"。⇒ 必须与 P0 同批改造（F1）。

---

## 二、已批准的 6 项决策（2026-09-25 用户裁决）

| # | 决策 | 结论 |
|---|---|---|
| **D1** | `loadPage` 失败时保留旧数据 vs 清空 | **保留**（避免"突然没数据"误判），只弹错 |
| **D2** | `deleteTreeNodeWithConfirm` / `deleteTreeNode` 签名 `void → boolean` | **改**（否则调用方永远无法感知成败，正是假删除根源）；`vue-tsc` fail-fast，同步改 `dictionary/logic.ts:137` |
| **D3** | `YzhForm` 校验失败是否额外弹 warning | **默认关**（字段标红已足够，避免与页面提示重复）；保留 `validateMessage` prop 供页面开启 |
| **D4** | `toggleIsValid` 失败从 `return null` 改为 throw | **改 throw**（保持 F-2 一致）；`menu/logic.ts:104` 外层 `toggleTreeNodeWithConfirm` 已有 catch |
| **D5** | F0 与后端 P0 是否同一次提交 | **分两个逻辑批次**，同一次验收内完成 |
| **D6** | `client.ts` 的 `onError` 是否发提示 | **不发提示，只做 401 副作用**（清 Token / 跳登录）。**提示一律「谁 catch 谁弹」**。⇒ 22 文档 §六 代码示例同步修订 |

---

## 三、四条铁律（F-1 ~ F-4，前端改造红线）

| 铁律 | 内容 |
|---|---|
| **F-1 判定唯一** | 写操作返回的 `ApiResponse` **必须**经 `expectOk()` 判定通过，才允许触发副作用（弹成功 / 关弹窗 / 插行改行删行 / 调 `onAfter*` 钩子） |
| **F-2 失败必 throw** | 业务失败 → `throw BizError`，**不返回 `null`/`false` 给流程层**（否则每个调用点都要再判一次，必然遗漏） |
| **F-3 谁 catch 谁弹** | 提示唯一出口见 §四 分工表。底层方法**只 throw 不提示**；**绝不双弹** |
| **F-4 取消不是错误** | `ElMessageBox.confirm` 的 `cancel`/`close` 必须就地吞掉，**不得**进失败分支、不得弹错 |

### 3.1 分层模型

```
L-1 校验层   YzhForm.onSubmit:188   校验失败 → emit('validate',false) → 不发请求
L-2 请求层   client.ts              网络/HTTP 异常 → throw ApiError
L-3 结果层   expectOk()（新增）      success:false → throw BizError(err)
L-4 流程层   Core 流程方法           try/catch → 状态门禁 +（BizError 时）提示
```

### 3.2 提示出口分工表（F-3 落地细则）

| 错误来源 | 谁弹 | 说明 |
|---|---|---|
| 校验失败 | Element Plus 字段标红（可选 `validateMessage` warning） | `YzhForm:188-197` |
| **`ApiError`**（L1 网络 / L2 HTTP） | **Core 流程方法 / 页面 handler / `YzhTable.loadData` 的 catch** | client 的 `onError` **不弹**（D6） |
| **`BizError`**（L3 `expectOk`） | **同上**（同一个 catch） | `expectOk` 只在 Core 内抛，client 不感知 |
| `init()` / 读路径 | `init` 的统一 catch；表格由 `YzhTable:303` 既有 catch | |
| **无人 catch 的漏网** | `app.config.errorHandler` + `unhandledrejection` 兜底 → `ElMessage.error` 一次 | 宿主注入，带 `handled` 标记防重 |
| `client.ts` `onError` | **不发提示**（D6） | 只做 401 清 Token/跳登录副作用 |

> **双弹防护**：Core catch 内弹完后设 `e.handled = true`；兜底监听 `if (!e.handled)` 才弹。

---

## 四、改造范围总账（实测基线 2026-09-25）

### 4.1 后端

| 指标 | 数量 | 归属阶段 |
|---|---|---|
| `return BadRequest(` | **130**（三基类 56 = TreeTable 24 / Tree 17 / Yzh 15） | P1 |
| `Ok(new {` 匿名对象 | **132**（16 文件，Top6 = 127；P2 后裸 `Ok(new {` = 0，余 22 处为已包 `ApiResponse<>.Ok(new {...})` 内层） | P2 |
| `Unauthorized/NotFound` | 19（6 + 13） | P1 |
| `new ApiResponse`（唯一构造点） | **1**（`GlobalExceptionFilter.cs:102`） | P0（封闭构造的全部成本） |
| 非泛型 `ApiResponse.Ok/Fail/Error` 调用 | 105（7/97/1） | P0 补 `Code`/`Err` |
| 直接返回 `Result` 的端点 | **0**（保持 0） | — |
| `ApiResponse.Ok` 无条件包成功（B01） | `TreeTableControllerBase.cs:305` 等 | P0 |

**132 处匿名对象分布**：`StandardDirectory` 35 / `ReportDefinition` 25 / `DocExtractionRule` 21 / `FileStorage` 13 / `DirectoryTemplate` 8 / `QueueMonitor` 7 / `RoleMenu` 4 / `Role` 4 / `RoleApi` 4 / `Menu` 2 / `AIUsage` 2 / `CertOrgStandard` 2 / `CertOrgStage` 2 / `YzhControllerBase` 1 / `TreeTableControllerBase` 1 / `CertificationBody` 1。

### 4.2 前端

| 指标 | 数量 | 归属阶段 |
|---|---|---|
| `.success` 读取 | 80 | F0/F1 保留（安全死分支），F3 清理 |
| `.message` 读取 | 100（其中 **88 处在失败/错误语境**） | 多数是 `e.message`（Error 对象，不受影响）；**约 15 处读响应体 `message`** 需改 `err \|\| message` |
| `.msg` 读取 | 15 | P2 随 E7 消灭 |
| `code === 200`（E7） | **3** | P2 删除 |
| `.err` 占用 | **0**（命名安全） | — |
| 逻辑内核写路径缺口 | `SingleTableCore` 16 项 / `TreeTableCore` 16 项 | F0 |
| 页面自定义 handler | 6 | F1 |
| `@validate` 绑定 / `YzhFormDialog` 透传 | **0 / 0** | F0-F2 / F0 |

**约 15 处失败语境读响应体 `message`**（需改 `err || message`）：
`apiResponse.ts:18`、`SingleTableCore:411`、`TreeTableCore:749`、`client.ts:162,247,281,305`、`YzhAppLayout:297,333,360`、`report-rule-config:48`、`nc-config/designer:66`、`WorkflowDesigner:1055`、`doc-extraction-rule:188`、`directory/index.vue:202,232,380,414`、`ConfigTab:94,116`、`useDirectoryApi:222,240,248`。

---

## 五、分阶段计划与 TODO

> 每阶段完成后**必须**跑该阶段的门禁，全绿才进下一阶段。

### 阶段 0 · 文档（本次先行）

- [x] 新建 `23-前后端信封统一改造计划-V1.md`（本文）
- [x] `22-接口返回规范-V1.md`：
  - §六 宿主注入示例改 `onError` 只做 401 副作用（D6）
  - §十 10.2 增加 **F 阶段**（前端逻辑内核）行
  - §十 10.4 新增「本文实施记录」指针
- [x] `20-前后端契约权威表-V1.md` §3.1：`err` 行状态改「实施中」
- [x] `10-YZH架构/README.md` + `docs/00-工程体系/README.md` + `AGENTS.md` 加 23 指针

**门禁**：文档索引无死链。

### 阶段 F0 · 前端逻辑内核 success 控制（**可独立先上线**）

| # | 任务 | 文件·行 |
|---|---|---|
| F0-a | 公共原语 `BizError` / `expectOk` / 升级 `unwrap` / `confirmOrFalse` | `yzh.vue.core/src/utils/apiResponse.ts` |
| F0-b | `SingleTableCore` 16 项（见 §六 A 表） | `logic/SingleTableCore.ts` |
| F0-c | `TreeTableCore` 16 项（见 §六 B 表） | `logic/TreeTableCore.ts` |
| F0-d | `YzhFormDialog` `validate` 透传 + 提交中禁用 | `components/form/YzhFormDialog.vue` |
| F0-e | 宿主注入 `onError`（401 副作用）+ `unhandledrejection` 兜底 | `cert-admin/main.ts`、`cert-auditor/main.ts` |
| F0-f | 下游 4 处 override 跟随 | `dictionary:137`、`menu:104`、`nc-config:127`、`api:99` |

**门禁**
```bash
cd src/certplatform-web && node scripts/guards.mjs        # 11 规则 0 违规
npx vue-tsc --noEmit -p yzh.vue.core/tsconfig.json         # 0 error
npm run build --workspace cert/cert-admin                  # 0 error
npm run build --workspace cert/cert-auditor                # 0 error
```

### 阶段 P0 · 后端信封 + 假成功止血 ✅（`bb4b375`）

| # | 任务 | 文件 |
|---|---|---|
| P0-a | `ApiResponse<T>`/`ApiResponse` 加 `Err` + **私有构造（封闭）** + 非泛型补 `Code` + `BizFail` 工厂 | `YZH.Core.Stand/Models/Result/ApiResponse.cs` |
| P0-b | `GlobalExceptionFilter` 改工厂（唯一 `new` 站点）+ **业务异常 → HTTP 200** + **只脱敏系统异常** | `YZH.Core.Api/Filters/GlobalExceptionFilter.cs:101-116` |
| P0-c | `ExecuteTreeAction` 判 `Result.Success`（B01/B02） | `YZH.Core.Api/Controllers/TreeTableControllerBase.cs:288-306` |
| P0-d | `EntityService` 删除结果判定 + 计数禁兜 0（B05/B06） | `EntityService.cs:284-323,556-651` |
| P0-e | `client.ts` L1–L4 归一化 + `res.json().catch()` + `err` 优先 | `yzh.vue.core/src/api/client.ts` |
| P0-f | `types/contracts.ts` `ApiResponse` 加 `err` | `types/contracts.ts:31` |

**门禁**：`dotnet build src/yzh-core/YZH.Core.Web` **0 错误**（封闭构造让漏改处编译不过）+ 服务能起 + `./scripts/backend/test-dictionary-api.sh`。

### 阶段 P1 · HTTP 收口（业务失败 → 200）

- [x] 三基类 54 处 `BadRequest` → `Ok(ApiResponse.Fail(...))`（`TreeTable` 22 / `Tree` 17 / `Yzh` 15；TreeTable 另 2 处已在 P0-c 转）
- [x] 其余 74 处 `BadRequest` → `Ok(ApiResponse.Fail(...))`（含 11 处裸 `{code,msg}`/`{success,error}` 载荷一并标准化）
- [x] 13 处业务语义 `NotFound` → 200；**6 处 `Unauthorized` 保留 401**（3 处 Token 失效 + 3 处登录失败）
- [x] **新增**：`ApiBehaviorOptions.InvalidModelStateResponseFactory` —— `[ApiController]` 自动 400 + RFC7807 → 200 + `success:false` + `err`（DataAnnotations 英文模板转中文）
- [x] 测试脚本断言从「看 400」改看 `success:false + err 非空`；并修 5 项既有失败（jq `||`→`or`、夹具守卫改 SQL 侧 `LEFT(DicName,5)='__T__'`、`fields-tables` 改传 ruleCode、失败文案改读 `err`）

**门禁** ✅：`grep -rn "return BadRequest(" src --include=*.cs`（排除 `src/old/`）→ **0**；`return NotFound(` → **0**；`return Unauthorized(` → **6**（保留）；
`test-dictionary` **64/64**、`test-doc-extraction` **51/51**、`test_iso_clause_tree` **38/38**、`smoke_test` ✓、`dotnet test` **10/10**。

### 阶段 P2 · 132 处裸 JSON 收口（前后端同批）

- [x] 110 处裸 `Ok(new {` → `Ok(ApiResponse<object?>.…)`（`StandardDirectory` 35 / `ReportDefinition` 25 / `DocExtractionRule` 21 / `FileStorage` 13 / `DirectoryTemplate` 8 / `QueueMonitor` 7 / `MenuController` 1）；另 22 处在 P2 之前已是 `ApiResponse<>.Ok(new {...})`，故基线 132 = 110 裸 + 22 已包
- [x] 前端 8 处 E7 `code === 200` / `!== 200` 分支删除（`directory/index.vue:197,232,380,414,297`、`ConfigTab.vue:94,116`、`useDirectoryApi.ts:122,222,240,248`、`WorkflowDesigner.vue:1200`）→ 一律 `res.success` / `!res.success`
- [x] 前端失败语境 `res.message`/`res.msg` → `res.err || res.message`（`directory`/`ConfigTab`/`useDirectoryApi`/`WorkflowDesigner`/`YzhAppLayout`/`client.ts` 下载路径/`doc-extraction-rule`）
- [x] `guards.mjs` R3 debt 白名单清空（`workflow/directory/` 6 处修完即删）；R3b 本就为空
- [x] E7 状态改 ⛔ **已消灭**（同步 20 / 22 / 18 / 样板页面指南）
- [x] `BizResult` 接口（`useDirectoryApi.ts`）由 `{code,msg}` 改标准信封 `{success,code,message,err,data}`

**门禁** ✅：裸 `Ok(new {` → **0**（`Ok(new {` 全量 22，均为已包内层）；前端 `code === 200/!== 200` → **0**；`guards.mjs` 11 规则 614 文件通过；`typecheck:all` EXIT=0；`build:all` EXIT=0。

### 阶段 F1 · 页面与共享层收口

- [x] 6 处页面自定义 handler 收口 —— **实测分流**：
  - **5 处已由 F0 的 throw 语义自愈**（`add`/`update`/`addRootNode`/`updateTreeNode` 走 `unwrapOk`/`expectOk`，失败必抛 → 页面 catch 拿到 BizError）：`iso-standard/index.vue:161,172`、`skill-manage/index.vue:135,146`、`ConfigTab.vue:76`（P2 已改 `!res?.success` → throw）。**不改**，避免与 `reportError` 形成双弹（F-14）
  - **1 处真缺陷（`report-rule`）**：`saveSection` 等 API 返回信封但 handler **丢弃返回值** → 失败也弹「保存成功」。同文件一并修 6 处：`handleSaveSection` / `handleDeleteTemplate` / `handleDeleteSection` / `handleSaveTemplate` / `handleUploadFile` 统一 `expectOk(res, …)`；`loadTemplate` 失败由「静默清空模板」改弹错；`loadSections` 失败由「静默清空列表」改**保留上次数据 + 弹错**（F-10/F-3）
- [x] `cert-share` 2 个 composables 统一 —— `useDirectoryApi`（28 处）、`useQueueMonitor`（8 处）：
  - 读路径 `return res.data!` → `unwrapOk(res, '…失败')`（原失败返回 `null` → 页面空白且零提示）
  - 写路径裸 `await yzhApi.post(...)` → `expectOk(...)`（原丢弃信封 → 假成功）
  - `uploadInit` 加 `expectOk`（原失败 `taskId:''` 只 `console.error`）
  - **保留**返回 `BizResult` 的写接口（调用方已判 `res?.success`）与 `retryFailedConversions` 的 `{ok,message}` 结果形态
- [x] `YzhTable.loadData`：`error.value = e?.err || e?.message || '数据加载失败'`；**删除 `rows=[] / total=0`**（原失败清空表格 → 违反 F-10「保留上次数据」）

**门禁** ✅：`guards.mjs` 11 规则 / 614 文件；`typecheck:all` EXIT=0（5 workspace）；`build:all` EXIT=0。

### 阶段 F2 · YzhForm 体验

- [ ] F1 `validateMessage` prop（默认关，D3）
- [ ] F2 `YzhFormDialog` 透传 `validate`
- [ ] F3 提交中禁用取消 / ESC / 关闭 X
- [ ] F4 detail 模式按钮 `disabled`
- [ ] F5 成功关窗后 `resetFields()`

**门禁**：三端 build + 手工验证 F-12 / F-13。

### 阶段 P3/F3 · 守卫与防回潮

**后端** `scripts/backend/guards.mjs`（新建）：
- **B-R1** 禁**裸** `Ok(new {`（P2 已清零，P3 起基线 0；已包 `ApiResponse<>.Ok(new {...})` 内层 22 处属合法）
- **B-R2** 禁 `new ApiResponse`（基线 1，P0 完成后启用）
- **B-R3** 禁业务语境 `return BadRequest(`
- **B-R4** 信封不变量：`success:false ⇒ err!==""`（运行时 `ApiResponseContractFilter`）

**前端** `scripts/guards.mjs` 新增：
- **R13** 禁 Core 内直接 `if (!res.success) { return }`（必须走 `expectOk`）
- **R14** 禁空 `catch {}`（吞错）
- **R15** 禁 `ElMessageBox.confirm` 无 catch
- **R16** 禁 `ElMessage.success` 出现在 `expectOk` 之前

**运行时** `ApiResponseContractFilter`（ActionFilter）放行前校验不变量 ①②，违规即抛。

**门禁**：后端 + 前端守卫 **0 违规**。

### 阶段 V · 全面验证（用户验收前）

见 §八 验收矩阵 14（F）+ 13（P）项逐条执行，全绿后通知用户。

---

## 六、前端逐方法改造清单（F0 执行依据）

### A. `SingleTableCore.ts`（870 行，16 项）

| # | 方法·行 | 现状（问题） | 目标行为 |
|---|---|---|---|
| S1 | `init` 276-279 | 无 catch → unhandled | try/catch：`ElMessage.error` + `config=null` |
| S2 | `loadConfig` 282-285 | `apiGet:283` 不判 | `expectOk(res,'加载配置失败')` → throw，由 S1 接 |
| S3 | `loadPage` 295-318 | `apiPost:305` 不判；**`catch{rows=[]}` 静默清空** | 删吞 catch；`expectOk`；**失败保留旧 rows** + 弹错（D1） |
| S4 | `dataLoader` 327-356 | `apiPost:344` 不判、**无 catch** | `expectOk`；失败 **throw** → `YzhTable:303` 既有 catch |
| S5 | `add` 391-394 | 不判，返回 `undefined` | `unwrap<V>(res,'新增失败')` |
| S6 | `update` 397-400 | 同上 → `:753 (saved as any)[pk]` **TypeError** | 同 S5 |
| S7 | `delete` 403-405 | **响应完全丢弃** | `expectOk(res,'删除失败')`；`Promise<void>`，失败 throw |
| S8 | `executeAction` 408-423 | 读 `res.message:411`；`:415 success(res.data)` 依赖 data 是字符串 | `expectOk`；成功提示 `res.message \|\| (typeof data==='string'?data:'') \|\| '操作成功'` |
| S9 | `toggleIsValid` 428-438 | `else` **静默 return null** | 失败 **throw**（D4）；成功路径不变 |
| S10 | `toggleRowIsValidWithConfirm` 443-466 | `confirm:452` **无 catch**；不判 result | ① `confirmOrFalse` ② 失败由 catch 统一处理 |
| S11 | `importData` 486-491 | `apiUpload:489` 不判 | `expectOk` + 成功 `ElMessage.success('导入成功')` |
| S12 | `dispatch` 570-607 / `onRowAction:610` / `onToolbarAction:615` | handler 抛出无人接 | **顶层 try/catch**（唯一兜底），catch 弹错 + 复位 `selectedRows` |
| S13 | **`submitForm` 736-762** | **无 catch**；`onAfterAdd:743`/`onAfterUpdate:749` **在成功判定前** | 见 §六.1 伪代码 |
| S14 | **`confirmDelete` 768-802** | `confirm:790` 无 catch；`:796` 无条件成功；`:797` 无条件移行 | 见 §六.2 伪代码 |
| S15 | `exportData`/`downloadTemplate` 473-496 | 流下载无判定 | 保留（L2 兜）+ 补 try/catch 弹错 |
| S16 | `apiGet/apiPost/apiUpload` 810-846 | — | 不改（薄封装） |

#### §六.1 `submitForm` 目标结构

```ts
async submitForm(): Promise<boolean> {
  this.submitting.value = true
  try {
    const isAdd = this.dialogMode.value === 'add'
    isAdd ? this.onBeforeAdd(this.formData) : this.onBeforeUpdate(this.formData)
    const payload = this.normalizeBeforeSubmit({ ...this.formData })
    const saved = isAdd
      ? unwrap<V>(await this.apiPost<ApiResponse<V>>('/add', payload), '新增失败')
      : unwrap<V>(await this.apiPost<ApiResponse<V>>('/update', payload), '修改失败')
    // —— 以下全部在 expectOk 通过之后 ——
    if (isAdd) {
      this.onAfterAdd(this.formData)
      this.insertRow(saved)
    } else {
      this.onAfterUpdate(this.formData)
      this.replaceRowByCode(saved[this.primaryKey], { ...this.editingRow.value, ...saved } as V)
    }
    ElMessage.success('保存成功')
    this.dialogVisible.value = false
    return true
  } catch (e: any) {
    if (!e?.handled) { e.handled = true; ElMessage.error(e?.message || '保存失败') }
    return false                      // 弹窗不关、行不插、钩子不跑
  } finally {
    this.submitting.value = false
  }
}
```

#### §六.2 `confirmDelete` 目标结构

```ts
async confirmDelete(rows?: V[]): Promise<void> {
  // …前置校验与现有一致（770-779）
  const confirmed = await confirmOrFalse(message, '删除确认')   // F-4 取消静默
  if (!confirmed) return
  try {
    expectOk(await this.apiPost<ApiResponse<string>>('/delete', codes), '删除失败')
  } catch (e: any) {
    if (!e?.handled) { e.handled = true; ElMessage.error(e?.message || '删除失败') }
    return                                                  // 不移行
  }
  ElMessage.success('删除成功')
  for (const code of codes) this.removeRowByCode(code)
  this.selectedRows.value = []
  this.onAfterDelete(codes)
}
```

### B. `TreeTableCore.ts`（969 行，16 项）

| # | 方法·行 | 现状（问题） | 目标行为 |
|---|---|---|---|
| T1 | `init` 369-384 | 无 catch | try/catch（同 S1） |
| T2 | `loadConfig` 357-368 | **`res.data.TableConfig` 在 data undefined 时 TypeError** | `unwrap` 后再取 `TableConfig` |
| T3 | `loadTreeRoot` 385-396 | 不判，`res.data ?? []` → **失败静默空树** | `expectOk`；失败保留旧树 + throw（T1 接） |
| T4 | `loadChildren` 397-470 | 不判 | **登记例外**：`expectOk` 失败 → `ElMessage.error` + `resolve([])`，**不 throw**（el-tree 懒加载抛错会卡 loading） |
| T5 | `loadPageWithTree` 471-497 | 吞 catch 清空 | 同 S3（D1） |
| T6 | **`submitTreeNodeForm` 623-648** | **无 catch**；`onAfterAddTree:630`/`onAfterUpdateTree:639` 在判定前；`:642` 无条件成功；`finally:644` 重置 `treeDialogMode` 污染下次打开 | 同 §六.1；失败**不关弹窗、不重置 mode** |
| T7 | **`deleteTreeNodeWithConfirm` 651-663** | `confirm:655` 无 catch；`:662` **无条件 success**；`onAfterDeleteTree:661` 在判定前 | 同 §六.2；签名改 **`Promise<boolean>`**（D2） |
| T8 | `addTreeNode` 670-701 | 不判；**`:685 res.data ?? 兜底构造` → 幽灵节点** | `unwrap<TreeItemDto>` 后再 `dtoToNode`+append；失败 throw **不造节点**；保留"前端预分配 Code"（`:688` 仅 `success:true && data 空` 时生效） |
| T9 | `updateTreeNode` 704-725 | 不判；失败仍改本地 `node.Name:723` | `unwrap` 后再 replace；失败 throw **本地不改** |
| T10 | `deleteTreeNode` 728-770 | 已判 `:748` 但 `return` 令调用方**无法感知**（void） | 签名 **`Promise<boolean>`**（D2）：成功 `true` / 预检拦截 `false`（`:736` warning 保留）/ 业务失败 `false` / 异常 throw |
| T11 | `executeTreeAction` 773-785 | 不判；**`:783 失败也 loadTreeRoot()`（掩盖）** | `expectOk` **在刷新之前**；失败不刷新并 throw；返回 `unwrap` 后的 data |
| T12 | `toggleTreeNodeIsValid` 788-807 | `else` 静默 `return null:806` | 失败 **throw**（D4）；成功路径不变（`:796-804`） |
| T13 | `toggleTreeNodeWithConfirm` 810-829 | `confirm:822` 无 catch | `confirmOrFalse` + 外层 catch |
| T14 | **`onNodeAction` 854-900** | `confirm:887` 无 catch；**`:896 ElMessage.success(result)` 不看 success**；`:894 refreshTable` 在成功与否未知时执行 | 顶层 try/catch；`result` 改 `unwrap` 后的 data；**仅成功后** refresh + success |
| T15 | `refreshChildren` 931-941 | 不判 | `expectOk`；失败 throw（T14 顶层接） |
| T16 | `loadPageWithoutTree` / `refreshTable` | 无 catch | 由 `YzhTable:303` 既有 catch 兜 |

### C. `YzhForm.vue` / `YzhFormDialog.vue`

| # | 点·行 | 现状 | 目标 |
|---|---|---|---|
| C1 | `YzhForm.onSubmit` 188-197 | 校验失败只 `emit('validate')`；**全仓 `@validate` 绑定 = 0** | 保留 emit；新增 `validateMessage` prop + `showValidateMessage`（**默认关**，D3）→ `ElMessage.warning` |
| C2 | `YzhFormDialog` | **未透传 `validate`（透传数 0）** | 补 `@validate` 转发 |
| C3 | `YzhFormDialog` footer 25-31 + dialog 2-10 | 提交中可点取消 / ESC / 关闭 X → "关了但没保存" | `:close-on-press-escape="!loading"`、`:show-close="!loading"`、取消 `:disabled="loading"` |
| C4 | `YzhForm` 401-405 | 提交按钮 `:loading` ✅ | detail 模式 `disabled`；reset 按钮提交中禁用 |
| C5 | 成功关窗后 | 无 `resetFields` | 成功关窗后 `resetFields()`（F5） |

### D. 下游同步清单

| 文件·行 | 影响 | 处理 |
|---|---|---|
| `system/dictionary/logic.ts:137` | `override deleteTreeNodeWithConfirm(): Promise<void>` → 基类改 `boolean` → **vue-tsc 必报错** | 同步改签名；**删除其 `:150 ElMessage.success('已删除')`** |
| `system/menu/logic.ts:104` | `super.toggleTreeNodeIsValid` 失败由 null 改 throw | 外层 `toggleTreeNodeWithConfirm` 已有 catch ✅ |
| `workflow/nc-config/logic.ts:127`、`system/api/logic.ts:99` | `override dataLoader` 基类改可能 throw | 各补 `expectOk`（**2 处**） |
| 6 处页面自定义 handler | 5 处已由 F0 throw 自愈；`report-rule` 真缺陷（丢弃信封） | F1 已统一 `expectOk` |
| `cert-share` 2 composables | 36 处裸 `res.data!` / 丢信封 | F1 已统一 `unwrapOk`/`expectOk` |
| `WorkflowDesigner.vue:1039` | `success===false \|\| code>=400` 双判 | 保留到 F3（P0 后 `code>=400` 失效，`success` 接管 ✅ 兼容） |

---

## 七、后端写法转换对照（P1/P2 执行依据）

| 现状写法 | 目标写法 | 备注 |
|---|---|---|
| `return Ok(new { code = 200, data = X })` | `return Ok(ApiResponse<T>.Ok(X))` | `T` = `X` 的类型；`IActionResult` 返回点无需改签名 |
| `return Ok(new { code = 200, data = X, message = M })` | `return Ok(ApiResponse<T>.Ok(X, M))` | |
| `return Ok(new { code = ok ? 200 : 400, msg = ok ? "成功" : "失败" })` | `ok ? Ok(ApiResponse.Ok("成功")) : Ok(ApiResponse.Fail("失败"))` | **HTTP 恒 200** |
| `return Ok(new { code = 400, message = M })` | `return Ok(ApiResponse.Fail(M))` | 业务拒绝，HTTP 200 |
| `return Ok(new { code = 404, message = M })` | `return Ok(ApiResponse.Fail(M, 404))` 或按语义 `Fail(M)` | 业务「不存在」→ 200 + `code:400` |
| `return Ok(new { code = 500, message = M })` | `return Ok(ApiResponse.Error(M))` | 系统异常；但**已知业务语义应走 `Fail`** |
| `return BadRequest(M)`（业务拒绝） | `return Ok(ApiResponse.Fail(M))` | **P1 核心** |
| `return NotFound(M)`（业务「不存在」） | `return Ok(ApiResponse.Fail(M))` | 基础设施 404 保留 |
| `return Unauthorized()` | **保留** | 401 = 未登录 |
| `return Ok(ApiResponse.Ok(result))` 无条件包成功（B01） | 判 `result.Success` → `Ok(...)` / `Ok(BizFail(...))` | `ExecuteTreeAction` |
| `await Xxx.Update(...)` 丢弃返回值（B05） | 判结果 → 失败 `Fail(err)` | `OrganizationController` |
| `catch { return 0; }` / 删除结果不判（B06） | 上抛 / 判结果 | `EntityService` |
| `return result`（`Result`/`Result<T>`） | `result.ToApiResponse()` | 实测 0 处，**保持 0** |

### 7.1 `ApiResponse` 目标形态（P0-a）

```csharp
public class ApiResponse<T>
{
    [JsonPropertyName("success")] public bool Success { get; init; }
    [JsonPropertyName("message")] public string Message { get; init; } = string.Empty;  // 成功提示，失败时 ""
    [JsonPropertyName("err")]     public string Err     { get; init; } = string.Empty;  // ★ 新增，失败时必非空
    [JsonPropertyName("data")]    public T? Data { get; init; }
    [JsonPropertyName("code")]    public int Code { get; init; }
    [JsonPropertyName("timestamp")] public DateTime Timestamp { get; init; } = DateTime.Now;

    private ApiResponse() { }                                   // ★ 封闭：唯一 new 站点只剩过滤器（改为工厂）

    public static ApiResponse<T> Ok(string message = "操作成功");      // success:true, code:200, err:""
    public static ApiResponse<T> Ok(T data, string message = "操作成功");
    public static ApiResponse<T> Fail(string err, int code = 400);     // success:false, message:"", err:err
    public static ApiResponse<T> BizFail(string err);                  // P1 起，HTTP 恒 200
    public static ApiResponse<T> Error(string err, int code = 500);
    public static ApiResponse<T> FromOperationResult(IOperationResult<T> result);
}
// 非泛型同构，★ P0-a 补 Code（现状缺失）
```

**不变量由工厂保证**（不在调用点手工拼）：
- `success:true` ⇒ `err:""` ②
- `success:false` ⇒ `err` 非空 ① 且 `message:""`

### 7.2 运行时校验 `ApiResponseContractFilter`（P3）

ActionFilter，放行前断言 ①②，违规即抛（开发环境）/ 记日志（生产），**绝不让违规响应流到前端**。

---

## 八、验收矩阵

### 8.1 前端 F 项（14）

| # | 场景 | 期望 |
|---|---|---|
| **F-01** | 新增提交时后端 `success:false` | 弹错误、**弹窗保持打开**、表格**无新行**、`onAfterAdd` **未执行** |
| **F-02** | 编辑提交业务失败 | 同上；`onAfterUpdate` 未执行、行数据未变 |
| **F-03** | 删除业务失败 | 弹错误、**行仍在**、`onAfterDelete` 未执行 |
| **F-04** | 删除确认点"取消" | **零提示、零请求**（非错误） |
| **F-05** | 树节点删除业务失败 | 弹错误、节点仍在、**不出现"已删除"** |
| **F-06** | 树节点新增业务失败 | 弹错误、**无幽灵节点** |
| **F-07** | 树节点改名业务失败 | 弹错误、**本地 Name 不变** |
| **F-08** | 切换启用/禁用业务失败 | 弹错误、`Extra[field]` 不变 |
| **F-09** | 自定义 action 业务失败 | 弹错误、**树不刷新**、不出现 success |
| **F-10** | 分页 / 树加载业务失败 | 弹错误、**保留上次数据**（不清空闪烁） |
| **F-11** | 懒加载子节点失败 | 弹错误、loading 结束、可重试 |
| **F-12** | 表单校验失败 | 字段标红、**零网络请求** |
| **F-13** | 提交进行中 | 取消 / ESC / 关闭 X 全部禁用 |
| **F-14** | 同一操作 | **只出现一条提示**（无双弹、无 `unhandledrejection`） |

### 8.2 后端 P 项（13，来自 22 §十一）

| # | 场景 | HTTP | `success` | `err` | 前端表现 |
|---|---|---|---|---|---|
| **P-01** | 删除有子机构的机构 | 200 | `false` | 非空 | 红色，**节点仍在** |
| **P-02** | 删除不存在的 Code | 200 | `false` | 非空 | 红色（现状：绿色） |
| **P-03** | 正常删除 | 200 | `true` | `""` | 绿色 + **SQL 复查 `IsDeleted=1`** |
| **P-04** | 计数查询异常 | 200 | `false` | 非空 | 红色（**拒绝执行**，不放行） |
| **P-05** | 禁用时 `Update` 失败 | 200 | `false` | 非空 | 红色，不显示「已禁用 N 个」 |
| **P-06** | 禁用不存在的机构 | 200 | `false` | 「机构不存在」 | 红色（现状：绿色） |
| **P-07** | 抛 `YZHNotFoundException` | **200** | `false` | **原文** | 红色，可见真实原因 |
| **P-08** | 抛未处理异常 | **500** | `false` | **脱敏** | 红色「操作失败，请联系管理员」 |
| **P-09** | 403（**空响应体**） | 403 | — | — | 「无权限」，**不是 JSON 解析错误** |
| **P-10** | 改错 `VITE_API_BASE` | — | — | — | 「网络错误」 |
| **P-11** | 断网 | — | — | — | 「网络错误」 |
| **P-12** | `success:false` 但 `err==""` | — | — | **违规** | 过滤器 / 守卫**红灯拦截** |
| **P-13** | 匿名对象响应（无 `success`） | — | — | — | 后端守卫 **B-R1 红灯** |

---

## 九、回归命令

```bash
# ===== 文档 =====
grep -rn "23-前后端信封统一改造" docs/ AGENTS.md | wc -l     # ≥3（三处索引）

# ===== 后端 =====
dotnet build src/yzh-core/YZH.Core.Web                      # 0 错误（封闭构造让漏改处编译不过）
./scripts/backend/run-backend.sh
./scripts/backend/test-dictionary-api.sh
./scripts/backend/test-doc-extraction-api.sh
node scripts/backend/guards.mjs                             # B-R1..R4 0 违规（P3 后）

# ===== 前端 =====
cd src/certplatform-web
node scripts/guards.mjs                                     # 11(+R13..R16) 规则 0 违规
npx vue-tsc --noEmit -p yzh.vue.core/tsconfig.json          # 0 error
npm run build --workspace cert/cert-admin
npm run build --workspace cert/cert-auditor
npm run build --workspace yzh.vue.core                      # 若存在

# ===== 实测计数（应全部归零或达标） =====
grep -rn "return BadRequest("    src/yzh-core src/certplatform-api --include=*.cs | wc -l   # P1 后：业务语境 0
grep -rnE "(^|[^.])Ok\(new \{"   src/yzh-core src/certplatform-api --include=*.cs | wc -l   # P2 后（裸）：0
grep -rn "new ApiResponse"       src/yzh-core src/certplatform-api --include=*.cs | wc -l   # P0 后：0（改工厂）
grep -rn "code === 200"          src/certplatform-web/{yzh.vue.core/src,cert} --include=*.ts --include=*.vue | wc -l  # P2 后：0
grep -rn "\.err\b"               src/certplatform-web/{yzh.vue.core/src,cert} --include=*.ts --include=*.vue | wc -l  # >0
```

---

## 十、风险与回滚

| 风险 | 缓解 |
|---|---|
| `loadPage` 不再清空 → 旧数据停留 | F-10 验证 + 错误条明确「加载失败」（D1） |
| `loadChildren` 不 throw 与 F-2 冲突 | **登记为例外**（el-tree 懒加载约束），写入 22 §九 例外表 |
| 签名变更打断 4 处 override | `vue-tsc` fail-fast；§六.D 清单同步改，一次提交 |
| 现有 80 处 `.success` 变死分支 | F0/F1 保留不删（安全），F3 统一清理 |
| 失败时 `message:""` 导致 15 处显示回退文案 | §四.2 已列全清单，P2/F1 逐处改 `err \|\| message` |
| 封闭构造引发大面积编译错误 | **只 1 个 `new` 站点**（`GlobalExceptionFilter`），风险极低 |
| 130 处 `BadRequest` 手改出错 | 分批（三基类 → 业务控制器），每批 `dotnet build` + 接口脚本 |
| 132 处裸 JSON 漏改 | B-R1 守卫红灯 + `ApiResponseContractFilter` 运行时兜 |
| **回滚** | 每阶段一个独立 commit；`git revert` 单点回滚，阶段间无交叉依赖 |

---

## 十一、执行记录（实施完成后填写）

| 阶段 | 完成时间 | commit | 门禁结果 |
|---|---|---|---|
| 0 文档 | 2026-09-25 | `2bd9044` | 23 计划定稿 + 22/20/README/AGENTS 索引同步 |
| F0 | 2026-09-25 | `63634fb` | guards 11 规则通过；`vue-tsc` 五 workspace EXIT=0；`build:all` EXIT=0 |
| P0 | 2026-09-25 | `bb4b375` | `dotnet build` 0 错误；后端重启就绪 4s；`test-dictionary` 63/64、`test-doc-extraction` 46/50（**5 项失败为改造前既有脚本/夹具缺陷，见下**）；实测：删不存在 Code → `success:false`+`err:"记录不存在或已被删除"`；业务异常 → **HTTP 200** + `err:"机构不存在"`；成功信封 `err:""` |
| P1 | 2026-09-25 | `0461cc8` | `BadRequest`→0、`NotFound`→0、`Unauthorized`→6（保留）；`ApiBehaviorOptions` 模型校验 400→200 信封；三套接口脚本全绿（64/51/38）+ smoke + dotnet test 10/10 |
| P2 | 2026-09-25 | `bfbb502` | 裸 `Ok(new {` → 0；前端 `code === 200` → 0；guards 11 规则/614 文件；`typecheck:all` EXIT=0；`build:all` EXIT=0；实测 `organization-tree`/`configs`/`summary` 成功信封 `err:""`、`upload-confirm`/`folders delete`/`download 缺参` → **HTTP 200 + `success:false` + `err` 非空 + `message:""`**；`retry-failed-conversions` → `data:{enqueued,queueCount}`；三套接口脚本 64/51/38 + smoke + dotnet test 10/10 |
| F1 | 2026-09-25 | （本阶段 commit） | guards 11 规则/614 文件；`typecheck:all` EXIT=0；`build:all` EXIT=0 |
| F2 | | | |
| P3/F3 | | | |
| V 全面验证 | | | |

### P0 门禁附注：既有失败（非本次引入，P1 一并修）

| # | 失败点 | 根因 | 归属 |
|---|---|---|---|
| 1 | `test-dictionary` 清理步骤「仍有残留（未知 行）」 | 框架自动生成 32 位 GUID Code，而 `cleanup_dict_test_data.sh` 守卫要求 Code 以 `__T__` 开头 → 恒拒绝（`2>/dev/null` 吞掉退出码 2） | 夹具守卫与数据生成方式不匹配 |
| 2 | `test-doc-extraction`「返回 200 或 400（降级）」「code=200 或 400」 | 断言用「200 双竖线 400」写法，**jq 没有双竖线运算符**（应为 `or`）→ jq 编译错误 → 断言恒假 | 脚本语法 |
| 3 | `test-doc-extraction`「字段数=1」「表格数=1」 | 响应 `fields:[] tables:[]`，库内无该规则的字段/表格种子数据 | 测试数据未 seed |

---

## 十二、变更流程

```
① 先改 22-接口返回规范-V1.md      ← 语义唯一权威
② 再改本文（23）                  ← 改造计划与验收
③ 同步 20-前后端契约权威表 §3.1
④ 改代码（按 §五 阶段顺序）
⑤ 跑 §九 全套回归
⑥ 填 §十一 执行记录 → 通知用户验收
```

**★ 铁律**：**先改文档，再改代码**。文档与代码不一致时以文档为准；若文档错，先修文档。

---
status: living
---

# 前端架构详解

> **版本**：V1.1 | **日期**：2026-09-23 | **状态**：编写中
>
> 本文档详细定义前端核心基类、组件和类型体系。
> 来源：`src/certplatform-web/yzh.vue.core/src/logic/`、`src/certplatform-web/yzh.vue.core/src/components/`

---

## ⚠️ 基类名勘误（2026-09-23 实测，读本文前必看）

**本文 §二 / §三 曾长期使用 `SingleTableCore<V>` —— 该类名全仓不存在。**
机制描述仍然有效，但**类名与文件路径按下表为准**：

| 文档旧称（❌ 不存在） | 实际类名（✅ 唯一正确） | 实际位置 |
|---|---|---|
| `SingleTableCore<V>` | **`SingleTableCore<V>`** | `yzh.vue.core/src/logic/SingleTableCore.ts` |
| `TreeTableCore<V> extends SingleTableCore<V>` | **`TreeTableCore<V>`**（`TreeTableLogic` 只是它的**别名**，见 `logic/TreeTableLogic.ts`：`export { TreeTableCore as TreeTableLogic }`） | `yzh.vue.core/src/logic/TreeTableCore.ts` |
| — | `AssociationTreeCore` / `CheckTreeCore` / `LinkTableCore` | 同目录 |
| 导入 `from '@yzh-core'` | 导入 **`from '@yzh-core'`** | 包出口 `yzh.vue.core/src/index.ts` |

**新建页面请直接看 `docs/10-YZH架构/样板页面指南-V1.md`**（含唯一样板与"照抄时必须改的 5 处"），不要照本文的示例代码逐字抄。

---


## 一、整体架构

```
src/certplatform-web/
├── yzh.vue.core/                    ← YZH 架构 npm 包（系统底座，五原子，不启动、不感知后端地址）
│   ├── src/components/              ← 原子组件（R1 零领域依赖）
│   │   ├── table/
│   │   │   ├── YzhTable.vue         ← 数据表格组件
│   │   │   └── types.ts             ← 表格类型定义
│   │   ├── form/
│   │   │   ├── YzhForm.vue          ← 表单组件
│   │   │   └── types.ts             ← 表单类型定义
│   │   └── layout/
│   │       ├── YzhTreeTableLayout.vue     ← 左树右表布局组件
│   │       ├── YzhPageLayout.vue    ← 页面布局组件
│   │       ├── YzhDialog.vue        ← 弹窗组件
│   │       ├── YzhSearchBar.vue     ← 搜索栏组件
│   │       └── YzhToolbar.vue       ← 工具栏组件
│   ├── src/logic/                   ← 原子方法（SingleTableCore/TreeTableCore/CheckTreeCore 内核）
│   ├── src/api/                     ← 原子 API（yzhApi + configureYzhApi + auth + system/*）
│   ├── src/pages/                   ← 原子界面（★迁移中：auth/ home/ system/ 11 页）
│   ├── src/layouts/                 ← 原子界面（★迁移中：YzhAppLayout 应用壳）
│   └── src/router/                  ← 原子路由（★迁移中：yzhLoginRoute/yzhSystemRoutes/createYzhRoutes）
│
└── cert/                            ← 认证平台业务层（宿主：独立可运行的 Vue 启动框架）
    ├── cert-share/                  ← 业务共享层（API + 类型；**不含 Logic 基类**）
    │   └── src/
    │       ├── api/                 ← API 调用（镜像后端 Controllers）
    │       │   ├── auth.ts
    │       │   ├── generic.ts
    │       │   ├── cert/            ← Foundation 域 API
    │       │   │   ├── iso-standard.ts
    │       │   │   └── iso-clause.ts
    │       │   └── workflow/        ← Workflow 域 API
    │       │       └── ...
    │       └── types/
    │           ├── contracts.ts     ← 前后端统一契约
    │           └── tree.ts          ← 树节点类型
    │
    ├── cert-admin/                  ← 管理员端（端口 9990，Element Plus）
    │   └── src/
    │       ├── api/                 ← 仅管理端使用的 API 模块
    │       └── pages/               ← 页面（镜像后端 Controllers）
    │           ├── system/

    │           │   ├── user/{index.vue,logic.ts}      ← ★ 单表唯一样板
    │           │   ├── role/{index.vue,logic.ts}      ← ★ 纯树节点样板
    │           │   └── organization/{index.vue,logic.ts}
    │           ├── foundation/
    │           │   └── iso-standard/{index.vue,logic.ts}  ← ★ 左树右表样板
    │           └── workflow/
    │               └── ...
    │
    ├── cert-auditor/                ← 专家端（端口 9991，Naive UI）
    └── cert-enterprise/             ← 企业端（尚未开工）

> **Logic 基类位置**：`SingleTableCore` / `TreeTableCore` / `AssociationTreeCore` / `CheckTreeCore` /
> `LinkTableCore` 全部在 **`yzh.vue.core/src/logic/`**（框架级），**不在** `cert-share/`。
> 业务层通过 `import { SingleTableCore } from '@yzh-core'` 使用。
```

---

## 二、SingleTableCore<V> — 单表 Logic 基类

### 2.1 类定义

```typescript
// 文件：yzh.vue.core/src/logic/SingleTableCore.ts

export abstract class SingleTableCore<V extends Record<string, any>> {
    // ──── 必须设置 ────
    abstract controllerName: string    // 控制器名称（如 'User'、'Role'）

    // ──── 后端配置 ────
    config = ref<EntityConfigDto | null>(null)

    // ──── 表格状态 ────
    rows = ref<V[]>([])
    loading = ref(false)
    selectedRows = ref<V[]>([])

    // ──── 分页状态 ────
    pagination = reactive({ page: 1, pageSize: 20, total: 0 })

    // ──── 搜索过滤状态 ────
    searchParams = reactive<Record<string, any>>({})
    sortField = ref<string | undefined>()
    sortOrder = ref<'asc' | 'desc' | undefined>()

    // ──── 弹窗状态 ────
    dialogVisible = ref(false)
    dialogMode = ref<'add' | 'edit'>('add')
    submitting = ref(false)
    formData = reactive<Record<string, any>>({}) as Partial<V>
}
```

### 2.2 从 config 派生的 UI 结构（computed）

```typescript
// 表格列配置（XsFlag=true 的列）
get columns(): YzhTableColumn<V>[]

// 表单字段配置（BcFlag=true 的字段）
get formFields(): YzhFormField[]

// 搜索栏字段配置（优先 SearchFields，否则从 Columns 推导）
get searchFields(): SearchField[]

// 工具栏按钮配置（从 ToolbarConfig 派生）
get toolbarButtons(): Array<{key: string, text: string, type: string, action?: string}>

// 行操作按钮配置（从 RowButtonConfig 派生）
get rowButtons(): Array<{key: string, text: string, type: string, action?: string}>

// 主键字段名（统一使用 'code'）
get primaryKey(): string { return 'code' }
```

### 2.3 核心方法

```typescript
// ──── 初始化 ────
async init(): Promise<void>           // loadConfig → loadPage
protected async loadConfig(): Promise<void>  // GET /api/{controller}/config

// ──── 数据查询 ────
async loadPage(): Promise<void>       // POST /api/{controller}/filter

// ──── 写入操作 ────
protected async add(entity: Partial<V>): Promise<V>        // POST /add
protected async update(entity: Partial<V>): Promise<V>     // POST /update
protected async delete(codes: string[]): Promise<void>     // POST /delete
async executeAction(methodName: string, row: V): Promise<void>  // POST /action/{name}

// ──── 导出导入 ────
async exportData(format?: 'excel' | 'csv', fields?: string[]): Promise<void>
async importData(file: File): Promise<any>
async downloadImportTemplate(): Promise<void>

// ──── Split 增量更新（不重新请求）───
protected removeRowByCode(code: string): void    // 删除行
protected replaceRowByCode(code: string, row: V): void  // 替换行
protected insertRow(row: V, position?: 'top' | 'bottom'): void  // 插入行
```

### 2.4 钩子方法（子类可覆盖）

```typescript
// 数据加载后
protected onDataLoaded(_rows: V[]) {}

// 新增/修改前（可修改待提交实体）
protected onBeforeAdd(_entity: Partial<V>) {}
protected onBeforeUpdate(_entity: Partial<V>) {}

// 新增/修改后
protected onAfterAdd(_entity: Partial<V>) {}
protected onAfterUpdate(_entity: Partial<V>) {}

// 删除前（返回 false 取消删除）
protected onDelete(_codes: string[]): boolean | Promise<boolean> { return true }

// 删除后
protected onAfterDelete(_codes: string[]) {}

// 准备新增数据（设置默认值）
protected onPrepareAdd(_entity: Partial<V>) {}
```

### 2.5 弹窗操作

```typescript
// 打开新增弹窗
openAddDialog()    // 使用 config.NewEntity 初始化表单

// 打开编辑弹窗
openEditDialog(row: V)  // 用 row 数据覆盖默认值

// 取消弹窗
async cancelDialog()

// 提交表单
async submitForm()
//   → add() 或 update() → Split 方法增量更新 → ElMessage.success
```

### 2.6 完整使用示例

```typescript
// cert/cert-admin/src/pages/system/user/logic.ts（可选，简单页面可直接在 .vue 中操作）
export class UserPageLogic extends SingleTableCore<Sys_User> {
    controllerName = 'User'

    // 覆盖钩子
    protected onBeforeAdd(entity: Partial<Sys_User>) {
        entity.Enable = 1  // 默认启用
    }
}
```

```vue
<!-- cert/cert-admin/src/pages/system/user/index.vue -->
<script setup lang="ts">
import { YzhTable } from '@yzh-core/components/table'
import { YzhForm } from '@yzh-core/components/form'
import { ElMessage, ElMessageBox } from 'element-plus'
import { ref, onMounted } from 'vue'
import { UserPageLogic } from './logic'

const logic = new UserPageLogic()
const tableRef = ref()

onMounted(async () => {
  await logic.init()
})
</script>

<template>
  <YzhTable
    :columns="logic.columns"
    :data-loader="logic.loadPage.bind(logic)"
    :search-fields="logic.searchFields"
    :selectable="true"
    @selection-change="logic.selectedRows = $event"
  >
    <template #toolbar-left>
      <el-button type="primary" @click="logic.openAddDialog()">新增</el-button>
      <el-button type="danger" :disabled="logic.selectedRows.length === 0"
        @click="logic.confirmDelete()">批量删除</el-button>
    </template>

    <template #column-Enable="{ row }">
      <el-tag :type="row.Enable === 1 ? 'success' : 'info'">
        {{ row.Enable === 1 ? '启用' : '禁用' }}
      </el-tag>
    </template>

    <template #column-actions="{ row }">
      <el-button text type="primary" @click="logic.openEditDialog(row)">编辑</el-button>
      <el-button text type="danger" @click="logic.confirmDelete([row])">删除</el-button>
    </template>
  </YzhTable>

  <el-dialog v-model="logic.dialogVisible" :title="logic.dialogMode === 'add' ? '新增' : '编辑'">
    <YzhForm
      v-model="logic.formData"
      :fields="logic.formFields"
      :loading="logic.submitting"
      @submit="logic.submitForm"
      @reset="logic.dialogVisible = false"
    />
  </el-dialog>
</template>
```

---

## 三、TreeTableCore<V> — 左树右表 Logic 基类（别名 TreeTableLogic 已废弃）

### 3.1 继承关系

```
SingleTableCore<V>                       ← 单表：列/表单/搜索/按钮 由后端 EntityConfig 驱动
    │
    ▼
TreeTableCore<V>                         ← 继承单表全部能力 + 叠加树能力
    │
    └─ 别名 TreeTableLogic（@deprecated，见 logic/TreeTableLogic.ts，待消费者迁移后删除）
```

> **实测签名**：`export abstract class TreeTableCore<V extends Record<string, any> = any> extends SingleTableCore<V>`
> —— 泛型只有一个且有默认值，所以 `extends TreeTableCore<any>` 与 `extends TreeTableCore` 都合法。

### 3.2 新增状态

```typescript
// 真实实现（yzh.vue.core/src/logic/TreeTableCore.ts）
// 树状态通过 TreeSide 混入持有，对外以 getter/setter 暴露（不是裸 ref）
export abstract class TreeTableCore<V extends Record<string, any> = any>
    extends SingleTableCore<V> {

    protected readonly treeSide = new TreeSide()

    // ──── 树状态（getter/setter 转发到 treeSide） ────
    get/set treeData: TreeNode[]
    get treeLoading
    get/set selectedNode: TreeNode | null

    // ──── 树节点弹窗状态 ────
    treeDialogVisible, treeDialogMode, treeSubmitting,
    treeFormData, treeParentNode, treeEditingNode
}
```

### 3.3 快捷访问器

```typescript
// 从 treeTableConfig 派生
protected get treeConfig(): any         // TreeBehaviorConfigDto
protected get treeFormConfig(): EntityConfigDto | null  // 树节点表单配置
protected get noSelectionBehavior(): 'empty' | 'all'    // 未选中时的表格行为
protected get relateField(): string     // 关联字段名（PascalCase）
protected get treeFormFields(): YzhFormField[]  // 树节点表单字段（从 treeFormConfig 派生）
```

### 3.4 核心方法

```typescript
// ──── 配置加载（覆盖父类）───
async loadConfig(): Promise<void>   // GET /config → 返回 TreeTableConfigDto

// ──── 树加载 ────
async loadTreeRoot(): Promise<void>          // POST /tree/root
async loadChildren(node: any, resolve?: Function): Promise<TreeNode[]>  // POST /tree/children

// ──── 树→表格联动 ────
async onNodeClick(node: TreeNode): Promise<void>   // 选中节点 → 加载表格
async loadPageWithTree(treeCode: string): Promise<void>  // 带树条件的查询
async loadPageWithoutTree(): Promise<void>          // 无树条件（按 NoSelectionBehavior）

// ──── 树节点 CRUD ────
async addTreeNode(parentNode: TreeNode | null, data: Record<string, any>): Promise<TreeNode | null>
async updateTreeNode(node: TreeNode, newName: string, extra?: Record<string, any>): Promise<void>
async deleteTreeNode(node: TreeNode): Promise<void>
async executeTreeAction(methodName: string, node: TreeNode, extra?: Record<string, any>): Promise<any>

// ──── 刷新 ────
async refreshTree(): Promise<void>           // 重新加载整棵树
async refreshChildren(node: TreeNode): Promise<void>  // 刷新指定节点的子节点

// ──── 初始化（覆盖父类）───
async init(): Promise<void>  // loadConfig → loadTreeRoot → loadPageWithoutTree

// ──── DTO 映射（子类可覆盖）───
protected dtoToNode(dto: TreeItemDto, parent?: TreeNode): TreeNode
```

### 3.5 Split 树节点方法

```typescript
// 从树中移除节点（递归）
protected removeNodeFromTree(code: string): void

// 在树中查找节点
protected findNode(code: string): TreeNode | null

// 替换树中的节点
protected replaceTreeNode(code: string, newNode: TreeNode): void

// 树节点操作快捷方法
async addRootNode(data: Record<string, any>): Promise<TreeNode | null>
async addChildNode(parentNode: TreeNode, data: Record<string, any>): Promise<TreeNode | null>
async renameNode(node: TreeNode, newName: string): Promise<void>
```

### 3.6 完整示例（Organization）

```typescript
// cert/cert-admin/src/pages/system/organization/logic.ts
import { TreeTableCore } from '@yzh-core'
import { ref } from 'vue'

export class OrgPageLogic extends TreeTableCore<any> {
    controllerName = 'Organization'

    // ShowDisabled 开关
    showDisabled = ref(false)

    // 表格列扩展（添加操作列）
    get columnsWithActions() {
        const cols = this.columns as any[]
        const enhanced = cols.map(c => c.prop === 'Enable' ? { ...c, slot: true } : c)
        return [...enhanced, { prop: 'actions', label: '操作', slot: true, width: 240, fixed: 'right' }]
    }

    // 表单字段扩展（添加隐藏字段）
    get formFieldsWithHidden() {
        const fields = [...this.formFields]
        if (this.dialogMode.value === 'add') {
            fields.push({ prop: 'OrgCode', label: '所属机构', type: 'text', disabled: true, span: 24 })
        }
        return fields
    }

    // 覆盖 buildFilters：添加 ShowDisabled
    protected buildFilters(extra?: Record<string, any>) {
        const filters = super.buildFilters(extra)
        if (this.showDisabled.value) {
            filters.push({ Field: 'ShowDisabled', Value: 'true', Operator: 'eq' })
        }
        return filters
    }

    // 切换 ShowDisabled
    toggleShowDisabled() {
        this.showDisabled.value = !this.showDisabled.value
        this.pagination.page = 1
        this.loadPage()
    }
}
```

---

## 四、核心组件

### 4.1 YzhTable — 数据表格

```vue
<!-- 位置：yzh.vue.core/src/components/table/YzhTable.vue -->
<YzhTable
    :columns="logic.columns"
    :data-loader="loadData"
    :search-fields="logic.searchFields"
    :selectable="true"
    @selection-change="logic.selectedRows = $event"
>
    <!-- 工具栏左侧 -->
    <template #toolbar-left>
        <el-button type="primary" @click="logic.openAddDialog()">新增</el-button>
    </template>

    <!-- 工具栏右侧 -->
    <template #toolbar-right>
        <el-switch v-model="logic.showDisabled" label="显示已禁用" />
    </template>

    <!-- 自定义列 -->
    <template #column-Enable="{ row }">
        <el-tag :type="row.Enable === 1 ? 'success' : 'info'">
            {{ row.Enable === 1 ? '启用' : '禁用' }}
        </el-tag>
    </template>

    <!-- 操作列 -->
    <template #column-actions="{ row }">
        <el-button text type="primary" @click="logic.openEditDialog(row)">编辑</el-button>
        <el-button text type="danger" @click="logic.confirmDelete([row])">删除</el-button>
    </template>
</YzhTable>
```

**类型定义**（`yzh.vue.core/src/components/table/types.ts`）：

```typescript
export interface YzhTableColumn<V = any> {
    prop: string
    label: string
    width?: number
    minWidth?: number
    fixed?: 'left' | 'right'
    align?: 'left' | 'center' | 'right'
    sortable?: boolean | 'custom'
    slot?: boolean
    dictCode?: string
    formatter?: (row: V, column: any, cellValue: any) => string
}

export interface SearchField {
    prop: string
    label: string
    type: 'text' | 'number' | 'select' | 'date' | 'dateRange' | 'cascader'
    options?: { value: string; label: string }[]
    placeholder?: string
}
```

### 4.2 YzhForm — 表单

```vue
<!-- 位置：yzh.vue.core/src/components/form/YzhForm.vue -->
<YzhForm
    v-model="logic.formData"
    :fields="logic.formFields"
    :loading="logic.submitting"
    :cols="2"           <!-- 默认 2 列布局 -->
    label-width="100px"
    @submit="logic.submitForm"
    @reset="logic.dialogVisible = false"
>
    <!-- 自定义插槽（如只读展示字段） -->
    <template #orgCode="{ value }">
        <el-tag v-if="logic.selectedNode" type="info">
            {{ logic.selectedNode.name }}
        </el-tag>
    </template>
</YzhForm>
```

**类型定义**：

```typescript
export interface YzhFormField {
    prop: string
    label: string
    type: 'text' | 'textarea' | 'number' | 'select' | 'radio' | 'checkbox'
        | 'date' | 'datetime' | 'switch' | 'upload' | 'treeSelect' | 'cascader' | 'password'
    required?: boolean
    disabled?: boolean
    span?: number          // 栅格占位（1-24，默认 12）
    placeholder?: string
    dictCode?: string
    options?: { value: any; label: string }[]
    fieldSchema?: EntityFieldSchema  // 后端反射生成的字段描述
}
```

### 4.3 YzhTreeTableLayout — 左树右表布局

```vue
<!-- 位置：yzh.vue.core/src/components/layout/YzhTreeTableLayout.vue -->
<YzhTreeTableLayout
    :tree-data="logic.treeData"
    :tree-width="260"
    :tree-toolbar="true"      <!-- 显示添加根节点按钮 -->
    :tree-searchable="true"   <!-- 显示树搜索框 -->
    :tree-lazy="true"         <!-- 懒加载 -->
    :tree-load-data="logic.loadChildren.bind(logic)"
    @tree-node-click="logic.onNodeClick"
>
    <template #default>
        <!-- 右侧表格内容 -->
        <YzhTable :columns="logic.columnsWithActions" ... />
    </template>
</YzhTreeTableLayout>
```

---

## 五、类型契约（contracts.ts）

```typescript
// 文件：cert/cert-share/src/types/contracts.ts

// API 响应
export interface ApiResponse<T = any> {
    Success: boolean
    Message: string
    Data: T
    Code: number
    Timestamp: number
}

// 分页结果
export interface PagedData<T = any> {
    Items: T[]
    Total: number
    Page?: number
    PageSize?: number
}

// 树节点 DTO（与后端 TreeItemDto 一一对应）
export interface TreeItemDto {
    Code: string
    Name: string
    ParentCode?: string
    NodeType?: string
    IsLeaf: boolean
    Level: number
    Sort?: number
    Extra?: Record<string, any>
    Children?: TreeItemDto[]
}

// 过滤请求
export interface FilterRequest {
    Page: number
    PageSize: number
    SortField?: string
    SortOrder?: 'asc' | 'desc'
    Filters: FilterItem[]
}

export interface FilterItem {
    Field: string
    Operator: 'eq' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte' | 'like' | 'in' | 'isnull' | 'isnotnull'
    Value?: any
}

// 实体配置（与后端 EntityConfigDto 一一对应）
export interface EntityConfigDto {
    Title: string
    FillMode?: 'AutoFix' | 'PixFix'
    Columns: ColumnConfig[]
    NewEntity?: Record<string, any>   // 空实体模板（前端初始化表单用）
    Schema?: Record<string, EntityFieldSchema>  // 字段结构描述
    SearchFields?: any[]
    Toolbar?: any
    RowButtons?: any
}

export interface ColumnConfig {
    FieldName: string
    DesName: string
    Width?: number
    Type: string          // TextBox/NumberBox/DatePicker/ComboBox/Switch/...
    XsFlag: boolean       // 表格显示
    BcFlag: boolean       // 表单编辑
    Yxk: boolean          // 允许空
    Enable?: boolean
    Sortable?: boolean
    Align?: string
    Fixed?: string
    DictCode?: string
    Mrz?: any             // 默认值
    Sxh?: number          // 排序号
    Row?: number
    Col?: number
    RowSpan?: number
    ColSpan?: number
}
```

---

## 六、树节点类型（tree.ts）

```typescript
// 文件：cert/cert-share/src/types/tree.ts

// 核心 TreeNode（纯业务数据 + 结构元数据，无 UI 状态）
export interface TreeNode<T = any> {
    code: string                        // 业务编码（唯一标识）
    name: string                        // 显示名称
    parentCode: string | null           // 父节点编码（根节点为 null）
    nodeType?: string                   // 节点类型（异构树区分来源）
    isLeaf?: boolean                    // 是否有子级（后端事实）
    sort?: number                       // 排序号
    extra?: Record<string, any>         // 附加业务字段
    children: TreeNode<T>[]             // 子节点集合
    raw?: T                             // 原始实体数据（可选）
}

// UI 状态扩展（由组件持有，不进核心模型）
export type UiTreeNode<T = any> = TreeNode<T> & {
    checked?: boolean
    indeterminate?: boolean
    selected?: boolean
    isExpanded?: boolean
    isLoading?: boolean
}
```

---

## 七、页面开发模式

### 7.1 模式一：配置驱动（推荐）

适用于：标准 CRUD 页面，字段来自后端 JSON 配置。

```
┌──────────────────────────────────────────────────────────────┐
│  后端 Controller 继承 YzhControllerBase<V>                    │
│  → 提供 /config /filter /add /update /delete /action/*        │
│                                                              │
│  前端 Logic 继承 SingleTableCore<V>                             │
│  → 自动从 /config 获取 columns / formFields / searchFields    │
│  → 提供 loadPage() / add() / update() / delete()              │
│                                                              │
│  前端 .vue 只需：                                              │
│  1. new Logic() 并 await logic.init()                         │
│  2. <YzhTable :columns="logic.columns" ... />                 │
│  3. <YzhForm :fields="logic.formFields" ... />                │
└──────────────────────────────────────────────────────────────┘
```

### 7.2 模式二：手写 API（当前已有页面）

适用于：后端尚未完成迁移、或特殊业务页面。

```
┌──────────────────────────────────────────────────────────────┐
│  前端直接调用 cert/cert-share/src/api/xxx.ts 中的手写 API 函数   │
│  → 自行管理 columns / searchFields / 弹窗状态                   │
│  → 不依赖 Logic 基类                                           │
│                                                              │
│  示例：foundation/iso-standard/index.vue                       │
│  → columns 硬编码                                              │
│  → 直接调用 getISOStandardPage(params)                         │
└──────────────────────────────────────────────────────────────┘
```

> **趋势**：新页面优先使用模式一（配置驱动），旧页面逐步迁移。

---

## 八、关键文件索引

| 文件路径 | 职责 |
|---------|------|
| `yzh.vue.core/src/logic/SingleTableCore.ts` | 单表 Logic 基类 |
| `yzh.vue.core/src/logic/TreeTableCore.ts` | 左树右表 Logic 基类 |
| `cert/cert-share/src/types/contracts.ts` | 前后端统一契约类型 |
| `cert/cert-share/src/types/tree.ts` | 树节点类型定义 |
| `yzh.vue.core/src/components/table/YzhTable.vue` | 数据表格组件 |
| `yzh.vue.core/src/components/table/types.ts` | 表格类型定义 |
| `yzh.vue.core/src/components/form/YzhForm.vue` | 表单组件 |
| `yzh.vue.core/src/components/layout/YzhTreeTableLayout.vue` | 左树右表布局组件 |
| `yzh.vue.core/src/components/layout/YzhPageLayout.vue` | 页面布局组件 |
| `cert/cert-admin/src/pages/system/user/index.vue` | 单表页面参考（当前手写模式） |
| `cert/cert-admin/src/pages/system/organization/index.vue` | 左树右表页面参考 |
| `cert/cert-admin/src/pages/system/organization/logic.ts` | 左树右表 Logic 实现参考 |

---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9a16bac6e27d25132787d930f50d9879_0e5248e3897811f18108525400287e28
    ReservedCode1: I6mMZCeFjC8DbVP1ozCnOyC4061gsGsUOIZgH5lUwffxWi8hqvD0rrZnmj0YfPMLHyYti+zb51gmpkHPfXyWMadGIajPkQBHsn0GT9pNBLUPIr3tRN7cfLLcKhh9kDqOTGEaLwda4c73GC12o1kQrhJtvozy3kTXnFw3AzDxPPDyPdKFxxz1p5H33Ck=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9a16bac6e27d25132787d930f50d9879_0e5248e3897811f18108525400287e28
    ReservedCode2: I6mMZCeFjC8DbVP1ozCnOyC4061gsGsUOIZgH5lUwffxWi8hqvD0rrZnmj0YfPMLHyYti+zb51gmpkHPfXyWMadGIajPkQBHsn0GT9pNBLUPIr3tRN7cfLLcKhh9kDqOTGEaLwda4c73GC12o1kQrhJtvozy3kTXnFw3AzDxPPDyPdKFxxz1p5H33Ck=
---

# 体系认证审核平台 — 项目全局规则

> **版本**：V1.1 | **日期**：2026-07-29 | **状态**：成熟态
>
> ⚠️ **本文件是项目最高优先级规范。所有 AI 对话必须首先对齐本文件，所有开发行为必须以本文件为边界。**

---

## 一、文档即宪法

```
本项目的一切从文档开始，以文档作为最终验收标准。

开发前：
  文档定义了"要做什么"和"怎么做"
  AI 从文档中获取约束、边界、设计决策

开发中：
  代码实现必须对齐对应文档
  发现文档与实现不一致 → 更新文档，而非迁就代码

验收时：
  文档是唯一的验收标准
  文档说了"必须支持分片上传" → 代码必须有分片上传
  文档没说"支持 WebSocket" → 代码做了也不算数
```

---

## 二、文档目录结构

### 2.1 顶层结构

```
docs/
├── 项目全局规则-V1.md               ← 本文件（最高优先级）
├── 00-工程体系/                   ← 宪法级：管文档本身、管协作、管AI
├── 10-调研与可行性/               ← 归档态：已完成历史使命
├── 20-架构决策/                   ← 成熟态：技术选型、架构图、性能评估
├── 30-范围与边界/                 ← 成熟态：MVP 边界、遗漏排查
├── 40-领域设计/                   ← 萌芽态为主：业务建模，用时深化
├── 50-规划与优先级/               ← 成熟态：排期、迭代计划
├── 60-AI工程设计/                 ← 成熟态：宪法模板、Skills、知识库
└── 历史文档/                       ← 过期文档统一存放处（不分文件夹，扁平存放）
```

### 2.2 文件夹命名规范

| 规则 | 说明 |
|------|------|
| 前缀数字 | `00-` 到 `60-`，数字越小优先级越高，也代表阅读顺序 |
| 中文描述 | 用简洁的中文描述文件夹职责 |
| 禁止平铺 | 不允许在 docs/ 根目录放置文档——所有文档必须有归属文件夹 |
| 历史文档 例外 | 历史文档/ 是唯一的扁平存放区，不分文件夹，不放 README |

### 2.3 每个文件夹必须包含 `README.md`

```
README.md 的强制结构：

1. 文件夹作用（一句话）
2. 文档清单（每份文档的文件名 + 一句话职责 + 当前状态）
3. 关键词索引（AI 快速检索用）
4. 其他文件夹的引用（如果本文件夹的文档依赖其他文件夹的结论）
```

---

## 三、文档版本管理

### 3.1 版本号规则

```
V1.0 → V2.0 → V3.0   （主版本：重大修订、推翻重写）
V1.1 → V1.2 → V1.3   （次版本：补充细节、修正错误）
```

### 3.2 过期文档处置

```
当文档被新版本替代时：
  旧版本 → 移入 docs/历史文档/（扁平存放，不分文件夹）
  源文件夹只保留最新版

原则：
  - 不删除任何文档——过期文档是思想碰撞的结果，有保留价值
  - 历史文档/ 只进不出，不做二次整理
```

### 3.3 版本号在文档中的位置

每份成熟态文档的文件头必须包含：

```markdown
> **版本**：V1.0 | **日期**：YYYY-MM-DD | **状态**：成熟态
```

萌芽态文档不需要版本号，只需要日期。

---

## 四、AI 文档检索协议

### 4.1 检索流程

```
用户提出需求
    ↓
AI 识别需求关键字
    ↓
查阅 docs/项目全局规则-V1.md（本文件）→ 定位目标文件夹
    ↓
查阅目标文件夹的 README.md → 定位具体文档
    ↓
读取具体文档 → 对齐约束 → 执行
```

### 4.2 关键字 → 文件夹映射

| 关键字 | 对应文件夹 | 说明 |
|--------|-----------|------|
| 架构、技术栈、部署、服务器、性能 | `20-架构决策/` | |
| MVP、边界、不做、范围 | `30-范围与边界/` | |
| 业务、审核、NC、证书、证据、检查表 | `40-领域设计/` | |
| 排期、优先级、迭代、Phase | `50-规划与优先级/` | |
| AI、宪法、Skill、知识库、代码生成、模板 | `60-AI工程设计/` | |
| 规则、文档管理、协作、人设、方针 | `00-工程体系/` | |
| 竞品、调研、市场、可行性 | `10-调研与可行性/` | 仅历史参考 |

### 4.3 AI 强制行为

- **每次对话开始时**，如果项目宪法（`项目全局规则-V1.md`）未被加载，必须先加载
- **生成任何代码前**，先确认该代码对应的文件夹 README 已查阅
- **发现文档缺失**时，不自行编造结论，而是告知用户"XX 文档缺失，需要补充后才能继续"

---

## 五、文档验收规则

### 5.1 开发中的文档验证

```
每个模块开发完成后：
  [ ] 对照文档逐项检查——文档中列出的功能点是否都已实现
  [ ] 发现实现与文档不一致 → 评估是文档错了还是代码错了
  [ ] 文档错了 → 更新文档（先改文档，再改代码）
  [ ] 代码错了 → 改代码
  [ ] 实现中有文档未覆盖的发现 → 补充到文档
```

### 5.2 文档质量检查清单

```
每份成熟态文档在定稿前：
  [ ] 文件头包含版本号、日期、状态
  [ ] 与其他文档无矛盾（交叉引用已验证）
  [ ] 量化结论有数据支撑（非"估计"、"大概"）
  [ ] 对齐项目全局规则（命名、路径、技术栈）
  [ ] 足够详细，可直接指导开发或 AI 生成代码
```

---

## 七、源代码目录结构

### 7.1 顶层结构

```
体系认证平台/                         ← Git 仓库根
├── README.md                         ← 项目说明 + 端口规划 + 快速开始
├── .gitignore                        ← Git 忽略规则
│
├── src/                              ← 源代码（按端拆分）
│   ├── README.md                     ← 子项目一览 + 关键词索引
│   ├── server/                       ← .NET 8 后端 API（Vol 框架）
│   │   └── README.md                 ← 后端技术栈 + 配置说明
│   ├── admin/                        ← 后台管理前端（Vol 自带）
│   ├── auditor/                      ← 审核员 Web 前端（Vue 3 + Vite）
│   │   └── README.md                 ← 前端技术栈 + 启动说明
│   └── auditor-app/                  ← 审核员移动端（预留）
│
├── docker/                           ← Docker Compose 配置
│   ├── compose.yml                   ← MySQL 8.0 + Redis 7 + MinIO
│   ├── .env                          ← 环境变量
│   ├── start.sh / stop.sh            ← 启停脚本
│   └── mysql/conf/                   ← MySQL 自定义配置
│
├── docs/                             ← 项目文档（分层：00-60 + 历史文档）
├── scripts/                          ← 运维/工具脚本
└── 输出产物/                          ← 系统输出目录（临时交付物）
```

### 7.2 端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | `9992` | Vol WebApi |
| 后台管理前端 | `9990` | Vol admin |
| 审核员前端 | `9991` | Auditor web |
| MySQL | `3307` | Container yzh-mysql → 宿主机映射 |
| Redis | `6380` | Container yzh-redis → 宿主机映射 |
| MinIO API | `9000` | Container yzh-minio → 宿主机映射 |
| MinIO Console | `9001` | Container yzh-minio → 宿主机映射 |

### 7.3 AI 检索路径

```
用户提出代码级需求
    ↓
AI 识别涉及哪个端（server / admin / auditor）
    ↓
读取 src/{端}/README.md → 获取技术栈、启动方式
    ↓
查阅 docs/ 对应设计文档 → 获取业务约束
    ↓
查阅 docs/60-AI工程设计/vol-skill.md → 获取代码模板
    ↓
生成代码
```

### 7.4 子项目 README 规范

每个 `src/` 下的子目录必须包含 `README.md`，包含：
1. 技术栈（框架、语言、主要依赖）
2. 启动命令
3. 目录结构说明
4. 关键词索引（供 AI 检索）

---

## 八、禁止事项

| 禁止 | 原因 |
|------|------|
| 在 docs/ 根目录放置文档 | 违反分层原则，AI 无法快速定位 |
| 同一份文档在多个文件夹中出现 | 违反单一事实来源原则 |
| 文档与代码不一致却不更新 | 文档失效，AI 失去锚点 |
| 删除旧版本文档（不移入历史文档/） | 丢失思想碰撞记录 |
| 用"待补充"、"TODO"敷衍关键章节 | 关键信息缺失导致开发阻塞 |
| 文档中引用未创建的文档 | 引用链断裂 |

---

**文档版本**：V1.0
**创建时间**：2026-07-27
**创建背景**：在完成 17 份调研/设计/评估文档 + 5 份工程体系文档后，建立最高层级的文档管理元规则
*（内容由AI生成，仅供参考）*

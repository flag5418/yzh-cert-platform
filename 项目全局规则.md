---
status: living
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9a16bac6e27d25132787d930f50d9879_2b0d2606952a11f181ac525400f8a581
    ReservedCode1: oAgTZ9xCBO2U7ad0yXBUxk/8ciTxjiftxS7L7Fq2Snt39DFsUfy22V7EefQPqbs7QofzY4lER42Y0lX2hVySMIVfOT14FfZHOn+2TfQas7w+Z4RDN/lb3Olyp1SIDBHhtw1UjZfqXduMuGH8qYgMaalhbzLcVWIpde6ceTXpoG7BCdDjPT5+dCCIlFE=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9a16bac6e27d25132787d930f50d9879_2b0d2606952a11f181ac525400f8a581
    ReservedCode2: oAgTZ9xCBO2U7ad0yXBUxk/8ciTxjiftxS7L7Fq2Snt39DFsUfy22V7EefQPqbs7QofzY4lER42Y0lX2hVySMIVfOT14FfZHOn+2TfQas7w+Z4RDN/lb3Olyp1SIDBHhtw1UjZfqXduMuGH8qYgMaalhbzLcVWIpde6ceTXpoG7BCdDjPT5+dCCIlFE=
---



# 体系认证审核平台 — 项目全局规则

> **版本**：V3.2 | **日期**：2026-09-23 | **状态**：成熟态
>
> ⚠️ **本文件是项目最高优先级规范。所有 AI 对话必须首先对齐本文件，所有开发行为必须以本文件为边界。**

---

## 〇、项目概述

> 2026-08-16 由根目录 README.md 迁移：README 已删除，本文件承接"项目说明"入口职能（项目概述 / 技术栈 / 端口规划 / 快速开始 / 目录结构）。

**映智汇认证审核管理系统**（`yzh-cert-platform` · 四川映智汇信息技术有限公司）：面向认证行业的数字化审核管理平台，覆盖 ISO 体系认证从企业建档、审核任务分派、资料预审、审计复核到报告生成与 NC 管理的全流程。

- 仓库根：`/Volumes/Expand/wangqingquan/Documents/work/study/体系认证平台`
- 技术栈：见 §二（.NET 8 + Vol / Vue 3 + TypeScript + Vite + Element Plus（admin/auditor 端）/ MySQL 8.0 / Redis 7 / MinIO / Docker Compose）
- 端口规划：见 §7.2；快速开始：见 §7.5；目录结构：见 §7.1
- 文档入口：本文件（宪法级）→ `docs/00-工程体系/README.md`（全项目文档导航）→ 各目录 README

### 0.1 核心目的与架构演进（2026-09-07 新增）

> **⚠️ 本章是项目最高优先级定义，所有开发行为必须以本章为根本遵循。**

**当前核心目的**：完善新架构（YZH.Core），用新架构重新构造原项目的接口和业务功能。

**架构分层**：

| 层级 | 项目路径 | 状态 | 说明 |
|------|---------|------|------|
| **新后端架构** | `src/certplatform-api/` | ✅ 活跃开发（默认目标） | 包含 `CertPlatform.Shared/`、`CertPlatform.Admin/`、`CertPlatform.Auditor/`、`CertPlatform.Enterprise/`，唯一启动入口 `src/yzh-core/YZH.Core.Web` |
| **框架层** | `src/yzh-core/` | ⚠️ **受控修改** | 4 个工程 Stand/DataBase/Api/Web，框架基类与安全过滤器所在；**仅允许两类修改**：①框架 bug/安全修复 ②框架新能力基座。每次修改必须通过 `dotnet test` + 重启冒烟双验证，并同步更新 `docs/10-YZH架构/` 对应章节；禁止夹带业务代码 |
| **新前端架构** | `src/certplatform-web/` | ✅ 活跃开发（默认目标） | 包含 `yzh.vue.core/`（核心组件库）、`cert/cert-share/`（业务共享层）、`cert/cert-admin/`（管理员端）、`cert/cert-auditor/`（审核员端）、`cert/cert-enterprise/`（企业端） |
| **历史后端代码** | `src/old/server/Vue.NetCore/vol.api/` | **历史保留，禁止修改** | Vol 框架后端实现，仅作为新项目业务逻辑/接口设计/数据库结构参考 |
| **历史前端代码** | `src/old/server/Vue.NetCore/vol.web/` | **历史保留，禁止修改** | Vol 框架前端实现，仅作为新项目页面结构/UI 参考 |
| **旧审核员前端** | `src/old/auditor/` | **历史保留，禁止修改** | 未使用的旧审核员前端代码，已归档 |

**强制规则**：
1. **新架构是唯一开发目标**：所有新功能、接口重构、业务逻辑调整必须在新架构下实现
2. **旧项目禁止修改**：`src/old/` 下所有目录不允许任何修改（包括 bug 修复、接口调整、配置变更）
3. **旧项目仅作参考**：可用于理解业务逻辑、接口设计、数据库结构，但必须通过新架构重新实现
4. **数据库共享**：新旧架构共享同一 MySQL 实例（端口 3307），新架构直接复用现有表结构
5. **端口分离**：新后端目标是 9992（与旧后端一致），管理员端 9990，审核员端 9991

**AI 行为约束**：
- ❌ 禁止建议修改旧架构代码（`src/old/` 下所有目录）
- ❌ 禁止在新架构中调用旧架构的控制器/服务
- ✅ 新架构开发时可参考旧架构的业务逻辑和接口设计
- ✅ 遇到旧架构的 bug 时，在新架构中修复并记录

**术语定义**：
- **历史后端代码**：`src/old/server/Vue.NetCore/vol.api/`，旧 Vol 框架后端，禁止修改，仅参考
- **历史前端代码**：`src/old/server/Vue.NetCore/vol.web/`，旧 Vol 框架前端，禁止修改，仅参考
- **旧审核员前端**：`src/old/auditor/`，未使用的旧审核员前端代码，已归档

---

## 一、文档即宪法

```
本项目的一切从文档开始，以文档作为最终验收标准。

开发前：
  文档定义了"要做什么"和"怎么做"
  AI 从文档中获取约束、边界、设计决策

开发中：
  代码实现必须对齐对应文档
  发现文档与实现不一致 → 更新文档，而非迁就代码

验收时：
  文档是唯一的验收标准
  文档说了"必须支持分片上传" → 代码必须有分片上传
  文档没说"支持 WebSocket" → 代码做了也不算数
```

---

## 二、技术栈锁定

### 2.1 Vol 框架版本锁定

```
Vol 框架版本：VOL.Vue.NetCore（EF Core 版本）
锁定策略：非重大缺陷不升级
升级条件：仅当存在以下情况时才考虑升级
  - 安全漏洞（CVE）
  - 严重功能性 Bug（影响生产环境）
  - 新功能对项目有重大价值（需主导者评估）

AI 行为约束：
  - 不得主动提议升级 Vol 版本
  - 不得使用新版 Vol 特有的 API（除非确认当前版本已支持）
  - 发现 Vol 功能缺失时，优先用钩子扩展而非升级
```

### 2.2 技术栈清单

| 层级 | 技术 | 版本 | 状态 |
|------|------|------|------|
| 后端框架 | YZH.Core（新架构）/ Vol（旧架构） | .NET 8 | 锁定 |
| 后端架构 | certplatform-api/（CertPlatform.Shared + Admin + Auditor + Enterprise） | V6 2026-09 | 锁定 |
| 前端框架 | Vue 3 + TypeScript + Vite | Vue 3.4+ | 锁定 |
| 前端架构 | certplatform-web/（yzh.vue.core + share + admin + auditor） | V4 2026-09 | 锁定 |
| 旧前端 | vol.web（保留历史参考） | Vue 3 | 已弃用 |
| UI 组件库 | Element Plus（admin/auditor 端） | 最新稳定版 | 锁定 |
| 数据库 | MySQL | 8.0 | 锁定 |
| 缓存 | Redis | 7.x | 锁定 |
| 对象存储 | MinIO | 最新稳定版 | 锁定 |
| 移动端 | Flutter | 暂不开发 | Phase 2+ |

---

## 三、文档目录结构

### 3.1 顶层结构（2026-09-12 按磁盘实况校正）

```
docs/
├── 00-工程体系/                   ← 宪法级：管文档本身、管协作、管AI
├── 10-YZH架构/                    ← ★ YZH 架构唯一入口：架构总纲/后端基类/前端基类/数据契约/权限/代码结构/开发流程/常见错误
├── 20-体系认证/                   ← 系统全局概览：总体设计、数据库设计、整体架构/流程
├── 30-项目规则/                   ← 编码规范、Skill 清单、知识库（含 AGENTS.md 副本）
├── 40-实施/                       ← 实施报告、架构评审
├── 50-任务/                       ← 执行中的任务计划、模块设计与开发文档
├── 90-归档/                       ← 过期文档统一存放处（旧版本/ 历史项目/ 案例资料/）
└── README.md                     ← 全项目文档导航索引
```

> **注**：`项目全局规则.md` 位于仓库根目录（不在 docs/ 内）。`80-功能设计/` 为规划目录，待有功能设计文档落盘后启用；启用前功能设计类文档暂存 `50-任务/` 或 `40-实施/` 对应位置（见 §3.4.6）。

### 3.2 文件夹命名规范

| 规则 | 说明 |
|------|------|
| 前缀数字 | `00-` 到 `60-`，数字越小优先级越高，也代表阅读顺序 |
| 中文描述 | 用简洁的中文描述文件夹职责 |
| 禁止平铺 | 不允许在 docs/ 根目录放置文档——所有文档必须有归属文件夹 |
| 历史文档 例外 | 历史文档/ 是唯一的扁平存放区，不分文件夹，不放 README |

### 3.3 每个文件夹必须包含 `README.md`

```
README.md 的强制结构：

1. 文件夹作用（一句话）
2. 文档清单（每份文档的文件名 + 一句话职责 + 当前状态）
3. 关键词索引（AI 快速检索用）
4. 其他文件夹的引用（如果本文件夹的文档依赖其他文件夹的结论）
```

### 3.4 文档归类与存放规则（强制）

> **本章是文档存放的唯一权威判定依据。任何 AI 创建、移动、归档文档前，必须先按本节规则判定归属，禁止凭个人习惯、旧结构印象或猜测乱放。**

#### 3.4.1 各目录归属判定标准（2026-09-12 按磁盘实况校正）

| 目录 | 定位 | 存放什么 | 禁止放什么 |
|------|------|---------|-----------|
| `docs/` 根目录 | 无存储职能 | 仅允许 `README.md`（全项目文档导航） | 禁止平铺其他文档（`项目全局规则.md` 位于仓库根目录，不属于 docs/） |
| `00-工程体系/` | 宪法级 | 管文档本身、管 AI 协作的规范：生命周期规范、README（导航入口）、用户画像、YZH-建设原则、项目结构与启动指南（术语表已迁移至知识库 `30-项目规则/知识库/08-术语表/`） | 系统功能设计、技术选型、业务领域建模 |
| `10-YZH架构/` | 架构唯一入口 | YZH 框架总纲/后端基类/前端基类/数据契约/权限体系/代码结构/开发流程/常见错误/能力清单/安全分析 | 业务功能细节设计（放 20-体系认证 或规划中的 80-功能设计） |
| `20-体系认证/` | 系统全局概览 | 与系统全局有关的内容：总体设计、数据库表设计、整体架构/流程、ADR、架构评审、业务流程 | 功能细节设计 |
| `30-项目规则/` | 编码规则与知识底座 | 编码规范（C#/Vue-TS）、前后端代码结构统一规则、Skill 清单、AI 工程规范、知识库/（踩坑/速查/模板，含 AGENTS.md 副本） | 业务功能设计、架构理念（放 10-） |
| `40-实施/` | 实施记录 | 实施报告、架构设计评审报告、阶段实施总结 | 活跃任务计划（放 50-任务） |
| `50-任务/` | 执行中任务 | 当前迭代的任务计划、模块设计与开发文档、接口权限/数据字典等实施计划 | 已完成归档内容（移 40-实施 或 90-归档） |
| `90-归档/` | 归档态 | 已过时 / 被替代 / 旧版本备份（子区：旧版本/ 历史项目/ 案例资料/），只进不出，不做二次整理 | 活跃文档 |

> **规划目录说明**：`80-功能设计/`（功能设计中枢）与 `90-延展规划/` 为规划目录，当前尚未落盘；启用时按原定位执行（80：按端/模块分组 + README 总览地图；90：萌芽态延展设计）。启用前需同步更新 §2.1 与 `docs/00-工程体系/README.md`。

#### 2.4.2 新文档归类决策流程

```
新文档产生
    ↓
① 判性质：是 规范/流程？架构/选型？业务建模？执行中内容？延展想法？还是归档？
    ↓
② 选目录：对照 §2.4.1 表格，只允许进入一个归属目录；拿不准走 §2.4.5
    ↓
③ 按规范命名：成熟态 `<主题>-V<版本>.md`，萌芽态 `<主题>.md`；
   有编号序列的子目录（如 YZH-知识库/）先查 README 现有编号再续号，禁止与已有编号冲突
    ↓
④ 落盘后：更新该目录 README（文档清单+关键词）→ 更新 00-工程体系/README.md（全项目文档导航）
```

#### 2.4.3 禁止行为（红线）

- ❌ 禁止在 `docs/` 根目录平铺文档（唯一例外：`README.md` 导航索引）
- ❌ 禁止 AI 自行创建 §2.1 顶层结构中不存在的目录；确需新增必须向用户说明理由并获得确认，并同步更新 §2.1、§4.2 与 `docs/00-工程体系/README.md`
- ❌ 禁止跨目录乱放：一份文档只能存在于其归属目录；发现放错必须移动到正确目录并同步 README / INDEX
- ❌ 禁止把功能细节设计放入 `20-体系认证/`（只放全局概览）；也禁止把功能设计长期存放在非归属位置（当前暂存 `50-任务/`，`80-功能设计/` 启用后迁入）
- ❌ 禁止归档 / 移动 / 重命名文档后不同步 README 与 00-工程体系/README.md（全项目文档导航）

#### 2.4.4 移动 / 归档同步机制

任何文档发生**移动、归档、重命名、升版、删除**时，必须同步执行以下动作：

1. 更新源目录 README（移除条目）
2. 更新目标目录 README（新增条目）
3. 更新 `00-工程体系/README.md`（全项目文档导航 / 全量清单）
4. 检查引用该文档的其他文档，修正路径 / 名称引用

#### 2.4.5 拿不准放哪时（处理办法）

当无法确定文档归属时，按以下顺序处理，**禁止自行猜测乱放**：

1. 先读 `项目全局规则.md` §2.4 与相关目录 README，确认目录定位；
2. 仍不确定 → 对照 §4.2 关键字 → 文件夹映射与文档实际内容判断性质；
3. 判断为"功能设计类"的歧义文档 → 默认放 `80-功能设计/` 对应分组（拿不准分组先放 03-平台基础 并标注待归组）；
4. 仍无法判定 → **必须询问用户**，不得擅自创建新目录或跨目录存放。

#### 2.4.6 功能设计管理规范（80-功能设计/ 专属）

> 本规范为宪法级总纲，操作细则由 80-功能设计/README.md 承载（11 章模板全文）。

- **中枢定位**：`80-功能设计/` 是功能设计唯一权威存放处（功能总览地图见 README.md），一个功能一个文档，命名 `{功能名}-功能设计-V{N}.md`；
- **分组规则**：按端/模块分组——`01-系统管理/`（基础资料管理、工作流管理，日志、收费等模块待有文档后新增）、`02-审核员端/`、`03-平台基础/`（YZH 框架、AI 引擎、前端架构等底层支撑）；逻辑必须清晰，新增模块先建分组再放文档；
- **文档先行**：进入实施前必须完成对应功能设计文档并评审通过，**文档未完成不得实施**；
- **11 章模板**：所有功能设计文档必须按 README.md「四、文档模板」编写（功能概述/业务背景与目标/前置条件/业务规则/数据设计/接口设计/数据流与时序/边界与异常处理/测试要点/TODO 执行计划/章节变更记录），不得增删章节；
- **状态机**：文头状态取值唯一：`设计稿 → 评审中 → 实施中 → 已实施 → 已废弃`；TODO 全部完成才可标"已实施"；
- **实施同步**：实施后逻辑变化必须同步更新对应功能设计文档（状态头注明修订日期）；功能完成后文档留在本目录作为权威说明，不归档。

---

## 四、AI 文档检索协议

### 4.1 版本号规则

```
V1.0 → V2.0 → V3.0   （主版本：重大修订、推翻重写）
V1.1 → V1.2 → V1.3   （次版本：补充细节、修正错误）
```

### 4.2 过期文档处置

```
当文档被新版本替代时：
  旧版本 → 移入 docs/90-归档/旧版本/（按归档日期子文件夹或扁平存放，不分主题）
  源文件夹只保留最新版

原则：
  - 不删除任何文档——过期文档是思想碰撞的结果，有保留价值
  - 90-归档/ 只进不出，不做二次整理
```

### 4.3 版本号在文档中的位置

每份成熟态文档的文件头必须包含：

```markdown
> **版本**：V1.0 | **日期**：YYYY-MM-DD | **状态**：成熟态
```

萌芽态文档不需要版本号，只需要日期。

---

## 五、文档验收规则

### 5.1 开发中的文档验证

```
每个模块开发完成后：
  [ ] 对照文档逐项检查——文档中列出的功能点是否都已实现
  [ ] 发现实现与文档不一致 → 评估是文档错了还是代码错了
  [ ] 文档错了 → 更新文档（先改文档，再改代码）
  [ ] 代码错了 → 改代码
  [ ] 实现中有文档未覆盖的发现 → 补充到文档
```

### 5.2 文档质量检查清单

```
每份成熟态文档在定稿前：
  [ ] 文件头包含版本号、日期、状态
  [ ] 与其他文档无矛盾（交叉引用已验证）
  [ ] 量化结论有数据支撑（非"估计"、"大概"）
  [ ] 对齐项目全局规则（命名、路径、技术栈）
  [ ] 足够详细，可直接指导开发或 AI 生成代码
```

---

## 六、源代码目录结构

> **强制规范**：前后端代码结构必须遵守 `docs/30-项目规则/前后端代码结构统一规则-V1.md`，包括三层同构、命名约定、禁止扁平化等约束。迁移完成后按该文档 §六 检查清单逐项验证。

### 6.1 顶层结构

```
体系认证平台/                         ← Git 仓库根
├── 项目全局规则.md                    ← 项目宪法（项目概述/技术栈/端口规划/快速开始）
├── AGENTS.md                          ← AI 编程助手自动加载入口（编码必须启用，见 §8.0）
├── CertPlatform.sln                   ← 总解决方案（聚合 certplatform-api 4 工程 + yzh-core 4 工程）
├── DB/mysql/                          ← 历史 SQL 脚本（只读参考，新脚本一律进 scripts/db/）
│
├── src/                              ← 源代码（按端拆分）
│   ├── README.md                     ← 子项目一览 + 关键词索引
│   ├── yzh-core/                     ← ★ 框架层（受控修改，见 §0.1）
│   │   ├── YZH.Core.Stand/           ← 标准层：实体基类/契约模型/工具（BaseEntity、FilterRequest、ApiResponse）
│   │   ├── YZH.Core.DataBase/        ← 数据访问层：SqlSugar ORM 实现
│   │   ├── YZH.Core.Api/             ← 能力层：控制器基类/过滤器/EntityService
│   │   ├── YZH.Core.Web/             ← 开发期聚合宿主（唯一启动入口，extern alias 桥接业务工程）
│   │   └── YZH.Core.Api.Tests/       ← 框架层单元测试
│   ├── certplatform-api/             ← 新后端架构（V6，2026-09 起；业务层，默认开发目标）
│   │   ├── Cert.Platform.sln         ← 业务解决方案
│   │   ├── CertPlatform.Shared/      ← 共享层（实体，命名空间 YZH.Entity.Admin.Platform.*）
│   │   ├── CertPlatform.Admin/       ← 管理员端
│   │   ├── CertPlatform.Auditor/     ← 审核员端
│   │   └── CertPlatform.Enterprise/  ← 企业端
│   ├── certplatform-web/             ← 新前端架构（V4，2026-09 起）
│   │   ├── yzh.vue.core/             ← 第 1 层：系统底座 npm 包（五原子：原子组件/方法/API/路由/界面；system 11 页 + 登录/布局/首页 + api/system 均在此）
│   │   ├── cert/cert-share/          ← 第 2 层：admin+auditor 共用的业务逻辑、API、类型
│   │   ├── cert/cert-admin/          ← 第 3 层：管理员端（端口 9990，Element Plus；只组装 core 五原子 + 业务页）
│   │   ├── cert/cert-auditor/        ← 第 4 层：审核员端（端口 9991，Element Plus）
│   │   ├── cert/cert-enterprise/     ← 企业端（预留）
│   │   └── package.json              ← workspace 根配置
│   └── old/                          ← ⛔ 历史代码（禁止修改，仅参考）
│       ├── server/Vue.NetCore/       ← 旧 Vol 框架后端（vol.api）+ 旧前端（vol.web）
│       └── auditor/                  ← 旧审核员前端（已归档）
│
├── docker/                           ← Docker Compose 配置（MySQL 8.0 + Redis 7 + MinIO + anydoc/libreoffice）
│   ├── compose.yml                   ← 服务编排
│   └── start.sh / stop.sh / status.sh ← 数据库/缓存启停脚本
│
├── docs/                             ← 项目文档（00/10/20/30/40/50/90，见 §3）
├── scripts/                          ← 运维/工具脚本
│   ├── backend/                      ← 后端启停（run-backend.sh / stop-backend.sh / restart-backend.sh，唯一标准）
│   ├── frontend/                     ← 前端启停（start.sh admin|auditor|all / build.sh）
│   ├── db/                           ← ★ 新架构 SQL 脚本（NNN-描述_V1.sql，见 §11.1）
│   └── storage/ generate/ tools/     ← 其他工具脚本
└── 输出产物/                          ← 系统输出目录（临时交付物，已 gitignore）
```

### 6.2 端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | `9992` | YZH.Core.Web（新架构宿主，启动脚本 `scripts/backend/`） |
| 后台管理前端 | `9990` | cert-admin（Element Plus） |
| 审核员前端 | `9991` | cert-auditor（Naive UI） |
| MySQL | `3307` | Container yzh-mysql → 宿主机映射 |
| Redis | `6380` | Container yzh-redis → 宿主机映射 |
| MinIO API | `9000` | Container yzh-minio → 宿主机映射 |
| MinIO Console | `9001` | Container yzh-minio → 宿主机映射 |

### 6.3 AI 检索路径

```
用户提出代码级需求
    ↓
AI 识别涉及哪个端（yzh-core 框架层 / certplatform-api 业务层 / certplatform-web 前端）
    ↓
读取对应 src/ 子目录 README.md → 获取技术栈、启动方式
    ↓
查阅 docs/ 对应设计文档 → 获取业务约束
    ↓
查阅代码架构规范：
  - 框架层（YZH）：docs/10-YZH架构/02-后端架构.md、03-前端架构.md
  - 编码规范：C#/Vue/TS 编码规范已归档（Vol 专属），新架构参考 `docs/10-YZH架构/02-后端架构.md`（C#）与 `docs/10-YZH架构/03-前端架构.md`（Vue/TS）
  - 代码模板：YZH 编码示例参考 `docs/10-YZH架构/` 各章节；旧 Vol 代码模板已归档
  - 常见错误：docs/10-YZH架构/08-常见错误与修复.md
    ↓
生成代码
```

### 6.4 子项目 README 规范

每个 `src/` 下的子目录必须包含 `README.md`，包含：
1. 技术栈（框架、语言、主要依赖）
2. 启动命令
3. 目录结构说明
4. 关键词索引（供 AI 检索）

### 6.5 快速开始

> 后端启停一律走 `scripts/`（见 §十五），禁止裸 `dotnet run &`。前端启停走 `scripts/frontend/`。

```bash
# 1. 启动数据库和缓存
cd docker && bash start.sh

# 2. 导入数据库表结构（首次）
docker exec -i yzh-mysql mysql -uroot -pYzh123456. yzh_cert_platform \
  < src/certplatform-api/DB/mysql/mysql表结构与表数据.sql

# 3. 启动后端（新架构）
cd src/certplatform-api
dotnet run --project YZH.Core.Web

# 或旧架构：
# ./scripts/backend/restart-backend.sh

# 4. 启动前端（新架构）
./scripts/frontend/start.sh all start
# 或单独启动：
#   ./scripts/frontend/start.sh admin start
#   ./scripts/frontend/start.sh auditor start

# 5. 构建生产版本
./scripts/frontend/build.sh all
```

**访问地址**：
- 后端 API：http://127.0.0.1:9992/
- 管理员端：http://127.0.0.1:9990/
- 审核员端：http://127.0.0.1:9991/

**新后端架构说明**：
- 位置：`src/certplatform-api/`
- 结构：`CertPlatform.Shared/`（共享） + `CertPlatform.Admin/` + `CertPlatform.Auditor/` + `CertPlatform.Enterprise/`
- **启动项目**：`src/yzh-core/YZH.Core.Web`（Program.cs 调用 `UseYzhCore`）
- **启动方式**：`./scripts/backend/run-backend.sh`（推荐） 或 `./scripts/backend/restart-backend.sh`
- **禁止**：裸 `dotnet run &`（会被工具清理子进程）
- 旧 vol.web 保留在 `src/server/Vue.NetCore/`，待新架构稳定后清理

---

## 七、AI 行为规范（从用户画像提炼）

### 7.0 编码必须启用 AGENTS.md（新增 2026-08-16）

- **强制要求**：所有 AI 编程助手（Cursor / Claude Code / Copilot / Aider 等）在**编码任务（生成/修改任何代码）时必须启用并遵循**根目录 `AGENTS.md`；非编码类任务（纯文档/问答）不受限，但建议一并加载以获得项目指针。
- **启用方式**：
  - Claude Code / Cursor / Copilot / Aider：按工具官方机制自动加载根目录 `AGENTS.md`（部分工具需在设置中开启 Agents/自定义指令读取）；
  - 手动会话：编码前先读取根目录 `AGENTS.md`，按其"快速指针"逐级加载 项目全局规则.md → 00-工程体系/README.md → 业务域设计文档 → YZH-知识库；
- **适用范围**：`src/` 全部代码（后端 server / 前端 admin / auditor / auditor-app 预留）与 `scripts/` 脚本，含新增、修改、重构、排错。
- **与知识库的关系**：`AGENTS.md` 是"入口/指针 + 编码约束"，`docs/30-项目规则/知识库/` 是"知识底座"（Vol 能力清单 / YZH 增量 / 边界约束 / 代码模板 / 踩坑记录）。编码时必须结合两者：先按 AGENTS.md 对齐约束，再查知识库获取接口签名与踩坑经验。
- **同步要求**：根目录 `AGENTS.md` 为权威源，`docs/30-项目规则/知识库/AGENTS.md` 为知识库副本（供检索），两处必须保持一致；修改任一处须同步另一处。
- **违反后果**：AI 未加载 AGENTS.md 直接生成代码，视为违反宪法级流程，所生成代码需按 §7.3 检索路径重做对齐。

### 7.1 沟通规范

| 场景 | 允许 | 禁止 |
|------|------|------|
| 代码生成 | 文件路径 + 完整代码块 | 解释性文字 > 3 行 |
| 方案选择 | 表格对比 + 结论 | 段落式分析 |
| 错误处理 | 错误原因 + 修复方案 | "让我来帮您..." |
| 需求确认 | 1-2 个关键问题 | "让我先了解一下您的需求..." |
| 文档更新 | diff 格式或完整替换 | "建议修改..." |

### 7.2 思维模式

```
发散阶段（想法期）：
  - 天马行空，快速记录，不设边界
  - 允许模糊、矛盾、重叠
  - 文档状态：萌芽态
  - AI 角色：帮我把一闪而过的念头记下来，不做评判

收敛阶段（行动期）：
  - 聚焦具体模块，深挖细节
  - 对齐宪法，消除矛盾，补充遗漏
  - 文档状态：成熟态
  - AI 角色：帮我审视是否遗漏、是否对齐宪法、是否有风险
```

### 7.3 代码结构约束（2026-09-09 新增）

> **强制规范**：所有代码文件组织必须遵守 `docs/30-项目规则/前后端代码结构统一规则-V1.md`。

| 约束 | 说明 |
|------|------|
| **三层同构** | Controller 路径 = API 文件路径 = Pages 路径 |
| **禁止扁平化** | 新文件必须进入对应子目录，禁止放在模块根目录 |
| **命名一致** | Controller 类名主体 = API 文件名 = Pages 文件夹名 |
| **EntityConfig 分模块** | 禁止在 `Assets/EntityConfigs/` 根目录直接放 JSON |
| **迁移检查** | 完成后按规则文档 §六 全部检查清单逐项验证 |
| **新模块三层同步** | 创建 Controller 时必须同时创建 API + Pages |

### 7.4 规则观

- 规则是**喂给 AI 的饲料**，不是约束人的枷锁
- 规则必须**机器可解析**——AI 能直接照做的才值得写
- "代码风格一致"不如"所有 API 返回 `ApiResult<T>`"
- 规则体系的核心用途：拦住 AI 的随机性，而非教育团队成员

### 7.5 技术决策偏好

| 倾向 | 回避 |
|------|------|
| 单体架构优先（非微服务） | Kubernetes、微服务拆分 |
| 成熟框架（Vol / ABP） | 自己造轮子 |
| 代码生成器 > 手写 CRUD | 重复搬砖 |
| 约定大于配置 | 过度灵活的配置体系 |
| 2 人能维护的技术栈 | 需要大团队才能玩转的架构 |
| 量化评估（QPS/数据量/成本） | 凭感觉拍板 |
| 先做核心闭环，再扩展 | 一开始就做大而全 |

### 7.6 常见误区（AI 应避免）

| 错误行为 | 为什么错 | 正确做法 |
|---------|---------|---------|
| 用户提一个模糊想法，立刻开始做架构设计 | 还在发散阶段 | 帮我记录下来，追问 1-2 个关键问题即可 |
| 长篇解释一个简单的技术决策 | 不需要被教育 | 直接说结论 + 引用宪法对应条目 |
| 用户说"先记下来"，AI 开始深挖细节 | 在推迟决策 | 记下来，标记状态，不展开 |
| 主动提议引入新的技术栈 | 宪法已锁定技术栈 | 除非当前技术栈实在无法满足，否则不提 |
| 过度解释工具调用过程 | 只需要结果 | 只说"已完成 X"或"遇到问题 Y" |
| 用表情符号、网络流行语 | 偏好专业克制 | 零表情，极简表达 |

---

## 八、代码架构规范（新增 2026-08-25）

### 8.1 后端架构

> 详见 `docs/30-项目规则/backend-architecture-guide.md`

**核心原则**：
- `CertPlatform.Shared` = 共享层（实体/控制器/服务，跨角色复用）
- `CertPlatform.Admin` = 管理员端业务
- `CertPlatform.Auditor` = 审核员端业务
- `CertPlatform.Enterprise` = 企业端业务（Phase 2+）
- `YZH.Core.Web` = 框架层（唯一启动入口）

**代码存放规则**：
- 业务实体 → `CertPlatform.Shared/Entities/`
- 共享控制器 → `CertPlatform.Shared/Controllers/`
- 共享服务 → `CertPlatform.Shared/Services/`
- 管理员端控制器 → `CertPlatform.Admin/Controllers/`
- 审核员端控制器 → `CertPlatform.Auditor/Controllers/`
- 企业端控制器 → `CertPlatform.Enterprise/Controllers/`

**GridConfig 配置**：
- 框架默认 → `YZH.Core.Web/Assest/GridConfigs/`
- 项目专属 → `certplatform-api/Assest/GridConfigs/`

### 8.2 前端架构

> 详见 `docs/30-项目规则/frontend-architecture-guide.md`

**核心原则**：
- `yzh.vue.core/` = 核心架构层（业务无关，可跨项目复用）
- `share/` = 业务共享层（认证平台业务，跨角色复用）
- `admin/` = 管理员端页面（仅管理员可见）
- `auditor/` = 审核员端页面（仅审核员可见）

**代码存放规则**：
- 通用组件 → `yzh.vue.core/src/components/`
- 业务组件 → `share/src/components/`
- 管理员页面 → `admin/src/pages/`
- 审核员页面 → `auditor/src/pages/`

**路由配置**：
- 管理员端：`admin/src/router/index.ts`
- 审核员端：`auditor/src/router/index.ts`

### 8.3 新功能开发检查清单

**开发前**：
- [ ] 确认功能属于哪个业务子模块
- [ ] 确认是否需要新的 Repository/Service（后端）
- [ ] 确认是否需要新的组件（前端）

**开发中**：
- [ ] 使用正确的命名空间和引用路径
- [ ] 代码放在正确的目录

**开发后**：
- [ ] 编译通过（0 errors）
- [ ] 更新相关 README 文档

---

## 九、禁止事项

| 禁止 | 原因 |
|------|------|
| 在 docs/ 根目录放置文档 | 违反分层原则，AI 无法快速定位 |
| 同一份文档在多个文件夹中出现 | 违反单一事实来源原则 |
| 文档与代码不一致却不更新 | 文档失效，AI 失去锚点 |
| 删除旧版本文档（不移入历史文档/） | 丢失思想碰撞记录 |
| 用"待补充"、"TODO"敷衍关键章节 | 关键信息缺失导致开发阻塞 |
| 文档中引用未创建的文档 | 引用链断裂 |
| 直接修改 Vol 框架源码 | Vol 升级时 YZH 全部编译失败 |
| 调用未实现的 YZH 组件 | 编译错误，应使用 Vol 原生方案 |
| 用 EFsql 替代 EF LINQ | 绕过钩子体系，审计/权限过滤失效 |

---

## 十、文档归档记录

> **状态**：本章所列归档项已于 2026-08-10 全部执行完毕，对应文档均已移入 `docs/90-归档/旧版本/`。此处仅保留归档依据备查。

### 1.1 已过时的调研文档（原 10-调研与可行性/ 整个目录）

| 文档 | 原因 |
|------|------|
| 01-竞品调研报告-V1.md | 历史使命已完成 |
| 02-深度调研与入局可行性分析-V1.md | 结论已吸收 |
| 03-技术难点分析与MVP规划-V1.md | 已被总体设计-V3 取代 |
| 04-行业调研与核心技术方案-V1.md | 结论已吸收 |

### 2.1 已过时的架构决策（20-架构决策/ 中的过期文档）

| 文档 | 原因 |
|------|------|
| 已确认架构决策汇总-V1.0.md | Flutter/PostgreSQL/自建AI引擎等决策已过时 |
| 系统定位与演进路线-V1.md | "审核员个人工具"定位已取代 |
| 产品矩阵与远景规划-V1.0.md | Flutter移动优先策略已过时 |
| 核心任务确认与总体设计策略-V1.0.md | 原则已吸收 |
| 核心流程穿透评审-V1.0.md | 评审基线已过时 |
| 05-技术架构设计与ACM分析-V1.md | 技术架构已过时 |
| 06-NETCore迁移与工作流移动端设计-V1.md | 移动端设计已不适用 |
| AI工程化分析-体系认证平台.md | 自建AI方案已推翻 |
| 头脑风暴-2026-07-27-成果汇总.md | 讨论记录 |
| 原型-prototype.html | 原型已过时 |

### 3.1 已过时的范围边界文档（30-范围与边界/ 整个目录）

| 文档 | 原因 |
|------|------|
| 07-系统边界收敛与MVP精简-V1.md | 边界原则已吸收 |
| 08-系统遗漏分析与输入精简-V1.md | 分析已覆盖 |
| 11-全局遗漏排查与开发建议-V1.md | 排查已覆盖 |

### 4.1 已过时的规划文档

| 文档 | 原因 |
|------|------|
| 14-终审设计总结与优先级矩阵-V1.md | 优先级已更新 |

### 5.1 已过时的 AI 工程设计文档

| 文档 | 原因 |
|------|------|
| 17-AI工程设计方法论-V1.md | 技术栈假设（PostgreSQL/VOL/UniApp/Elsa）已过时 |

### 6.1 应移回主目录的文档

| 文档 | 目标位置 | 原因 |
|------|---------|------|
| 总体设计-V3.md | docs/90-归档/旧版本/ | 最新版总体设计，应作为活跃文档 |

---

## 十一、SQL 脚本管理规范（原十一）

### 11.1 存放位置

```
所有项目相关的 SQL 脚本必须存放于：
scripts/db/

禁止位置：
❌ docs/90-归档/旧版本/sql/（仅作为历史备份）
❌ 根目录或其他散落位置
```

> **★ 字符集强制要求（2026-09-23 新增，详见 §16.10 铁律八）**：
> 所有建表语句**必须显式**写 `COLLATE=utf8mb4_general_ci`；含 `CREATE VIEW` 的脚本
> **必须**在开头写 `SET NAMES utf8mb4 COLLATE utf8mb4_general_ci;`。
> ⛔ 只写 `DEFAULT CHARSET=utf8mb4` 会落到 `utf8mb4_0900_ai_ci`（**不是**库默认值），
> 与库内既有表不一致，跨表关联即报 `ERROR 1267 Illegal mix of collations`。

### 11.2 数据库连接信息

| 环境 | 主机 | 端口 | 数据库 |
|------|------|------|--------|
| 开发 | localhost | **3307** | yzh_cert_platform |
| 缓存 | localhost | **6380** | Redis |

> ⚠️ **注意**：MySQL 使用非标准端口 3307（Docker 映射）

### 11.3 脚本命名规则

```
格式：cert_phase{N}_{功能描述}.sql

示例：
- cert_phase1_user_extension.sql    (Phase 1 - 用户表扩展)
- cert_phase2_data_dictionary.sql   (Phase 2 - 数据字典)
- cert_platform_tables_v2.1.sql     (通用 - 表结构)
```

### 11.4 执行顺序要求

```
必须按以下顺序执行（有依赖关系）：

1. cert_phase1_user_extension.sql      → 用户表扩展
2. cert_platform_tables_v2.1.sql        → 业务表结构
3. cert_phase1_exec.sql                 → Phase 1 数据初始化
4. cert_phase1_permissions.sql          → 权限配置
5. cert_platform_menu_init.sql          → 菜单初始化
6. cert_phase2_data_dictionary.sql      → 数据字典（Phase 2）
7. cert_phase2_test_data.sql            → 测试数据（Phase 2）
```

### 11.5 幂等性要求

所有 SQL 脚本必须支持重复执行：

```sql
-- ✅ 正确写法
CREATE TABLE IF NOT EXISTS xxx (...);
INSERT IGNORE INTO xxx (...) VALUES (...);

-- ❌ 错误写法
CREATE TABLE xxx (...);  -- 第二次执行会报错
INSERT INTO xxx ...;     -- 重复插入会冲突
```

### 11.6 版本控制

- ✅ SQL 脚本纳入 Git 版本管理
- ✅ 每次修改脚本需更新 README.md 中的说明
- ✅ 重大变更需在脚本头部注释中记录变更日志

---

## 十二、知识库管理规范（原十二）（新增 2026-07-31）

### 12.1 核心原则

```
❌ 遇到问题 → 临时修复 → 忘记 → 下次重复分析（浪费 30 分钟）
✅ 遇到问题 → 根因分析 → 记录知识库 → 永不复现（节省未来 N 小时）
```

**强制要求**：
- 所有已解决的问题必须记录到知识库
- 知识库文档必须包含：错误现象、根因分析、解决方案、预防措施
- AI 对话开始时，必须先查阅相关知识库

### 12.2 知识库文件清单

| 文档 | 路径 | 内容 |
|------|------|------|
| **Vol 框架故障排查手册** | `docs/30-项目规则/vol-framework-troubleshooting.md` | 高频问题及解决方案 |
| SQL 脚本管理 | `src/server/Vue.NetCore/DB/mysql/README.md` | 执行指南与故障排查 |
| 路由配置说明 | `src/server/Vue.NetCore/vol.web/src/router/README.md` | Vol 前端路由机制 |

### 12.3 问题记录模板

每次解决问题后，按以下格式记录：

```markdown
### 问题 XX：[简短标题]

**错误现象**：（复制完整的错误信息）

**触发条件**：什么操作触发了此错误

**根因分析**：（为什么会出现，深层原因）

**解决方案**：（具体代码或步骤）

**预防措施**：如何避免再次发生
```

### 12.4 当前已知高频问题速查

| # | 问题 | 关键词 | 快速修复 |
|---|------|--------|----------|
| 1 | 实体缺少主键 | `requires a primary key` | YZHBaseEntity 添加 `[Key] long Id` |
| 2 | 前端路由 404 | `页面未找到` | viewGird.js 添加路由 + 确保 Vue 文件存在 |
| 3 | 数据字典为空 | 字典不显示 | 执行 cert_phase2_data_dictionary.sql |
| 4 | 循环依赖编译失败 | `circular dependency` | 复制基类到目标项目并改命名空间 |
| 5 | 缺少 using 引用 | `CS0246 未能找到类型` | 查阅 §12.5 命名空间对照表 |
| 6 | JS 语法错误 | `Invalid JS syntax` | 运行 `node -c viewGird.js` 检查 |
| 7 | Vue 文件不存在 | `Failed to resolve import` | 创建组件或移除路由定义 |

### 12.5 常用命名空间速查表（C# 后端）

| 类型/特性 | 命名空间 | 所在程序集 |
|-----------|----------|-----------|
| `ApiActionPermissionAttribute` | `VOL.Core.Filters` | VOL.Core |
| `SaveModel` / `PageDataOptions` / `PageGridData<T>` | `VOL.Entity.DomainModels.Core` | VOL.Entity |
| `BaseDbContext` / `RepositoryBase<T>` / `ServiceBase<T>` | `VOL.Core.BaseProvider` | VOL.Core |
| `IDependency` | `VOL.Core.Extensions.AutofacManager` | VOL.Core |
| `YZHBaseEntity` | `VOL.Entity.CertPlatform` | VOL.Entity（本地） |

### 12.6 新架构命名空间速查表（certplatform-api）

| 类型/特性 | 命名空间 | 所在程序集 |
|-----------|----------|-----------|
| `BaseController<T>` | `YZH.Core.Web.Controllers` | YZH.Core.Web |
| `BaseService<T>` | `YZH.Core.Web.Services` | YZH.Core.Web |
| `BaseRepository<T>` | `YZH.Core.DataBase.Repositories` | YZH.Core.DataBase |
| `BaseEntity` | `YZH.Core.Stand.Models` | YZH.Core.Stand |
| `SysUser` / `SysRole` / `SysMenu` | `YZH.Core.Stand.FrameworkEntities` | YZH.Core.Stand |
| `CertBody` / `ISOStandard` / `ISOClause` | `CertPlatform.Shared.Entities.Cert` | CertPlatform.Shared |
| `AuditTask` | `CertPlatform.Shared.Entities.Audit` | CertPlatform.Shared |
| `CertPlatformModule` | `CertPlatform.Shared` | CertPlatform.Shared |

---

## 十三、多 AI 协作机制（原十三）（独立开发，不适用）

项目当前为独立开发（2026-08-16 起），`多AI协作机制-V1.md` 已删除，本节不再适用。
AI 协作偏好以 `docs/00-工程体系/用户画像与AI协作偏好-V1.md` 为准。

---

## 十四、日志强制规则（原十四）（新增 2026-08-08）

### 14.1 核心原则

```
遇到 Bug → 立即加日志 → 复现问题 → 定位根因 → 修复 → 删除日志
```

**日志是第一优先级操作，不是事后补救！**

### 14.2 强制要求

| 场景 | 日志要求 | 示例 |
|------|----------|------|
| **API 请求** | 请求前打印 URL + 参数 | `console.log('[组件] 📡 POST /api/Xxx/GetPageData, params=', param)` |
| **API 响应** | 响应后打印完整数据 | `console.log('[组件] 📦 API 响应:', JSON.stringify(res))` |
| **数据加载** | 加载前后打印数量 | `console.log('[组件] ✅ 数据加载完成: N 条')` |
| **删除操作** | 删除前打印 ID，响应后打印结果 | `console.log('[组件] 🗑️ 删除 id=xxx, 结果=')` |
| **生命周期** | onMounted/watch 触发时打印状态 | `console.log('[组件] 🔧 onMounted, props.length=N')` |
| **错误捕获** | catch 中必须打印完整 error | `console.error('[组件] ❌ 操作失败:', e)` |

### 14.3 日志格式规范

```javascript
// 格式：[组件名] emoji 描述: 关键信息
// 组件名：文件名或功能名（如 OrgStandard, YzhCrudTable）
// emoji：📡请求 📦响应 ✅成功 ❌失败 ⚠️警告 🌲树 🗑️删除 🔧初始化 👀监听

console.log('[OrgStandard] 🌲 开始加载机构树...')
console.log('[OrgStandard] 📦 机构树 API 响应:', res)
console.log(`[OrgStandard] ✅ 机构树加载完成: ${treeData.value.length} 条`)
console.error('[OrgStandard] ❌ 加载机构树失败', e)
```

### 14.4 日志清理规则

**Bug 修复完成后，必须删除调试日志**：

1. 问题解决 → 标记 TODO 为 completed
2. 删除本次添加的所有 console.log/console.error（除非是永久性日志）
3. 永久性日志仅限：
   - 组件初始化关键状态（onMounted 首次加载）
   - 不可复现的边界情况防御

### 14.5 违规处理

| 违规行为 | 后果 |
|----------|------|
| Bug 修复时未添加日志 | 无法快速定位问题，反复浪费用户时间 |
| Bug 修复后未删除日志 | 生产环境泄露调试信息，影响性能 |
| 日志信息不完整（缺少参数/响应） | 仍需二次排查 |

---

## 十五、自动化开发工作流（原十五）（新增 2026-08-10）

### 15.1 核心原则

```
AI 必须自动化处理开发全流程，禁止反复要求用户手动执行。
用户只需关注业务逻辑，所有机械操作由 AI 自动完成。
```

### 15.2 SQL 脚本自动化

| 步骤 | AI 行为 | 禁止行为 |
|------|---------|----------|
| **创建表** | 自动生成 SQL 并执行 | 不执行仅告知用户 |
| **修改表** | 生成 ALTER 语句并执行 | 让用户手动执行 |
| **字典数据** | 自动插入/更新字典项 | 不处理字典依赖 |
| **菜单数据** | 自动插入 sys_menu 和权限 | 让用户手动配置菜单 |

**执行方式**：
```bash
# 自动从关键信息速查.md 获取连接信息执行
docker exec -i yzh-mysql mysql -uroot -pYzh123456. yzh_cert_platform < script.sql
```

### 15.3 后端自动化

| 阶段 | AI 行为 | 检查点 |
|------|---------|--------|
| **编译前** | 自动检查命名空间、using 语句 | 参考 §12.5 命名空间速查表 |
| **编译中** | 自动执行 `dotnet build` | 捕获所有错误 |
| **编译失败** | **自动修复常见错误**（见下方列表） | 不询问用户直接修复 |
| **运行前** | 自动停止已运行的后端进程 | 避免端口冲突 |
| **运行中** | 自动执行 `dotnet run` | 监控启动日志 |

**新后端架构**（certplatform-api）：
- 解决方案：`src/certplatform-api/Cert.Platform.sln`
- 启动项目：`YZH.Core.Web`
- 共享层：`CertPlatform.Shared/`
- 管理员端：`CertPlatform.Admin/`
- 审核员端：`CertPlatform.Auditor/`
- 企业端：`CertPlatform.Enterprise/`

**自动修复的错误类型**：
1. `CS0234` 命名空间不存在 → 自动添加正确的 using
2. `CS0246` 类型找不到 → 检查命名空间并修正
3. `CS0108` 隐藏继承成员 → 添加 `new` 关键字
4. 缺少程序集引用 → 检查项目引用关系

#### 15.3.1 后端重启标准操作（唯一标准，禁止自由发挥）

> `scripts/backend/` 提供三个标准脚本（`run-backend.sh` / `stop-backend.sh` / `restart-backend.sh`），
> 详细说明见 `scripts/README.md`。**所有后端启停必须走这三个脚本，禁止手动拼 `kill` / `nohup dotnet run` 命令。**

| 场景 | 标准命令 | 说明 |
|------|---------|------|
| **改完代码后重启** | `./scripts/backend/restart-backend.sh` | 停（按进程名）→ 编译 → 后台启动，一条命令完成 |
| **只编译** | `./scripts/backend/run-backend.sh build` | 编译不运行，用于快速验证 |
| **只启动（已编译）** | `./scripts/backend/run-backend.sh run` | 后台启动，3~5 秒就绪 |
| **停止** | `./scripts/backend/stop-backend.sh` | 按进程名过滤 dotnet/VOL.WebApi 关闭，不依赖端口 |
| **查状态** | `./scripts/backend/run-backend.sh status` | 显示是否在运行 + PID |

**停止策略（关键）**：
- 主手段：按进程名过滤 `VOL.WebApi` / `dotnet run` 直接关闭（SIGTERM → SIGKILL）
- 兜底：PID 文件 `/tmp/vol_backend_9992.pid` + 端口 9992 检查，进程名匹配不到才用端口
- 禁止按端口 `kill $(lsof -ti:9992)` 作为首选——会误杀其他占用端口的进程

**启动策略（关键）**：
- `scripts/backend/run-backend.sh` 内部用 **python3 `os.setsid()`** 让 dotnet 进程脱离当前会话
- **禁止裸 `nohup dotnet run &`**：nohup 只防 SIGHUP，挡不住执行环境在命令返回后对子进程的清理，后端会随命令结束死掉
- 日志 `/tmp/vol_backend_9992.log`，PID `/tmp/vol_backend_9992.pid`，启动后必须验证 `lsof -iTCP:9992` 或 `curl http://localhost:9992/swagger` 返回 200

**执行环境（Codebuff / Cursor / Claude Code 等）注意**：
每次工具调用结束后会清理自身启动的子进程，因此**禁止在工具命令里直接 `dotnet run &`**；
必须调用 `./scripts/backend/run-backend.sh run`（内部已处理会话脱离），并在**下一条独立命令**里验证进程存活 + 端口就绪。

### 15.4 前端自动化

| 阶段 | AI 行为 |
|------|---------|
| **路由添加** | 自动添加到 router/index.ts |
| **图标检查** | 自动验证 @element-plus/icons-vue 导出 |
| **编译检查** | 自动运行 `./scripts/frontend/build.sh` 或检查 Vite 错误 |
| **启动前端** | 自动运行 `./scripts/frontend/start.sh admin start` |

**新前端架构**（certplatform-web）：
- 路由位置：`src/certplatform-web/{role}/src/router/index.ts`
- 页面位置：`src/certplatform-web/{role}/src/pages/{module}/`
- 共享组件：`src/certplatform-web/share/src/components/`
- 核心组件：`src/certplatform-web/yzh.vue.core/src/components/`

**新后端架构**（certplatform-api）：
- 解决方案：`src/certplatform-api/Cert.Platform.sln`
- 启动项目：`YZH.Core.Web`
- 共享层：`CertPlatform.Shared/`
- 管理员端：`CertPlatform.Admin/`
- 审核员端：`CertPlatform.Auditor/`

### 15.5 完整开发流程（AI 自动执行）

```
用户：开发新功能 X

AI：
  1. 读取设计文档
  2. 【自动】创建后端实体
  3. 【自动】创建数据库表 SQL
  4. 【自动】执行 SQL 脚本
  5. 【自动】创建 Service 和 Controller
  6. 【自动】编译后端
     ├─ 失败 → 自动修复错误 → 重新编译
     └─ 成功 → 继续
  7. 【自动】停止旧进程 → 启动新进程
  8. 【自动】创建前端页面和组件
  9. 【自动】添加路由
  10.【自动】检查图标有效性
  11.【自动】验证前端编译
  12. 报告完成状态

用户只需：测试功能 + 反馈问题
```

### 15.6 错误处理策略

| 错误类型 | AI 行为 | 是否询问用户 |
----------|---------|-------------|
| 命名空间错误 | 自动修正 using | 否 |
| 类型找不到 | 自动搜索并添加引用 | 否 |
| 数据库连接失败 | 检查容器状态并报告 | 是 |
| 编译逻辑错误 | 分析并尝试修复 | 是（复杂时）|
| 运行时异常 | 添加日志并分析 | 是 |

### 15.7 禁止行为清单

AI **绝对禁止**以下行为：
- ❌ "请手动执行 SQL 脚本"
- ❌ "请手动重启后端"
- ❌ "请手动停止进程"
- ❌ 反复要求用户发送相同错误信息
- ❌ 只给出修复建议而不执行修复

---

## 十六、数据库-实体命名铁律（2026-09-13 新增，最高优先级）

> **⚠️ 本章是解决历史混乱的根治规则。从此以后，所有新表、新实体必须严格遵守本章规范。**
> **违反本章规范的代码不允许合入。**

### 16.1 根因回顾

项目历史上存在两套 ORM（EntityFramework + SqlSugar）各自独立定义审计字段，导致：
- **审计字段名不统一**：EF 用 `Creator/CreateDate`，SqlSugar 用 `CreateBy/CreateTime`
- **业务字段命名不统一**：部分表 snake_case（`cb_code`），部分 PascalCase（`CbCode`）
- **ORM 映射缺失**：Entity 用 PascalCase 属性，但数据库列名是 snake_case，缺少 `[Column]`/`[SugarColumn]` 映射

**结论：从此以后，统一为一套规范，不再区分 ORM。**

### 16.2 铁律一：审计字段统一命名（所有新表/新实体必须遵循）

| 用途 | 统一字段名 | DB 列名 | 类型 | 说明 |
|------|----------|---------|------|------|
| 主键 | `Id` | `Id` | `long` | 自增主键 |
| 业务码 | `Code` | `Code` | `string(36)` | GUID，全局唯一关联键 |
| 机构码 | `OrgCode` | `OrgCode` | `string(50)` | 多租户隔离 |
| 创建人 | `CreateBy` | `CreateBy` | `string(50)` | 登录人 Code |
| 创建时间 | `CreateTime` | `CreateTime` | `DateTime` | UTC |
| 更新人 | `UpdateBy` | `UpdateBy` | `string(50)` | 登录人 Code |
| 更新时间 | `UpdateTime` | `UpdateTime` | `DateTime?` | UTC |
| 删除人 | `DeleteBy` | `DeleteBy` | `string(50)` | 软删除操作人 |
| 删除时间 | `DeleteTime` | `DeleteTime` | `DateTime?` | 软删除时间 |
| 软删除 | `IsDeleted` | `IsDeleted` | `bool` | false=未删, true=已删 |
| 有效标志 | `IsValid` | `IsValid` | `int` | 1=有效, 0=无效 |
| 排序 | `Sort` | `Sort` | `int` | 默认 0 |
| 备注 | `Remark` | `Remark` | `string(500)` | 可空 |
| 状态 | `Status` | `Status` | `string(50)` | 业务状态（active/inactive 等） |

**禁止使用的历史字段名**（遇到必须改为上表标准）：
- `Creator` → 改为 `CreateBy`
- `CreateDate` → 改为 `CreateTime`
- `Modifier` → 改为 `UpdateBy`
- `ModifyDate` → 改为 `UpdateTime`
- `Deleter` → 改为 `DeleteBy`
- `CreateID`/`ModifyID`/`DeleteID` → 废弃（整数型审计字段不再使用）
- `Enable` → 改为 `IsValid`（或保留为业务字段，不与审计混淆）
- `status` (小写) → 改为 `Status` (PascalCase)

### 16.3 铁律二：业务字段命名 = PascalCase，DB 列名 = C# 属性名

```
规则：C# 属性名 == DB 列名 == PascalCase
禁止：snake_case 列名（如 standard_code、cb_code）
例外：历史遗留表可通过 ALTER TABLE 迁移，迁移前用 [Column]/[SugarColumn] 映射
```

**示例**：
```csharp
// ✅ 正确：DB列名 = C#属性名 = PascalCase
public string StandardCode { get; set; }
public string StandardName { get; set; }
public int VersionYear { get; set; }

// ❌ 错误：DB列名是 snake_case，需要映射
[Column("standard_code")]  // ← 这是历史债务，新代码禁止
public string StandardCode { get; set; }
```

### 16.4 铁律三：ORM 映射规则

#### SqlSugar（YZH.Core 框架，新实体首选）
```csharp
// 当 DB 列名 ≠ C# 属性名时（历史遗留表），必须加 [SugarColumn(ColumnName = "xxx")]
[SugarColumn(ColumnName = "standard_code")]
public string StandardCode { get; set; }

// 当属性不需要持久化时，必须加 [SugarColumn(IsIgnore = true)]
[SugarColumn(IsIgnore = true)]
public string? ParentCode { get; set; }
```

#### EntityFramework（Vol 框架，历史实体）
```csharp
// 当 DB 列名 ≠ C# 属性名时，必须加 [Column("xxx")]
[Column("standard_code")]
public string StandardCode { get; set; }

// 当属性不需要持久化时，必须加 [NotMapped]
[NotMapped]
public string? ParentCode { get; set; }
```

### 16.5 铁律四：继承基类审计字段时的 `new` 关键字规则

```
当子类需要覆盖基类同名属性时：
1. 使用 new 关键字隐藏基类属性
2. 基类属性有 [SugarColumn(IsIgnore = true)] 时，子类的 new 属性会自动覆盖 IsIgnore
3. 子类不需要重复加 [SugarColumn(IsIgnore = true)]（除非需要不同的 IsIgnore 值）
```

### 16.6 铁律五：新建表/实体的检查清单

新建任何表和实体前，必须逐项检查：

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 1 | 表名 | snake_case，前缀按域划分（`cert_`/`audit_`/`ent_`/`wf_`/`rpt_`） |
| 2 | 审计字段 | 完全遵循 §16.2 表格，字段名/类型一一对应 |
| 3 | 业务字段名 | PascalCase，DB 列名 = C# 属性名 |
| 4 | ORM 映射 | 无 `[Column]`/`[SugarColumn(ColumnName)]` 映射（因为 DB=属性名） |
| 5 | 虚拟字段 | `[SugarColumn(IsIgnore = true)]` 或 `[NotMapped]` |
| 6 | JSON EntityConfig | 属性名 = C# 属性名（PascalCase），`DataKey`→`DictCode` 已废弃 |
| 7 | 前端 TS ColumnConfig | camelCase，来自 DTO 的 `EntityConfigDto` |
| 8 | 主键 | `Id` (long, 自增)，`Code` (string(36), GUID) |
| 9 | 外键命名 | `{关联表名}Code`，如 `StandardCode`、`PhaseCode` |
| 10 | 唯一约束 | `[UniqueField("显示名")]` 或 DB UNIQUE KEY |

### 16.7 铁律六：历史遗留表迁移策略

对于已存在的 snake_case 列名表（如 `cert_iso_standard` 的 `cb_code`/`standard_code`），采取以下策略：

**方案A（推荐）：ALTER TABLE 重命名列为 PascalCase**
```sql
ALTER TABLE cert_iso_standard 
  CHANGE COLUMN cb_code CbCode varchar(50),
  CHANGE COLUMN standard_code StandardCode varchar(50),
  CHANGE COLUMN standard_name StandardName varchar(200),
  CHANGE COLUMN version_year VersionYear int,
  CHANGE COLUMN parent_code ParentCode varchar(100),
  CHANGE COLUMN is_leaf IsLeaf tinyint(1);
```
执行后：DB 列名 = C# 属性名，无需任何映射注解。

**方案B（过渡期）：保留 snake_case 列名，加 ORM 映射**
```csharp
[SugarColumn(ColumnName = "cb_code")]
public string CbCode { get; set; }
```
此方案为过渡方案，后续应迁移到方案A。

### 16.8 违规示例与修复

| 违规 | 修复 |
|------|------|
| Entity 用 `Creator` | 改为 `CreateBy` |
| Entity 用 `CreateDate` | 改为 `CreateTime` |
| DB 列名 `standard_code`，Entity 用 `StandardCode` 无映射 | 加 `[SugarColumn(ColumnName = "standard_code")]` 或 ALTER TABLE |
| `[SugarColumn(IsIgnore = true)]` 不生效 | 检查 `DbContextFactory.ConfigureExternalServices` 是否识别 SqlSugar 属性 |
| JSON EntityConfig 用 `DataKey` | 改为 `DictCode` |
| JSON EntityConfig 属性名是 camelCase | 改为 PascalCase（与 C# 属性名一致） |

### 16.9 铁律七：DB 列名 / C# 属性名 / JSON 字段名「原样一致」，禁止局部改写（2026-09-13 新增）

> **⚠️ 本条为强制约束，优先级高于任何"规范化""美化""修正拼写"的冲动。**

**现状前提**：项目已**刻意**把数据库列名与 C# 实体属性统一为 **PascalCase**（如 `OrgCode`、`CreateTime`、`IsValid`）。
这是**有意设计的结果，不是历史遗留**，任何模块都不需要、也不允许再单独调整字段的拼写规则。

**三处必须逐字段逐一对应，字符完全一致（含大小写）：**

| 位置 | 载体 | 示例 |
|------|------|------|
| ① 数据库 | 表列名 | `OrgCode` / `CreateTime` / `IsValid` |
| ② 后端 | C# 实体属性名 | `public string? OrgCode { get; set; }` |
| ③ 配置 | JSON EntityConfig 字段名 | `"FieldName": "OrgCode"` |

> **③′ 前端（2026-09-22 补充）**：Vue/TS 侧读取同一实体时，字段名必须与 ①②③ 逐字一致（`row.OrgCode`，不得写 `row.orgCode`）。
> 这是"3 类字段（DB / C# / TS）编写完全统一"的落地要求——前端出现 camelCase 读取即视为违规，
> 典型故障是树/表格渲染成空行且无报错（2026-09-22 NC 规则设计页问题一即为此类）。
> 运行时防御：通用组件应对 `textField`/`codeField` 做契约校验，字段缺失时显式报错而非静默空渲染。

**强制规则**：

1. **原样输出**：新增实体/字段时，DB 列名、C# 属性名、JSON 字段名三者必须**逐字一致**，不得出现任何转换（不加下划线、不加前缀、不改大小写）。
2. **禁止二次改名**：已存在的 PascalCase 列名**不得**因为"命名风格""可读性""与某张表对齐"等理由被 `ALTER TABLE ... CHANGE COLUMN` 改写。表结构变更仅允许出于**功能需要**（新增字段、改类型、加索引、加约束）。
3. **禁止局部加映射**：三处一致时**不写** `[SugarColumn(ColumnName=...)]` / `[Column(...)]`。只有当三者中已存在事实差异、且暂时无法统一时，才允许以映射注解过渡（见 §16.7 方案 B），并在注释中标明待迁移。
4. **禁止按模块自定风格**：不允许"这个模块用 camelCase、那个模块用 snake_case"。全项目只有一种风格——PascalCase。
5. **JSON 是投影，不是新定义**：`EntityConfig` 的 `FieldName` 必须从 C# 实体属性名复制，**不允许独立发明字段名**。

**检查清单（新增/修改字段时逐项确认）**：

| # | 检查项 | 通过标准 |
|---|--------|---------|
| 1 | DB 列名 | PascalCase，与 C# 属性逐字一致 |
| 2 | C# 属性名 | 与 DB 列名逐字一致，无映射注解 |
| 3 | JSON `FieldName` | 与 C# 属性名逐字一致 |
| 4 | 是否改动了既有列名 | **必须是"否"**（除功能需要） |
| 5 | 是否新增了转换/映射层 | **必须是"否"** |

**违规判定**：只要出现"为统一命名而对既有列做 `CHANGE COLUMN` 改名"，或"为对齐风格而在实体上新增 `[SugarColumn(ColumnName)]`"，即视为违反本条。

**适用范围**：数据库（MySQL）、后端实体（`CertPlatform.Shared`）、前端 JSON 配置（`Assets/EntityConfigs/`）。框架表（`Sys_*`）中历史存在的 `Creator`/`CreateDate` 等旧列名属于**已登记的历史债务**，按 §16.7 单独处理，不得据此推定其他表也可随意改名。

**已登记例外清单（2026-09-22 补充）**

> 以下 7 类**不是实体字段**，不参与铁律七；在此登记以免被误判为违规、也避免被"顺手改成 PascalCase"而破坏既有数据或框架契约。

| # | 例外对象 | 实际风格 | 登记理由 | 改动后果 |
|---|---------|---------|---------|---------|
| E1 | `ApiResponse` 信封（`success`/`message`/`data`/`code`/`timestamp`） | camelCase | YZH.Core 框架层显式 `[JsonPropertyName]`，全平台统一 | 破坏所有前端响应解析 |
| E2 | 工作流落库 schema（`nodeId`/`nodeType`/`inputPorts`/`sourceNodeId`…，即 `RuleJson`/`WorkflowConfig`/`LayoutJson` 内部） | camelCase | 历史落库 payload 格式，DB 中已有存量数据 | 存量工作流全部失效 |
| E3 | 工作流引擎运行时载荷字典键（`NcResult` 内层 `success`/`error`/`result`/`executedPaths`…；`Output` 端口名） | camelCase | 由 Skill 执行器动态产出，非 schema | 结果面板与节点取值失效 |
| E4 | 服务层手写树 DTO（`organization-tree` 的 `id`/`label`/`cbCode`/`stdCode`/`phaseCode`…） | camelCase | 服务层专为树组件构造的展示 DTO，无 DB 对应列 | 左树整棵加载失败 |
| E5 | `NewEntity`/`Schema` 反射字典 key | camelCase | `EntitySchemaHelper` 反射时 `ToCamelCase` 产出 | 表单渲染失效 |
| E6 | **未包信封的裸 JSON 响应**（`GET api/User/getVierificationCode` 的 `{ img, uuid }`） | camelCase | 该端点用 `new JsonResult(new { img, uuid })` 直接输出，为兼容 Vol 前端契约而**故意不包 `ApiResponse`**；字段名与 DB 列无关 | 登录页验证码不显示（`yzhApi` 原样透传、不拆信封，读 `res.data.img` 会抛错） |
| E7 | **`{ code, data, message }` 无 `success` 信封**（`DocExtractionRuleController` 全系列 15 端点） | camelCase 信封 + PascalCase payload | 自旧项目 `DocExtractionRuleController` 逐端点平移而来，`data` 内层为 `TestFieldResponse`/`RuleDetailResponse` 等 C# DTO（PascalCase，受铁律七约束）；前端 `@share/api/workflow/doc-extraction-rule.ts` 已按 `{ code, data }` 定型 | 用 `res.success` 判定会恒为 `undefined` → 功能恒报"执行失败"（2026-09-22 节点测试 bug 即此因） |

> E6 判定技巧：`curl` 看响应体**顶层**有没有 `success`/`data`。没有 → 字段就在顶层，且不受铁律七约束。
> E7 判定技巧：`curl` 看响应体顶层**有 `code` 但没有 `success`** → 判定必须用 `res.code === 200`，读值用 `res.data`，**禁止用 `res.success`**。已登记此类控制器：`DocExtractionRuleController`（`/api/Workflow/DocExtractionRule/*`）。
> ⚠️ 同一后端存在**两个验证码端点**，风格不同，勿混用：`AuthController.GetCaptcha()`（路由 `api/User/captcha`）返回**带信封**的 `ApiResponse`；`UserController.GetVierificationCode()`（路由 `api/User/getVierificationCode`）返回**裸对象**。前端登录页走的是后者。

**判定口径**：一个字段是否受铁律七约束，看它**有没有 DB 列**。
有 DB 列 → 三处（DB 列名 / C# 属性名 / TS 字段名）必须逐字一致，PascalCase；
无 DB 列（纯信封、纯 payload、纯展示 DTO、动态字典）→ 走本例外清单，且必须在代码注释中标注"已登记例外"。

---

### 16.10 铁律八：字符集 / 排序规则全库统一 `utf8mb4_general_ci`（2026-09-23 新增）

> **⚠️ 本条为强制约束，优先级同铁律七。**
> 决策原文：「我们绝大部分的表是 `utf8mb4_general_ci`，先按这个统一……**全部依据 `utf8mb4_general_ci` 建设**，
> 针对非 `utf8mb4_general_ci`，重建表，**字符集不统一，后续问题会很多**。」

**唯一允许的组合：`utf8mb4` + `utf8mb4_general_ci`。**
全库（服务器 / 库 / 表 / 列 / 视图 / 存储过程）一律如此，**不接受任何例外**。

#### 一、为什么必须统一（真实事故，非理论风险）

「列 vs 列」比较或关联时两侧 collation 不同 → 直接报错：

```
ERROR 1267 (HY000): Illegal mix of collations
  (utf8mb4_0900_ai_ci,IMPLICIT) and (utf8mb4_general_ci,IMPLICIT) for operation '='
```

首次踩到（2026-09-23）：`Sys_RoleMenu.MenuCode`(`0900_ai_ci`) 与 `Sys_Menu.Code`(`general_ci`)
不一致，导致专家系统菜单授权 SQL 的 `NOT EXISTS ... rm.MenuCode = m.Code` 直接失败。

**⚠️ 为什么极易漏测**：**「列 vs 字面量」不会报错** —— 字面量 coercibility=4，列(=2) 胜出。
所以 `UPDATE ... WHERE Tag = ''`、单表增删改查**全部正常**，只有**跨表关联**才暴露。
⇒ 单表测试全绿、上线后才炸。**不要用"单表能跑"判断字符集没问题。**

#### 二、两个反直觉的根因（★ 必须记住）

| # | 写法 | 实际落到 | 说明 |
|---|------|---------|------|
| ① | `CREATE TABLE t (...) DEFAULT CHARSET=utf8mb4`（**不带 COLLATE**） | `utf8mb4_0900_ai_ci` | 取的是 **utf8mb4 这个字符集自身的默认排序规则**，**不是**库默认值 |
| ② | `SET NAMES utf8mb4`（**不带 COLLATE**） | `collation_connection = utf8mb4_0900_ai_ci` | **即使 `collation_server` 已是 `general_ci` 也如此**（2026-09-23 实测确认） |
| ③ | 建表**完全不写 charset** | 继承 `@@collation_server` / 库默认 | 治本需同时改服务器与库默认 |

⇒ **正确写法只有一种**：

```sql
-- 建表
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='...';

-- 迁移脚本开头：只要脚本里有 CREATE VIEW，就必须写这一行
SET NAMES utf8mb4 COLLATE utf8mb4_general_ci;
```

#### 三、视图是最隐蔽的一类残留

视图里 `CASE ... THEN '启用' ELSE '停用' END` 这类**字面量派生列**，其 collation 取自
**建视图时的 `collation_connection`** 并**固化在视图定义中**。表全改完了，视图里仍可能留着旧 collation。

- 实际残留：`v_cert_phase_definition.StatusName`（`utf8mb4_0900_ai_ci`）
- 另一类：`CAST(x AS CHAR CHARACTER SET utf8mb4)` **只给字符集不给排序规则** → 同样落到 `0900_ai_ci`
  （实际残留：`v_workflow.IsActiveName`）
- ⇒ 含视图的脚本**必须** `SET NAMES ... COLLATE ...`；对 `CAST`/`CASE` 表达式**再显式加**
  `COLLATE utf8mb4_general_ci` 更抗重跑（不依赖连接状态）
- ⚠️ 视图不能 `ALTER`，只能 `DROP VIEW` + `CREATE VIEW`；`DROP` 会**连带删除该视图上的授权**

#### 四、配置落点（四处必须一致，缺一即复发）

| 层 | 文件 | 要求 |
|----|------|------|
| 服务器 | `docker/compose.yml` → `command:` | `--collation-server=utf8mb4_general_ci`（**优先级高于 my.cnf**） |
| 服务器 | `docker/mysql/conf/my.cnf` → `[mysqld]` | `collation-server=utf8mb4_general_ci`；`init_connect='SET NAMES utf8mb4 COLLATE utf8mb4_general_ci'` |
| 客户端 | `docker/mysql/conf/my.cnf` → `[mysql]` | `init-command="SET NAMES utf8mb4 COLLATE utf8mb4_general_ci"` |
| 库 | `yzh_cert_platform` | `ALTER DATABASE ... CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci` |

> ⚠️ 客户端选项**只能放 `[mysql]`**，放 `[client]` 会让 `mysqldump` / `mysqladmin` 因未知变量报错。
> ⚠️ `docker/compose.yml` 的 `command:` 是**多行标量**，**不可在中间插入 `#` 注释** —— 会破坏 YAML 解析
> （2026-09-23 实际踩到，`docker compose up` 直接报 `did not find expected key`）。注释只能写在 `command:` **上方**。
> ⚠️ 改 `command:` 必须**重建容器**才生效：`cd docker && docker compose up -d mysql`（数据在 `./mysql/data` 卷中，不受影响）。

#### 五、禁止事项

1. ⛔ **禁止 `utf8mb3` / `utf8`**（旧字符集）：**无法存储 4 字节字符**（emoji、部分生僻汉字），写入即报 `Incorrect string value`。
2. ⛔ **禁止只写 `DEFAULT CHARSET=utf8mb4` 而不写 `COLLATE`**。
3. ⛔ **禁止在单侧写 `COLLATE` 提示来"绕开" 1267**（如 `ON a.X = b.Y COLLATE utf8mb4_0900_ai_ci`）。
   全库统一后此类提示是**有害**的：它把该表达式强制成另一种 collation，反而**制造**新的 1267。
   （2026-09-23 已清理 `fix_draft_folder_activate_V1.sql`、`20260920_fix_v_sys_user_add_isvalid_and_org_fields_V1.sql` 中的单侧提示。）
4. ⛔ **禁止为"统一"而逐列 `CHANGE COLUMN ... COLLATE`**：用整表 `CONVERT TO`（见第六节），否则必漏列。

#### 六、体检与修复

**体检（两个查询都必须返回 0 行）**：

```sql
-- 表级
SELECT TABLE_NAME, TABLE_COLLATION FROM information_schema.TABLES
 WHERE TABLE_SCHEMA = DATABASE() AND TABLE_TYPE = 'BASE TABLE'
   AND TABLE_COLLATION <> 'utf8mb4_general_ci';

-- 列级（含视图列）
SELECT TABLE_NAME, COLUMN_NAME, COLLATION_NAME FROM information_schema.COLUMNS
 WHERE TABLE_SCHEMA = DATABASE() AND COLLATION_NAME IS NOT NULL
   AND COLLATION_NAME <> 'utf8mb4_general_ci';
```

**修复整表**（重建表，同时转换该表**全部**字符列）：

```sql
SET FOREIGN_KEY_CHECKS = 0;                     -- ★ 必需：本库有 52 个外键，否则报 ERROR 1832
ALTER TABLE `t` CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
SET FOREIGN_KEY_CHECKS = 1;
```

> ⚠️ 筛选口径必须**两路取并集**：`表级 collation ≠ general_ci` **∪** `含任一非 general_ci 列`。
> 只按表级筛会漏掉「表级已是 general_ci、但个别列是 `utf8mb3`」的表 —— 2026-09-23 有 **34 张**此类表
> （共 37 个 `Remark` 列），是实际漏检点。

**一次性治理记录**：`scripts/db/20260923_unify_collation_V1.sql`
（68 张基表重建 + 2 个视图重建 + 5 个存储过程重建；外键 52 个不变；
转换前后数据**逐字节一致**：7,889,449 字节 / 13,689 行 `diff` 为空）。

---

**文档版本**：V3.2（与文首版本号对齐；此前文首为 V3.0 而文末停留在 V2.4，属历史维护遗漏）
**创建时间**：2026-07-27
**最后更新**：2026-09-23
**更新内容**：
- §16.10 **新增铁律八「字符集 / 排序规则全库统一 `utf8mb4_general_ci`」**：记录 2026-09-23 全库治理（68 张基表 + 2 个视图 + 5 个存储过程重建），给出两个反直觉根因（`DEFAULT CHARSET=utf8mb4` 不带 COLLATE → `0900_ai_ci`；`SET NAMES utf8mb4` 不带 COLLATE → 连接落到 `0900_ai_ci`）、视图字面量派生列的固化陷阱、四处配置落点、体检与 `CONVERT TO` 修复口径
- §11.1 修正过时的脚本存放路径（`src/server/Vue.NetCore/DB/mysql/` → `scripts/db/`），并补充字符集强制要求
- §0.1 新增「核心目的与架构演进」：明确新架构是唯一开发目标、旧项目禁止修改
- 新增架构分层表：新后端（certplatform-api/）、新前端（certplatform-web/）、旧后端（src/server/）、旧前端（src/admin/、src/auditor/）
- 新增 AI 行为约束：禁止修改旧架构、禁止调用旧架构控制器/服务
- §十六 新增「数据库-实体命名铁律」：统一审计字段、业务字段命名规范、ORM 映射规则、历史遗留迁移策略
- §16.9 新增铁律七「DB 列名 / C# 属性名 / JSON 字段名原样一致」：现有 PascalCase 列名系刻意设计，禁止再做拼写/大小写改写或局部映射
- §16.9 增补「③′ 前端」：TS 侧字段名必须与 DB/C#/配置逐字一致，前端 camelCase 读取即违规
- §16.9 增补「已登记例外清单」E1–E6（ApiResponse 信封 / 工作流落库 schema / 运行时载荷字典 / 组织树展示 DTO / NewEntity-Schema 反射字典 / **未包信封的裸 JSON 响应**），并给出「有无 DB 列」的判定口径
- 补充 E6 的判定技巧与「两个验证码端点风格不同」的提醒（`api/User/captcha` 带信封 vs `api/User/getVierificationCode` 裸对象）
- §16.9 例外清单扩充至 E1–E7：新增 E7「`{ code, data, message }` 无 `success` 信封」（`DocExtractionRuleController` 全系列），给出判定技巧并记录 2026-09-22 节点测试恒报"执行失败"的成因

---
*（内容由AI生成，仅供参考）*
*（内容由AI生成，仅供参考）*

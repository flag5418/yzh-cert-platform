# GridConfig - 核心默认配置

此目录存放 yzh.vue.core 核心库默认的表格/表单配置。

**优先级规则**：项目级 (`admin/src/assets/gridconfig/`) > 核心库默认

## 默认配置

如果项目未提供自定义配置，后端 `GridConfigController` 会从此目录读取默认配置。

实际使用时，配置文件位于后端 `YZH.Core.Web/GridConfigs/` 目录。
此目录仅供开发参考和文档说明。

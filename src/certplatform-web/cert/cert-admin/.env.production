# cert-admin — 正式环境（npm run build）
# ⛔ 禁止放密钥。

# 默认留空 = 同源部署（前端与后端经同一域名/nginx 暴露），请求走 /api/*
# 跨域部署（前后端不同域名）时改为后端绝对地址，例如：
#   VITE_API_BASE=https://api.example.com
VITE_API_BASE=

VITE_APP_TITLE=映智汇认证平台
VITE_APP_ENV=production

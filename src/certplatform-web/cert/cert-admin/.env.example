# ============================================================
# cert-admin（管理员端）— 环境变量模板
# ------------------------------------------------------------
# 用法：复制为对应环境文件后填写
#   .env.development   本地开发（npm run dev）
#   .env.staging       测试环境
#   .env.production    正式环境（npm run build）
#
# ⛔ 禁令（架构契约）：
#   1. 本文件**只允许**放「构建期注入的公开配置」（VITE_ 前缀）。
#   2. ⛔ 禁止放任何密钥/口令/Token —— 前端产物对用户完全可见。
#   3. ⛔ core（yzh.vue.core）**不得**读取 import.meta.env，环境由宿主注入
#      （守卫 R11）；宿主通过 configureYzhApi() 把值传给 core。
# ============================================================

# 后端接口根地址
#   留空 = 相对路径 /api/*（开发走 vite proxy → 127.0.0.1:9992；生产走同源/nginx）
#   跨域部署时才填写绝对地址，如 https://api.example.com
VITE_API_BASE=

# 应用标题（显示在浏览器标签与登录页）
VITE_APP_TITLE=映智汇认证平台

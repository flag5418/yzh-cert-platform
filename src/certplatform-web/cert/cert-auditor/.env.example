# ============================================================
# cert-auditor（认证专家端）— 环境变量模板
# ------------------------------------------------------------
# 用法：复制为 .env.development / .env.staging / .env.production 后填写
#
# ⛔ 禁令：禁止放密钥；core 不得读 import.meta.env（守卫 R11），
#         环境值由宿主经 configureYzhApi() 注入 core。
# ============================================================

# 后端接口根地址（留空 = 相对路径 /api/*，开发走 vite proxy → 127.0.0.1:9992）
VITE_API_BASE=

VITE_APP_TITLE=映智汇认证专家平台
